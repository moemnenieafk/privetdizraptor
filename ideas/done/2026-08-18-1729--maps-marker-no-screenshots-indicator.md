---
type: idea
status: done
game: eft
value: приятно          # ~вывод (QoL, только админ)
effort: S               # ~вывод (условная иконка на маркере)
source: telegram
tg_from: v4dyadesign
tg_date: 2026-08-18T17:29:15Z
tg_message_id: 345
tg_thread: 140
tg_topic: ИДЕИ
tg_link: https://t.me/c/3424189876/140/345
---
# Карты: индикатор image-off на маркере без скриншотов (админ-режим)

- **Раздел:** Карты — `/eft/maps` (editorial-маркеры на карте)
- **Компонент:** рендер editorial-маркера в слое карты (`MapViewerClient`, editorial layer) `~вывод`
- **Идея (QoL):** на маркере, у которого НЕТ скриншотов, показывать иконку-индикатор lucide **`ImageOff`** — **только в режиме Админ** (`canEditMarkers`). Слева от маркера.
- **Зачем / польза:** наглядно видно, где надо дозагрузить скриншоты, не открывая каждый маркер.
- **Ожидаемый результат:** админ смотрит на карту → маркеры без медиа сразу помечены `image-off` слева.

> исходное сообщение (as-is):
> QoL - Поставить иконку-индикатор на маркере image-off из lucide - когда нет скриншотов для  просмотра - чисто  для режима  Админ. Слева от маркера. Удобство видно наглядно где надо дозагрузить скриншоты

**Заметка (`~вывод`):** условие — `screenshots.length === 0`; видимость — гейт по `canEditMarkers` (не показывать обычным игрокам). Иконка — `ImageOff` (lucide), маленький бейдж слева от глифа маркера.

## Резолюция (done, 2026-08-18)
Решение V4DYA (грилл): охват — **только «контентные» типы** (poi/лут/контейнер/quest_zone/lock/switch/stationary), спавны/выходы/опасности без бейджа (шум). Цвет — **danger** (акцент; hazard из охвата исключён → конфликта нет).
**Файлы:** `MapViewerClient` — module-хелперы `IMAGE_OFF_CONTENT_TYPES`/`needsImageOff`/`syncImageOffBadge` (danger-SVG `ImageOff` слева от капли, зеркально `cta-floor-hint`), вшито в создание editorial-маркеров (капля+полигон-центроид), деп `canEditMarkers` · `globals.css` (`.cta-img-off`, `right:100%`, `--color-danger`).
**Верифай:** код-ревью + tsc/lint без новых замечаний (бейдж — DOM-append по образцу floor-hint, гейт `canEditMarkers`).
