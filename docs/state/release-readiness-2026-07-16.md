---
type: state
date: 2026-07-16
version: v0.2.0 (package.json — расходится с реальностью)
audit: сверка заметок ↔ код `main` + прямой спот-чек релиз-блокеров в коде
supersedes: "[[release-readiness-2026-07]]"
---

# Срез готовности к релизу — 16.07.2026

Обновляет [[release-readiness-2026-07]] (14.07). С тех пор релиз-гейт почти полностью закрыт:
прямой спот-чек кода `moemnenieafk/privetdizraptor@main` показывает, что 5 из 6 шоу-стопперов
14.07 уже в проде, а последний (RLS Comlink) закрыт этой сессией.

## Главный вывод

**К запуску бесплатным ядром готово. Технических блокеров релиза не осталось** — кроме
одной ручной проверки (юр-текст). Всё остальное из гейта — в коде и подтверждено.

## Релиз-гейт — статус по коду (спот-чек 16.07)

| Шоу-стоппер 14.07 | Статус | Подтверждение в коде |
|---|---|---|
| SEO-минимум (sitemap/robots/OG) | ✅ закрыт | `src/app/sitemap.ts`, `robots.ts` есть; root `layout.tsx` — `metadataBase` + `openGraph` + реальные title/description (не стайлгайд) |
| Страница 500 | ✅ закрыт | `error.tsx` + `global-error.tsx` на месте |
| Мёртвые ссылки футера | ✅ закрыт | в `footerConfig.ts` не осталось `href:'#'`/`TODO` |
| `/eft/styleguide` индексируется | ✅ закрыт | `page.tsx`: `robots: { index: false }` |
| **UGC без RLS (Comlink)** | ✅ **закрыт 16.07** | RLS был встроен в `COMLINK_DDL`; добавлен канонический `supabase/comlink-rls.sql` + верификация RLS в `/api/cron/migrate-comlink`; **прогон подтвердил** (`rlsAllOn`) |
| Юр-текст (черновик) | 🟡 **проверить** | аудит 14.07 отметил terms/privacy переписанными под 152-ФЗ — убедиться визуально, что баннера «ЧЕРНОВИК» больше нет на `/legal/{terms,privacy}` |

→ После визуальной проверки юр-текста — **можно пускать.**

## Готовность по модулям (факт по коду, дельта к 14.07)

| Модуль | 14.07 | 16.07 | Что осталось |
|---|---|---|---|
| Предметы | ✅ 95% | ✅ 95% | спот-чек категорий с 0 предметов; **инцидент 16.07 (пустой каталог из-за PvE-колонок) — устранён**, чтение цен теперь устойчиво |
| Карты | 🟡 85% | 🟡 88% | маркеры v1 **фазы A–J done + live-проверено** (под-виды выходов, спавн-фракции rogue/black-div, боссы-портреты на карте, loose loot клик-линки + кластеризация, замки, dev-tool пикер боссов). Остаток → карты v2: позиции Лабиринт/«Конец пути», локации без SVG, дизайн-система карт |
| Прогресс | ✅ 95% | ✅ 95% | `hideout/[metric]` — фолбэк-заглушка |
| Задания | ✅ 90% | ✅ 90% | галереи скриншотов 9 историй (heroImage у всех 10 ✅) |
| Кодекс | ✅ 85% | ✅ 85% | расширение статей; Кодекс→БД сделан (CMS E10.3) |
| Видео | ✅ 90% | ✅ 90% | ждёт `YOUTUBE_API_KEY` (env) + 4 playlist-ID от Димы |
| Связь (Comlink) | 🟡 80% | ✅ **90%** | **RLS закрыт 16.07**; осталась только текущая модерация по мере наполнения |
| Аккаунт | ✅ 90% | ✅ 90% | 2FA/биллинг — отдельные пост-MVP фичи |
| Роли/«Ульта» | — | 🟢 **код впереди заметки** | движок ролей, RolePicker, хаб, PvE-ось, RU-роли (Крыса/Шерп/Лорник) — в main. Остаток: поведенческий слой + объяснимость + гейт инсайтов. Статус [[adaptive-hub-ulta]] двинуть с 🔵 |
| Монетизация | 🔴 30% | 🔴 30% | таблицы `subscriptions` в схеме НЕТ → премиум не включить; paywall только на сборках; рельс оплаты не выбран (Boosty ~20% отклонён → кандидат ЮKassa) |
| Цены PvE | — | ✅ **инфра готова** | схема+синк+чтение; колонки мигрированы 16.07; PvE-значения подтянутся на синке |

## Пост-MVP — приоритеты (после запуска)

1. **Монетизация E4/E5** → [[monetization-subscriptions]]: таблица `subscriptions`+RLS, разметка `<Paywall>` по разделам, выбор рельса оплаты.
2. **Карты v2** → [[maps-marker-render]] (маркеры v1 фазы A–J done) · [[maps-v2-scope]] · [[maps-design-system]].
3. **Галереи историй** → [[story-content-assets]] (9 из 10 ждут скриншотов).
4. **CMS фаза 5** → [[cta-content-management-system]] (истории `src/data`→БД; фазы 1–4 в проде).
5. **«Ульта» — доводка** → [[adaptive-hub-ulta]] · [[player-roles-ru-expansion]].
6. **Горизонт:** [[multigaming-second-game]] · [[NightFall UI]]/[[UI 2.0 - Enhance]] · [[source-mrsouer]].

## Мелочи (не блокеры)
- Версионирование: `package.json` = `0.2.0`, CHANGELOG обрывается на `4.3.0` — выровнять.
- Кроны: в `vercel.json` только `sync-prices`; `sync-weapons`/`sync-patches`/`migrate-*` — `workflow_dispatch`. Проверить живость.
- Перф: Leaflet — проверить `next/dynamic` `ssr:false` на всех точках входа.

## Сделано этой сессией (16.07)
- Инцидент «пустой каталог»: чтение цен (`db/prices.ts`) сделано устойчивым к отсутствию PvE-колонок — порядок «деплой → миграция» больше не роняет каталог (`debb0329`).
- PvE-колонки мигрированы в прод (`migrate-prices-pve`).
- Comlink RLS: канонический `supabase/comlink-rls.sql` + верификация в миграции; прогон подтвердил RLS на всех таблицах раздела (`85d20404`).
