---
type: state
date: 2026-07-14
version: v0.2.0 (package.json — расходится с реальностью)
audit: глубокая сверка заметок ↔ код `main`
---

# Срез готовности к релизу — 14.07.2026

> [!warning] Заменён свежим срезом → [[release-readiness-2026-07-16]] (16.07.2026). Оставлен как история.

Сверка **всех заметок валта с фактическим кодом** `moemnenieafk/privetdizraptor@main`.
Предыдущий срез — [[snapshot-2026-06]] (устарел), инвентаризация — [[pre-mvp-release-audit]] (Проход 1, 05.07).

## Главный вывод

**Продукт к запуску бесплатным ядром готов.** Заметки сильно отстают от кода: с 05.07 в `main` уехали
целые разделы, которых нет ни в одном решении (Связь/Comlink, Сборки оружия + Gunsmith-солвер,
Видео-архив, Loot-контейнеры). Роутов было 50 → стало **73**.

**Блокирует релиз не продукт, а релиз-гигиена:** SEO-минимума нет, 500-страницы нет, юр-текст —
черновик, футер с мёртвыми `#`, и (новое, важное) — **появился UGC-раздел без аудита RLS**.

## Готовность по модулям (факт по коду)

| Модуль | Было (menu-audit 29.06) | Сейчас | Что осталось |
|---|---|---|---|
| Предметы | ✅ 95% | ✅ **95%** | спот-чек категорий с 0 предметов (D1) |
| Карты | ✅ 95% | 🟡 **85%** | Лаба — свой арт (статик, маркеры не геопривязаны); «Конец пути» нет; маркеры v2 — фазы A+B |
| Прогресс | 🟡 80% | ✅ **95%** | ✅ Сборки оружия построены (стат-движок, квоты, каталог, Gunsmith); `hideout/[metric]` — фолбэк-заглушка |
| Задания | 🟡 70% | ✅ **90%** | ✅ 10 сюжеток + События реальны; нет галерей скриншотов (9 историй) |
| Кодекс | 🟠 45% | ✅ **85%** | ✅ все 8 лор-разделов написаны (по 1 статье на категорию) — можно расширять |
| Видео | 🔴 10% | ✅ **90%** | ✅ архив + категории + Twitch/YouTube-фид; ждёт `YOUTUBE_API_KEY` + playlist-ID |
| **Связь (Comlink)** | — (не было) | 🟡 **80%** | **новый раздел:** форум, рейды, шерпы, карма, отчёты, блог, обновления игры. Нужен аудит RLS + модерация |
| Аккаунт | ✅ | ✅ **90%** | трекинг-хаб со всеми доменами закрыт |
| Монетизация | ⛔ | 🔴 **30%** | инфра гейта есть (`useSubscription`/`<Paywall>`/квоты), применена только к сборкам; **таблицы `subscriptions` в схеме НЕТ**; рельс оплаты не выбран |

## ✅ Закрыто 14.07 (6 коммитов в `main`)

`sitemap.ts` + `robots.ts` + корневые метаданные · `error.tsx`/`global-error.tsx` (500) ·
реальный юр-текст terms + privacy (152-ФЗ) · чистка мёртвых ссылок футера ·
RLS-аудит Comlink (код чист; остался ручной прогон `migrate-comlink`).

## 🔴 Шоу-стопперы релиза (исходная фиксация 14.07 — все закрыты, кроме ручных шагов)

1. **SEO-минимум отсутствует.** Нет `src/app/sitemap.ts`, нет `robots.ts`, нет `metadataBase`/`openGraph`.
   В `layout.tsx` корневые мета — **стайлгайдовские**: `title: "ЦТА Хаб"`, `description: "Технический паспорт дизайн-системы и игровой хаб"`. На паблике это лицо сайта в выдаче и в превью ссылок.
2. **Нет страницы ошибки 500.** `not-found.tsx` есть, `error.tsx` / `global-error.tsx` — нет.
3. **Юр-текст — черновик.** `/legal/{terms,privacy,offer,eula}` резолвятся, но с баннером «ЧЕРНОВИК, не имеет юридической силы» (`legal-docs.ts:1`). При живых аккаунтах и будущих платежах — правовой риск (152-ФЗ).
4. **Мёртвые ссылки футера.** `footerConfig.ts`: VK, X, Reddit + «Партнёрская программа», «Интеграции», «Экосистема ЦТА» = `href="#"` с `// TODO`. → дать URL или скрыть.
5. **UGC без аудита RLS (новое).** Comlink принимает пользовательский контент (темы, посты, отчёты, профили, рейды). Таблицы заведены DDL-роутом (`db/comlink-ddl.ts`), но в `supabase/` **нет** RLS-скрипта для них (для остальных доменов файлы есть). Перед паблик-запуском форума — обязательный `/security-review` + RLS.
6. **`/eft/styleguide` публичен и индексируется** — внутренняя страница, закрыть `noindex` или убрать из паблик-навигации.

## 🟡 Не блокеры, но до релиза стоит

- `subscriptions`: `fetchTier()` мягко падает в `free`, если таблицы нет — гейт работает, но премиум включить нельзя. При старте монетизации: `db:sql` + RLS (own row).
- Кроны: в `vercel.json` только `sync-prices` (05:00). `sync-weapons` / `sync-patches` / `migrate-*` — `workflow_dispatch`. Проверить живость перед запуском.
- CHANGELOG.md обрывается на `[4.3.0] — 23.06`, `package.json` = `0.2.0`. Версионирование рассинхронизировано с реальностью.
- Перф: Leaflet — проверить lazy-загрузку (`next/dynamic`, `ssr:false`) на всех точках входа.

## Что делать (быстрый путь к запуску)

1. **Релиз-гигиена (1 сессия, S):** `sitemap.ts` + `robots.ts` + нормальные `metadata`/OG в root layout, `error.tsx`, `noindex` на styleguide.
2. **Футер:** URL от V4DYA или скрытие TODO-ссылок → [[cta-footer-redesign]] (ЧП4 — единственный остаток эпика).
3. **Юр-текст:** privacy + terms реальным текстом (генератор под 152-ФЗ / юрист).
4. **RLS-аудит Comlink** + `/security-review` → [[pre-mvp-release-audit]] Проход 2.
5. Релиз бесплатным ядром. Монетизация (E4/E5), маркеры карт (E6/E7), CMS (E10) — пост-MVP.

## Заметки, закрытые этой ревизией (12)

`supabase-jwt-fix` · `fsd-lite-normalize` · `footer-v2` · `feedback-form` · `loot-contaier-table-page` ·
`mobile-first-audit` · `player-tracking-tab` · `tracking-favorite-items` · `the-lab-parity` ·
`player-position-tracking` · `deep-research-subscription-monetization` · `deep-research-payment-rails` → `decisions/done/`.

## Остались активными (13)

**Релиз:** [[pre-mvp-release-audit]] (Проход 2) · [[cta-footer-redesign]] (ЧП4).
**Монетизация:** [[monetization-subscriptions]] (E4 разметка + E5 рельс).
**Карты:** [[maps-marker-render]] (список правок иконок/боссов/спавнов) · [[maps-v2-scope]] · [[maps-design-system]].
**Контент:** [[story-content-assets]] (галереи 9 историй) · [[source-mrsouer]].
**Горизонт:** [[cta-content-management-system]] · [[multigaming-second-game]] · [[NightFall UI]] · [[UI 2.0 - Enhance]] · [[additional-workout]] (мастер-план).
