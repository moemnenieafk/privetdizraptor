---
kind: state-snapshot
topic: слои карт EFT из клиента — прогон по всем картам
date: 2026-09-04
canon: docs/decisions/eft-map-layers.md · docs/decisions/eft-map-layers-runbook.md
commits: 579611fe (слой 7), cb831294 (все карты), ветка feat/lighthouse-terrain — НЕ запушена
---

# Срез: слои карт EFT — что собрано, где лежит, чему верить

Ночной прогон конвейера по всем картам портала. **Собрано восемь карт вместо двух.**
Разбор причин и гочи — в каноне (`eft-map-layers.md`), порядок запуска — в ранбуке.
Здесь: где файлы, какие числа получились и **чему из этого можно верить**.

## 1. Где всё лежит

| Что | Путь |
|---|---|
| **Артефакты слоёв** | `map-exports/OBJECTS-MAPS/gen/<карта>/<слой>/` — вне git, 1.8 ГБ |
| Инструменты | `scripts/eft-map-layers/` · `scripts/eft-terrain/` · `scripts/eft-rooms/` — в git |
| Общее ядро слоёв | `scripts/eft-map-layers/mapgeom.py` (сцены, меши, рамка, земля, сечение) |
| Промежуточное (дампы) | `D:\eft-export\<карта>\` — вне git |
| Растр и манифесты | `D:\Games\raster\<карта>\manifest.json` |
| **Логи этой ночи** | `D:\eft-export\night-logs-2026-09-04\` (по шагам на карту + превью) |

Слои внутри карты: `ground` (высоты, отмывка, материал, растительность) · `rooms` (комнаты +
**аффина рамки**) · `zone` · `roads` · `stones` · `walls` · `obstacles` (**слой 7**) · `render`.

| Карта | МБ | Слои, где есть файлы |
|---|---|---|
| Лес | 395 | ground rooms zone stones walls obstacles render |
| Маяк | 384 | ground rooms zone roads stones walls obstacles render |
| Резерв | 288 | ground rooms zone roads stones walls obstacles render |
| Развязка | 272 | ground rooms zone stones walls obstacles render |
| Берег | 221 | ground rooms zone stones walls obstacles render |
| Таможня | 203 | ground rooms zone roads stones walls obstacles render |
| Ground Zero | 49 | rooms roads stones walls obstacles |
| Терминал | 20 | rooms stones walls obstacles |
| Ледокол | 4 | rooms zone walls obstacles |

## 2. Слой 7 «препятствия выше 1 м» — по картам

| Карта | Рез | Пересекли рез | Контуров | Покрытие рамки | Самопроверка камнями |
|---|---|---|---|---|---|
| Таможня | рельеф | 12 695 | 11 592 | 2.63 % | 79/83 |
| Маяк | рельеф | 10 214 | 13 473 | 8.03 % | **877/903** |
| Резерв | рельеф | 10 509 | 8 052 | 12.23 % | — |
| Развязка | рельеф | 8 855 | 14 552 | 4.65 % | 11/11 |
| Берег | рельеф | 8 388 | 10 386 | 3.06 % | — |
| Лес | рельеф | 5 271 | 9 083 | 4.37 % | 7/7 |
| Ground Zero | плоскость 23.50 м | 6 146 | 3 716 | 3.74 % | — |
| Ледокол | плоскость 18.50 м | 1 110 | 447 | 1.49 % | — |
| Терминал | плоскость −59.50 м | 30 188 | 29 640 | **0.00 %** | ⚠️ **мимо рамки** |

«Рельеф» = порог считается повершинно по карте высот. «Плоскость» = у карты нет террейна
вовсе, пол оценён модой низов геометрии и напечатан вслух.

## 3. ⚠️ Чему верить, а чему нет

Три вещи выглядят собранными, но собранными не являются. Все три ловятся ТОЛЬКО отношением
к рамке, а не счётчиками — счётчики у всех трёх здоровые.

### Зона: у четырёх карт это карман, а не игровая зона

Панели границы не образуют замкнутого кольца, и заливка находит случайный карман внутри карты.
Заведена гарда: доля площади зоны от рамки, поле `plausible` в JSON.

| Карта | Зона | Доля рамки | Верить? |
|---|---|---|---|
| Таможня | 31.29 га | 53.8 % | ✅ |
| Маяк | 66.11 га | 36.2 % | ✅ |
| Ледокол | 0.56 га | 29.6 % | ✅ (корабль и должен быть маленьким) |
| Развязка | 1.23 га | 1.4 % | ❌ карман |
| Лес | 2.45 га | 1.3 % | ❌ карман |
| Берег | 1.62 га | 1.0 % | ❌ карман |
| Резерв | 0.32 га | 1.0 % | ❌ карман |
| Ground Zero, Терминал | — | — | нет корня `*_LevelBorders` в сценах |

**Следствие:** на этих четырёх картах маской зоны НЕЛЬЗЯ клипать соседние слои — отрежет живое.
Флаг `inGameZone` у камней и деление слоя 7 на «в зоне / снаружи» там недостоверны. Сам слой 7
от этого не теряет данные: то, что вне зоны, попадает в группы `*-outside`, а не выбрасывается.

### Дороги: у Резерва и Ground Zero слой пустой

Шаг отработал с кодом 0, а полотна **0 м²**: у обеих карт нет ветки `OO/ROAD` (у Резерва есть
`OO/ROADS` с 421 объектом, дефолтный отбор по ней не идёт). Профиль дорог правится руками
на карту — `recon` даёт числа, решение за глазами.

Реально собраны дороги только у **Таможни** (полотно 36 782 м² после клипа зоной) и **Маяка**
(57 763 м²). У Леса, Берега, Развязки, Терминала и Ледокола дорожной сцены нет в реестре вовсе.

### Терминал лёг мимо рамки

30 188 пересечений, 29 640 контуров, **ноль закрашенных пикселей**: геометрия карты в
px[−29 240, −4 226] при рамке [0, 0, 13 181, 16 384]. Комнаты стоят в X≈195 / Z≈−223, а
синтетический манифест описывает X[−1605, −675] / Z[315, 1471] — то есть `bounds` Терминала
в `EFT_MAP_CONFIG` не соответствуют мировым координатам карты. Гарда `checks.outsideFrame`.

## 3б. Починенный дефект рендера (2026-09-04, `aa76cab5`)

**Швы нарезки на камнях и растительности — были на ВСЕХ картах, сейчас нет.**
Неквадратный пиксель рамки отдавался Blender'у через `pixel_aspect_y`, и движок выводил
вертикальный охват ортокамеры не так, как предполагал код: внутри каждой плитки вертикальный
масштаб уезжал на 1.6 %, а на каждой границе рядов плиток содержимое повторялось на 61 px.

| Замер | До | После |
|---|---|---|
| Скачок альфы на границах рядов плиток | 14–82 сигмы | −0.8…+4.1 сигмы |
| Наклон смещения внутри плитки (459 камней) | −0.0160 px/px = −33 px за плитку | −0.0005 px/px = −1 px |
| Медиана смещения | — | −0.5 px |

Перерендерены все шесть карт с рендером: Таможня, Лес, Берег, Резерв, Развязка, Маяк.
Заодно убран дефолт `--base-art` на арт Таможни — из-за него превью Леса и Берега
композились поверх чужой картинки.

### Камни рендерились белыми — шейдер мха (`b54789ff`)

У семейств `AM_Rock_*` / `Arid_rock_*` материал использует шейдер смешивания со мхом со своим
словарём свойств (`_BaseAlbedoASmoothness`, `_TopAlbedoASmoothness`, `_BaseNormalMap`…).
Ни одно привычное имя не заполнено → текстура не находилась → белые пятна.

| Карта | Прототипов с текстурой было | Стало |
|---|---|---|
| Маяк | 34 из 84 | **84 из 84** |
| Берег | 57 из 98 | **98 из 98** |
| Развязка | 25 из 49 | **49 из 49** |
| Лес | 47 из 59 | **59 из 59** |
| Резерв | 34 из 35 | **35 из 35** |
| Таможня | 38 из 38 | 38 из 38 |

### Растительность Резерва — 679 вместо 18 513 (`a4f0f076`)

Деревья лежат внутри `TerrainData`, а список файлов ассетов для их чтения брался из признака
реестра, которого у Резерва не было. Извлекался только «свой» слайс, покрывающий левый верхний
квадрант рамки — снизу карты растительности не было вовсе.

| Замер | До | После |
|---|---|---|
| Экземпляров извлечено | 679 | **18 513** |
| Внутри рамки | 679 | 4 283 |
| Покрытие рамки | 40.7 % × 29.9 % | 100 % × 100 % |
| Полос карты (из 8) с растительностью | 3 | 8 |

Дампер пишет сайдкар `<map>-terrain-sources.json` по факту прогона; `run-all` читает его вместо
признака реестра. Остальные карты не затронуты — у них покрытие было ~100 %.

## 4. Не собрано осознанно

| Карта | Почему |
|---|---|
| **Завод** (поворот 90°), **Лаборатория**, **Лабиринт** (270°) | Рамка умеет только 0 и 180. Разворот «на глаз» даёт у Завода **13 % анизотропии** масштаба (118.8 против 104.8 px/м) — квадратное здание легло бы прямоугольником. Привязку надо ВЫВОДИТЬ: у Завода есть `worldTransform` в `EFT_MAP_CONFIG` (11 опор, невязка 0.7 м). |
| **Улицы Таркова** | Нет ни манифеста, ни записи в `EFT_MAP_CONFIG` — рамку строить не из чего. |

## 5. Как воспроизвести

```bash
python scripts/eft-map-layers/run-all.py <карта>              # весь конвейер, 13 шагов
python scripts/eft-map-layers/run-all.py <карта> recon        # разведка, ~15 с
python scripts/eft-map-layers/run-all.py <карта> --only obstacles --force-step obstacles
python scripts/eft-terrain/check-orientation-gate.py          # харнесс гейта, ждём «5 из 5»
```

Шаг с готовым и свежим выходом пропускается сам, поэтому повторный прогон карты дёшев.

## 6. Открытые вопросы

1. **Расхождение с калибровкой сайта на Таможне — НЕ ПРОВЕРЕНО.** `worldTransform` из
   `EFT_MAP_CONFIG` даёт `canvasX = 10803.2 − 15.313·gx`, рамка слоёв — `10687.2 − 15.3112·gx`:
   разница **116 px ≈ 7.6 м по X**. По Y всё сходится (4054 px — это центрирование полосы арта
   в квадратном холсте тайлов). Наши оверлеи ложатся на арт пиксель в пиксель, значит подозрение
   на калибровку сайта; если она врёт, синканные маркеры Таможни смещены на 7.6 м.
   Проверять на живых маркерах.
2. **Камни Таможни на юго-востоке смещены влево-вверх на 6–12 м** относительно нарисованных,
   при этом заборы и трубы совпадают пиксель в пиксель. Кроп для глаз:
   `gen/customs/obstacles/customs-obstacles-stones-offset.jpg`. Решать V4DYA: арт или геометрия.
3. **Классы на картах без ветки `BUILDING`.** У Развязки таких веток ноль, поэтому стены и
   реквизит сваливаются в один класс `props`. Содержимое и силуэты верны, разбивка по классам —
   нет.
4. **Плоский рез на многоуровневой карте без террейна** показывает только уровень основания.
   Ground Zero с его перепадом честнее смотреть слоем стен по этажам.
5. **У стен только SVG, растра нет.** Теней на землю нет, рендер зданий не делался.
