---
type: state
status: 🟢 срез приоритетов 16.08 — релиз-гейт бесплатного ядра закрыт; борд почищен; голосовые 28.07 разобраны
affects: release, quests, questmap, trackers, maps, item-card, monetization, cms, seasons, companion
date: 2026-08-16
supersedes: release-readiness-2026-07-16
---

# Срез 16.08.2026 — приоритеты исполнения и планирования

Сквозной аудит всех живых заметок `docs/` (167 файлов, из них 75 — архив `done/`) и расстановка приоритетов разработки. Заменяет устаревший на месяц [[release-readiness-2026-07-16]] (до патча 1.1.0.0).

## Картина в одну минуту
1. **Релиз-гейт бесплатного ядра по сути закрыт** — технических шоу-стопперов нет, остались ручные проверки. Запуск не ждёт ни монетизации, ни киллер-фичи, ни сезонов.
2. **Узкое место — не разработка, а V4DYA и контент.** Львиная доля «почти готового» упирается в дизайн-ответы и ручной захват скринов, а не в код.
3. **Стратегия расставлена верно:** «Я в рейде» — post-launch, монетизация — post-MVP, компаньон — зрелый, но не блокер.
4. **Главный открытый функциональный кластер — граф квестов** (баги + ревизия данных), см. [[Ревизия MVP — сверка голосовых 28.07]].

## Что сделано в этой сессии (16.08)
- **Гигиена борда:** 7 фактически готовых заметок доархивированы в `decisions/done/` (борд: 33 → 26 живых). См. журнал ниже.
- **Голосовые 28.07 разобраны** → [[Ревизия MVP — сверка голосовых 28.07]] (сверка с раскладкой 25.07 + прод; открытый остаток вычленен).

---

## ⚠️ Срочное / ручное (только V4DYA)
- 🔴 **Проверить, снята ли локальная копия SPT** (`sp-tarkov/server` + `D:\Games\SPT`) — дедлайн ~12.08 (иск BSG) истёк; необратимая потеря резерва данных, если архива нет.
- **Релиз-блокеры (ручные):** визуально проверить отсутствие баннера «ЧЕРНОВИК» на `/legal/terms` и `/legal/privacy`; мобилка-смок ключевых страниц на устройстве.

## Приоритеты ИСПОЛНЕНИЯ

### P0 — закрыть релиз-гейт (запустить бесплатное ядро) · всё S
- Ручные проверки выше · скрыть кнопку **Twitch-авторизации** (санкции РФ, основной вход email/пароль) · деплой-верификация фикса анимации плиток на 2К · спот-чек пустых категорий предметов ([[menu-audit]] D1).

### P1 — самое ценное, стартует сейчас
1. **Починка дампа квестов** — `scripts/dump-quests.mjs`: мёртвый GraphQL → `json.tarkov.dev/tasks`, пере-дамп под 1.1.0.0. Краеугольный: разблокирует реконсиляцию квестов + questmap. Автономно.
2. **Граф квестов + трекеры** (из [[Ревизия MVP — сверка голосовых 28.07]]) — баги на отгруженных фичах: кнопка «Выполнено» не работает, поиск→граф вместо карточки, дубли/сброс трекера, лаг графа, двусторонние линки, объединение «Важные»+«Трекер». Сперва пере-проверить по коду (3 недели прошло), гейтится дизайном квест-вью (спринт УЛ 1.1.0.0).
3. **Левая колонка карточки предмета** ([[snapshot-2026-07-21]]) — верстается сразу; шапка/чипы/мобилка ждут 2 дизайн-ответов V4DYA. NB: «ждёт tarkov.dev» больше не блокер (json-дамп за кроном работает) — нужен live-пересмотр остатка.

### P2 — платное ядро (после запуска бесплатного)
- [[feature-gating-entitlements]] F1–F4 — стартует сейчас, **не** ждёт рельса (реюзит `subscription.server.ts`/`roles.ts`/karma) → затем платёжный рельс E5 из [[monetization-subscriptions]] (единственный жёсткий блокер — research Димы по эквайрингу, ЮKassa-кандидат; Boosty отвергнут ~20%).

## Приоритеты ПЛАНИРОВАНИЯ (решение/спека до кода)
- **«Я в рейде» Фаза 0** ([[Я в рейде — киллер-фича]]) — спайк матчера иконок (go/no-go) отдельной сессией. Движок переизобретён (матчинг 5044 иконок вместо Gemini → бесплатно, без биллинга). Post-launch, но Фазу 0 можно гонять параллельно.
- **[[effective-price-nesting]]** — колл V4DYA A/B/C; функциональный пробел, не полиш.
- **[[feature-gating-entitlements]]** — 4 открытых решения (ops БД vs конфиг, матрица operative↔veteran, тон апселла, связь кармы).
- **[[cta-content-management-system|CMS E10.5 «Истории → БД»]]** — ОК на DDL + дизайн конструктора дерева; сырьё (деревья 10 историй) уже извлечено в research.
- **Новое из голосовых** — переключатель ролей (в feature-gating), «путь новобранца», метрика популярности сборок.

## Пост-релиз / бэклог (не трогать до запуска)
Сезонный эпик [[seasons-battlepass-epic]] (ядро отгружено; остаток — ручная набивка меток) · компаньон-кластер (3 research-заметки ждут потока данных) · UGC-маркеры [[CTA-MAPS-MARKER-UGC-SPEC]] (до домена `ugc.cta.*`) · NIGHTFALL-редизайн (предусловие style-debt) · аркада (в проде, только тюнинг) · cta.quest лендинг · [[multigaming-second-game]].

---

## Что разблокирует V4DYA vs что могу автономно

| Только V4DYA | Разблокирует |
|---|---|
| SPT-архив + 2 юр/мобилка-проверки | сам запуск |
| 2 дизайн-ответа по шапке карточки + мобильный макет | доверстку карточки |
| Колл A/B/C по рекурсивной цене | движок выгодной цены |
| Визуальная сверка 2К/4К + сетки-спрайты | мёрж [[responsive-2k-4k-scaling]] |
| Скрины 9 историй + развилка «свои vs генерация» | галереи [[story-content-assets]] |
| ОК на DDL + дизайн дерева | CMS-фаза 5 |
| 4 решения по гейтингу | платное ядро |

**Автономно (Claude, без входа V4DYA):** починка дампа квестов · баги трекера/графа (кнопка «Выполнено», поиск→граф, дубли, сброс) · ревизия данных квестов по Wiki (после дампа) · фундамент feature-gating F1 · ~11 сеток [[container-grids-todo]].

## Журнал гигиены борда (16.08)
Доархивировано в `decisions/done/` (git mv, статусы синхронизированы):
`where-to-find-item` · `marked-room-terragroup` · `factory-hd-promote` · `marker-by-screenshot` (был «спека», код в проде `456675ab`) · `factory-map-detail-test` (был «локально», промоутнут через factory-hd-promote) · `HANDOFF-step-marker-wizard` + `UI - Step Marker Creation control` (Ф1–Ф5 на проде, Ф6 опц.).
Оставлено намеренно: `eft-data-autonomy-research` (структурная миграция ✅, но направление B — живые цены — открыто, статус уже честный); `HANDOFF-close-all-404-icons-sweep` (хвост: 3 гладиатор-скина).

> Ручной архив-callout в [[_index]] не трогал (курируется тобой) — Dataview-борд уже отражает статусы из фронтматтера.

---

## Ночная автономная сборка (16.08, ветка `feat/quests-cluster-fixes`)

Грил-план исполнен по кластеру «граф квестов». Диагностика показала: **3 из 6 багов уже починены** до этой сессии, слайс сжался.

**Сделано (закоммичено, `tsc` зелёный, НЕ в main — ждёт ревью V4DYA):**
- ✅ **#2 Поиск → карточка квеста** (`689546d4`): `questResultHref` → `/eft/quests/task/[id]` вместо графа.
- ✅ **#3 Дубли трекера find+give** (`9351f7a3`): цели «найти+сдать» одного предмета схлопнуты в один ряд, нужно=max, оба objectiveId двигаются вместе; убрал и следствие #4 (±/сброс).
- ✅ **#8 «Открывает задания»** на карточке (`09e86e76`): инверсия taskRequirements, зеркало блока «Открывает бартеры»; серверный расчёт на `/eft/quests/task/[id]`. Исполнено после «продолжай» V4DYA.

**Уже было починено (диагностика, вычеркнуто):** #1 «Выполнено» (`24cd1dcc`), #5 автозакрытие через трекер (`bc37823d`), #6 z-index «Завоз».

**Отложено на ревью V4DYA (§6 дизайн / внешние данные):**
- **#7 обрезка длинных имён** в hero-дровере — правит твою типографику (пример «Эпицентр» не воспроизводится).
- **#9 точный УЛ-гейтинг** — нужен `/game-data-ingest` из файлов игры (лишь 5/520 квестов размечены явно), вне клиентского слайса.

**Верификация (статикой, без дев-сервера — риск фриза ПК на общий `.next`, память `gotcha-tailwind-content-scan`):** `tsc --noEmit` зелёный; `eslint` по изменённым файлам — мои правки чисты, 0 новых замечаний. Всплыли 2 **pre-existing** eslint-ошибки `react-hooks/set-state-in-effect` в чужих эффектах (`ItemTrackerClient` setOrder-снимок, `QuestDetail` setHeroFailed-ресет) — не мои, по §4.10 не трогал. Живьём (поиск/трекер/карточка) — за тобой на превью.

Merge в main — по твоей строке после dev-превью.

---

## Стиль-долг NIGHTFALL (авто)

Счётчики пересчитывает `scripts/sync-style-debt.mjs` (pre-commit хук) из `src/`. Руками не править — перезапишутся.

<!-- debt:auto -->
**NIGHTFALL** (авто): any — 11 шт / 1 файлов · font-mono — 1 файлов · сырой HEX в произвольных значениях — 1 шт / 1 файлов · произвольный rounded в px — 3 шт · v3-утечки (gradient-направление, var() в bg) — ⚠️ 3 (в 3 файлах).
<!-- /debt:auto -->
