---
status: ✅ сделано
affects: routing
date: 2026-06-26
done: 2026-06-28
---

# Снести мёртвые route-папки

**Статус:** ✅ сделано (2026-06-28) — Вариант A, через `/execute-decision`
**Затрагивает:** [[snapshot-2026-06]]

## Контекст
Пустые папки без `page.tsx`: `eft/ammo`, `eft/crafts`, 11× `eft/maps/{customs…woods}`. Каталожные роуты резолвятся через `[slug]`/`[...category]`, так что папки — «призраки». Плюс `src/graphify-out/` (артефакт скилла) лежит в `src/`.

## Варианты
- **A —** Удалить все пустые папки + `graphify-out`, добавить артефакты в `.gitignore`.
- **B —** Оставить ammo/crafts как точки роста (там планируются реальные страницы), снести только maps-призраки и `graphify-out`.

## За / Против
| Вариант | За | Против |
|---|---|---|
| **A** | Чистое дерево, ноль мусора в роутинге | Если ammo/crafts в планах — пересоздавать |
| **B** | Сохраняет намеченные точки роста | Пустые папки продолжают мозолить глаз |

## Вывод
**Вариант A** — планов на отдельные ammo/crafts страницы нет (сверка `MVPMANIFEST` + `roadmap` пуста), точки роста держать незачем.

## Исполнено (2026-06-28)
Через `/execute-decision`, источник правды — Obsidian. Сверка с `src/` дала факты сверх заметки:
- Удалены пустые: `eft/ammo`, `eft/crafts`, 11× `eft/maps/{customs…woods}` (были без `page.tsx`; роутинг покрыт `maps/[slug]`). Они были untracked — git их и не видел.
- Удалён старый артефакт `src/graphify-out` (трекался, Jun-17).
- `.gitignore += graphify-out/`.
- ⚠️ **Вскрыто:** корневой `graphify-out` тоже трекается (23 файла) и перестраивается graphify-хуком каждый коммит → шум в истории. Откреп вынесен в отдельный вопрос (за рамками этого решения).
