---
status: ✅ сделано
affects: header, stream
date: 2026-07-01
done: 2026-07-01
---
# Положение кнопки статуса стрима

## Цель
Сделать так чтобы кнопка статуса стрима не была встроена в хедер но при этом не мешала остальным полноэкранным элементам. Продумать также как это будет выглядеть на мобилке.
В общем предложить варианты когда эта кнопка статус не будет мешать ни хедеру ни пользователю во время работы с модулем карты квестов или карты локаций.

## Границы
- **В scope:** `StreamStatus.tsx` + `Header.tsx` (пара строк — вынуть кнопку из потока ROW 1). Подход A: монтирование остаётся в дереве Header, но компонент позиционируется `fixed` → физически «не встроен», нового дома в layout/shell не заводим.
- **НЕ трогаем:** все остальное что не касается Header

## Контекст (чтобы Claude не гадал и не читал лишнее)
- Файлы/папки: `src\components\layout\header-modules\StreamStatus.tsx`, `src\components\layout\Header.tsx` (строка 47 — `<StreamStatus />` в блоке `ml-auto flex gap-3` ROW 1)
- Заметки/ссылки: [[...]]
- Известные гочи:
  - Кнопка-индикатор **уже прячется в фуллскрине** карт: `isQuestFullscreen ? 'invisible pointer-events-none'` (слушает `data-quest-fullscreen` / `data-app-fullscreen` на `<body>` через `MutationObserver`). Логику переиспользуем, не переписываем.
  - **Правый-нижний угол занят:** окно стрима 320×180 + кнопка «развернуть», оба `fixed bottom-4 right-4 z-70`. Кнопку-индикатор ставим **стеком над плеером** в том же углу (её низ не должен налезать на шапку окна `h-8`).
  - `w-40 h-10` (160px) — десктопный размер; на мобилке схлопывается в точку-индикатор без текста.

## Критерий готовности (детерминированный)
- [ ] `tsc` чисто / build ок
- [ ] проверка визуального соответствия с решением
- [ ] чтобы кнопка Стрим Оффлайн не мешала другим элементам

## Развилки (реши заранее, если можешь)
- **Якорь кнопки:** правый-низ, вертикальным стеком **над** окном стрима (всё «стримовое» в одном углу).
- **В фуллскрине карт (квесты/локации):** прятать **полностью** — сохраняем текущее поведение (`invisible pointer-events-none`).
- **Мобилка:** схлопнуть в **точку-индикатор** без текста «СТРИМ ОФФЛАЙН/ОНЛАЙН» (десктоп — полный бейдж `w-40`).

## Гарды
- Бюджет/масштаб: пару файлов связанных с хедером
- Ручное подтверждение нужно для: не нужно

---

## Исполнено (2026-07-01) ✅
Проведено через цепь docs→code (скилл `execute-decision`). `tsc --noEmit` ✅ чисто. Принято V4DYA, заметка → `done/`.

**`StreamStatus.tsx`:**
- Весь вывод обёрнут в единый плавающий стек: `fixed bottom-4 right-4 z-70 flex flex-col items-end gap-2 pointer-events-none`. Обёртка кликопрозрачна, интерактив — на детях (`pointer-events-auto`), поэтому пустой оверлей не перехватывает клики по карте.
- Кнопка-индикатор больше не в потоке хедера — сидит сверху стека, окно стрима под ней (переменная высота окна решается flex-ом, не магическими отступами).
- Мобилка: `w-10 sm:w-40` + текст/гексагоны/скелетон-полоса под `hidden sm:…` → на узком экране остаётся только точка-индикатор.
- Окно стрима и кнопка «развернуть» сняты с личного `fixed …` (позиционирует контейнер), получили `pointer-events-auto`.

**`Header.tsx`:**
- `<StreamStatus />` убран из бара ROW 1 (`ml-auto flex gap-3` — остались `PlayerTelemetry` + `BurgerMenu`).
- Смонтирован оверлеем после `</header>` под `{showFeatures && …}` → на главной по-прежнему скрыт (поведение сохранено).

**Сверено с реальностью:** фуллскрин-логика (`isQuestFullscreen → invisible pointer-events-none`, слушает `data-quest-fullscreen`/`data-app-fullscreen`) уже была — переиспользована, не переписана. Правый-низ был занят окном стрима → отсюда единый стек.

**Приёмка (V4DYA, `npm run dev`) — принято ✅:**
- [x] десктоп: кнопка плавает в правом-нижнем углу, не липнет к хедеру
- [x] мобилка: схлопывается в точку, не перекрывает контент
- [x] фуллскрин карты квестов/локаций: кнопка + окно скрыты
- [x] открыть стрим → окно под кнопкой; свернуть (1px, зритель жив) / развернуть

---

