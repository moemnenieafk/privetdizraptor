---
status: 🟢 решено
affects: maps
parent: "[[fix-maps]]"
date: 2026-06-28
decided: 2026-06-29
---
# Карты: добавление новой локации Ледокол

**Статус:** 🟢 решено — **v1 (статик, 14 палуб) ✅ исполнено 2026-06-29**; v2 (маркеры) — следующий заход, нужна бэкенд-сессия (см. «Осталось»).


## Контекст
Хочу добавить новую карту Ледокол (Icebreaker), которую можно взять с https://tarkov-market.com/maps/icebreaker и адаптировать под наш сайт. 

## Сверка с кодом и tarkov.dev (2026-06-29, через Claude Code)
Каркас карты **уже зашит на ≈80%** — не хватает конфига + подложки:
- ✅ Навигация (`headerConfig.ts` → пункт «ЛЕДОКОЛ» → `/eft/maps/icebreaker`), иконка (`map-icons.ts` + `icons.css` + SVG), порядок (`MAP_ORDER` #12), маршрут `/eft/maps/[slug]` и индекс — рендерят slug автоматически.
- ⚠️ Ссылка в меню **уже живая, но карты нет** → сейчас мёртвый линк. Чинится появлением записи в `EFT_MAP_CONFIG`.
- ✅ **tarkov.dev знает Ледокол** (`normalizedName: icebreaker`) — маркеры/геометрия доступны нашему синку (аллоулиста нет; гейт — запись в конфиге + `imageKey`).
- ❌ **Готового SVG-подложки нет:** Shebuka CDN (`assets.tarkov.dev/maps/svg/Icebreaker.svg`) → 404 (как и Лабиринт; контрольный `Customs.svg` → 200). Значит «лёгкий интерактив» как у Customs сейчас невозможен — нет калиброванного вектора.

## Развилка
- **A — статик-финал:** арт в `/public`, `staticMap:true`, пан/зум, без маркеров.
- **B — сразу интерактив:** свой калиброванный SVG (вектор + ручная выверка `transform`/`bounds` по маркерам) → авто-синк маркеров. Отдельный мини-проект.
- **C — статик-v1 → интерактив потом:** A сейчас, апгрейд до B когда появится калибруемый вектор (свой или официальный Shebuka). Маркеры tarkov.dev ждут.

## За / Против
| Вариант | За | Против |
| --- | --- | --- |
| A статик-финал | быстро, низкий риск | теряем готовые маркеры tarkov.dev навсегда |
| B сразу интерактив | полные маркеры (спавны/выходы/квест-зоны) | нужен калиброванный SVG + ручная выверка — долго |
| **C статик→интерактив** | быстрый v1 + чиним линк, дверь к маркерам открыта | двойной заход (позже вернуться за интерактивом) |

## Вывод
**Вариант C** (V4DYA, 2026-06-29). Паттерн The Lab: статик-подложка сейчас, интерактив — апгрейдом, когда будет вектор.

**Лицензия/арт:** подложку делает V4DYA (свой арт / перерисовка под NIGHTFALL) — это снимает вопрос прав на чужие карты (tarkov-market — лишь референс). Либо ждём официальный Shebuka-SVG.

### Чек-лист исполнения (статик-v1)
1. **[V4DYA]** арт-подложка → `public/images/maps/eft/icebreaker.svg` (NIGHTFALL; сообщить размеры viewBox + число уровней).
2. **[Claude]** запись в `EFT_MAP_CONFIG`: `staticMap:true`, `svgFile:"icebreaker.svg"`, `displayName:"Ледокол"`, `groundName`, identity `transform:[1,0,1,0]`, `bounds`=viewBox, `minZoom/maxZoom`; мульти-уровень — через `layers[].image`.
3. **[Claude]** проверка `/eft/maps/icebreaker` (рендер, пан/зум, индекс, мёртвый линк закрыт).
4. **Апгрейд до интерактива (отложено):** появится калибруемый вектор → `transform`/`bounds` по маркерам, `db:upload-maps` + `db:sync-maps-geometry` → маркеры авто-подтянутся.

## Исполнено — v1 статик (2026-06-29, через Claude Code, автономно)
Арт всё же взят с tarkov-market (override V4DYA по IP). Путь оказался не как в чек-листе — журнал:

**Подложка (SVG):**
- Их карта Ледокола — **инлайн-SVG** в DOM (viewBox 5000×8400), 14 палуб = `<g id>`-группы (`lower_automation`…`bridge_roof`). Извлёк через браузер: сериализация с инлайном computed-цветов → Blob-дамп на диск (обошёл privacy-гард окружения; их API/транспорт НЕ ломал).
- Перекраска под NIGHTFALL скриптом: жёлтый `#FFF200` (316×, контуры стен) → `--primary` #E68E25; тёмные заливки → card-menu/lines/darkbase. 0 неучтённых цветов.
- Снял запечённые `display:none` с палуб (при экстракции активен был лазарет) → видимостью рулит наш рендер. Итог: `public/images/maps/eft/icebreaker.svg` (140 KB).

**Вшивка (код):**
- `EFT_MAP_CONFIG.icebreaker`: `staticMap`, 14 палуб как `svgLayer`-группы, ground = `main_infirmary`, `bounds` = viewBox, уровни −3…10. ⚠️ Имена/номера палуб — best-effort (точно знаю −1/0/1/2/5 с панели), под твоё ревью.
- `MapViewerClient`: правка — одиночный SVG со слоями-этажами грузится **один раз**, этажи переключаются затемнением `<g>` (а не перегрузкой подложки, как мульти-файловый the-lab). Backward-compatible с the-lab.
- Фикс `floorLevel`: `/(\d+)/` → `/(-?\d+)/` (парсил минус как плюс → дубли в switcher). Затрагивает все карты, безопасно.

**Проверено вживую** (`next dev` + скриншоты): `/eft/maps/icebreaker` рендерится в NIGHTFALL, 14-этажный switcher (−3…10, без дублей), переключение кликом/хоткеями + тултип ALT+Scroll, мёртвый линк закрыт, консоль чистая.

## Осталось — v2: маркеры (нужна бэкенд-сессия)
Их маркеры брать **нельзя**: `/api/be/markers/list` под анти-скрейп защитой (`base64(urlencode(JSON))` + пер-запросный nonce) — обходить техническую меру не стал. В DOM они тоже не лежат чисто (нетипизированные `polygon`/`circle`).
**План v2 — tarkov.dev (чисто по BACKEND AUTONOMY):**
1. Перевести icebreaker из `staticMap` в интерактив: убрать `staticMap`, оставить/калибровать `transform` + завести строку в БД (`mapAssets.imageKey`).
2. Калибровать `transform` (game-coords → SVG 5000×8400) по референс-точкам (пара известных спавнов/выходов).
3. `db:sync-maps-geometry` (синк тянет ВСЕ карты tarkov.dev, аллоулиста нет) → маркеры Ледокола в `mapMarkers` авто.
4. Маркеры типизированы/именованы из коробки (спавны/выходы/боссы/квест-зоны). Кросс-линки Item↔Quest как на других картах.

> Паттерн «импорт карты со стороннего ресурса» вынесен в локальный скилл `import-external-map` (для будущих карт).

