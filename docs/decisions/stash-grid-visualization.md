---
type: epic-plan
status: ✅ реализовано (autopilot 2026-08-22, ветка feat/player-stash-grid) — ждёт live-verify+пуш V4DYA
sprint: feat/player-stash-grid
owner: V4DYA
related: docs/decisions/important-items-merge.md · docs/research/eft-editions-hideout-bonuses.md
---

# Эпик: Стойка «Схрон игрока» — визуальная сетка инвентаря

Идея V4DYA (2026-08-21, во время грилла craft-profit). **Не входит** в текущий автопилот craft-profit — самостоятельная фича, отдельный спринт. Здесь фиксируем замысел + точки переиспользования, чтобы не потерять нюансы.

## ✅ Решения грилла (2026-08-22) — КАНОН для реализации
Модель выбрана V4DYA, остальное — по рекомендации Claude, подтверждено «выбери всё по рекомендации, запускай срочно».

1. **Метафора = B «Витрина владения».** Одна ячейка = один уникальный предмет из `ownedItems`, размер по футпринту `gridW×gridH`, бейдж ×N. НЕ настоящий bin-packing юнитов (стек-данных `stackMaxSize` в зеркале нет, перф спокойнее, переиспользуем TrackCell).
2. **Ширина = 10 колонок** (канон схрона EFT). 56px × 10 = 560px.
3. **Стеки:** одна ячейка на уникальный предмет + бейдж количества. Реальные стек-лимиты не моделируем.
4. **Packing:** shelf/skyline-упаковка футпринтов слева-направо, сверху-вниз; порядок — крупные вперёд (по площади убыв., тай-брейк редкость) для плотной раскладки; **без поворота** (футпринт как есть).
5. **Grid-иконки:** MVP — существующий 512px-арт `itemIconUrl()`, `object-contain` внутри футпринта. `gridImageLink` НЕ зеркалим сейчас → долг-follow-up (game-data-ingest/cta-backend).
6. **Расширение:** растём строго под контент (packed-высота). Пустых рядов до размера издания НЕ рисуем; вместо этого индикатор-стат «Занято X ячеек / ёмкость издания Y» из `editionFloor(stash, edition)` игрока (10×30…10×72).
7. **Взаимодействие с ячейкой:** канон TrackCell — ЛКМ +1, ПКМ −1 (`bumpCount`), клик по бейджу X = прямой ввод; угловой линк ведёт на страницу предмета `/eft/items/...`. НЕ read-only.
8. **Перф:** `useMemo` на packing от `ownedItems`. Виртуализацию не вводим (уникальных предметов немного); порог виртуализации заложить константой-флагом на будущее.
9. **Мобилка (cta-mobile):** контейнер `overflow-x-auto`, сетка фикс 56px×10 = 560px → горизонтальный скролл на <640px. Ячейки не ужимаем.
10. **Маршрут:** `/eft/progress/stash`. Вход: кнопка из «Важных предметов» (`/eft/progress/needed`) и craft-profit + узел в HubNav (hubnav-canon: узел в headerConfig, навигацию не верстать вручную).
11. **Данные на клиент:** `ownedItems` — localStorage (client). Мета предметов (`shortName`, `gridWidth`, `gridHeight`, `backgroundColor`) тянем из зеркала Supabase. Способ: API-роут `GET /api/eft/items/meta?ids=...` (читает mirror, автономно §4.11) → малый payload по id владения. `gridWidth/gridHeight` ЕСТЬ в `items`, `backgroundColor` — в `prices`; сейчас до клиента НЕ доезжают → добавить в селект ридера.

## Замысел
Отдельный раздел-визуализация **схрона игрока**: тарков-подобная сетка ячеек (базово **56×56px**), куда попадает **всё, что пользователь накликал кнопкой «В схрон»** по сайту (Важные предметы, craft-profit-входы и т.д.). Держит юзера в курсе: «что и сколько у меня уже лежит».

Ключевые свойства:
- **Реальный размер предмета по ячейкам** — предмет занимает `gridWidth × gridHeight` (1×1, 1×2, 2×2…), а не одну клетку.
- **Расширяется от наполнения** — сетка растёт по мере добавления лута (авто-упаковка).
- Ячейка красится **рарити-фоном** предмета (как TrackCell), иконка предмета сверху, бейдж количества.

## Что переиспользуем (нюансы уже разработанного)
- **`useInventoryStore`** («в схроне» из Важных предметов): единый источник «сколько предмета у игрока». `bumpCount(id, delta, max)` — функциональный кламп (защита от гонки hold-repeat, см. needed-merge-session). Кнопки «В схрон» уже пишут сюда.
- **`items.gridWidth` / `items.gridHeight`** (зеркало) — footprint предмета по ячейкам. ЕСТЬ в БД.
- **`items.backgroundColor`** → `getTarkovBackgroundColor()` — рарити-фон ячейки (как на /needed и /modules).
- **`itemIconUrl(id)`** — иконка. ⚠️ Проверить: для сетки нужна **grid-иконка** (tarkov.dev `gridImageLink`, обрезана под клетки), а не 512px-арт. Уточнить, зеркалим ли `gridImageLink`; если нет — завести поле/синк (game-data-ingest / cta-backend).
- **`TrackCell`** — канон-ячейка (рарити-фон + бейдж + «в схроне»). Возможно, база для StashCell (или отдельный компонент с packed-layout).
- **Размер схрона по изданию** — `docs/research/eft-editions-hideout-bonuses.md` + `editionFloor`: Standard 10×30 … Unheard 10×72 (ячейки). Стандартная ширина схрона EFT — **10 колонок**, издания добавляют ряды. ⚠️ V4DYA упомянул «14×14» — сверить желаемую ширину сетки (10 канон EFT vs 14 произвольная витрина).

## Нюансы для проработки (открытые вопросы эпика)
1. **Алгоритм упаковки** (bin-packing) предметов разного размера в сетку фикс. ширины; правила поворота (2×1 ↔ 1×2), порядок (по размеру? по редкости? по времени добавления?).
2. **Стеки**: несколько одинаковых предметов — отдельные ячейки-стеки (как в игре, с лимитом стека) или один предмет × N? EFT-стек-лимиты в данных? (проверить `items.stackMaxSize` — есть ли поле).
3. **Ширина сетки**: 10 (канон схрона по изданию) или 14 (витрина). Влияет на реализм «реального занимаемого места».
4. **Расширение**: показывать пустые ряды до размера схрона издания, или растить строго под контент?
5. **Взаимодействие**: клик по ячейке = −1 «в схроне»? ссылка на предмет? read-only?
6. **Перф**: сотни предметов → виртуализация/мемо (гоча из needed-merge — тяжёлый пересчёт state).
7. **Мобилка** (cta-mobile): 56px×10кол = 560px — на телефоне скролл/зум сетки.

## Разбивка (набросок, до реального спека)
1. Ридер размеров: убедиться, что `gridWidth/Height/backgroundColor/gridImageLink` доезжают до клиента (join к `useInventoryStore` id).
2. `StashCell` (или расширить TrackCell) — предмет во «footprint» ячеек.
3. Packer — раскладка по сетке ширины W.
4. Раздел/страница + вход из Важных предметов и craft-profit.
5. Live-verify + мобилка.

**Следующий шаг:** после закрытия craft-profit — отдельный грилл по этому эпику (упаковка, ширина, стеки), затем спека и реализация. Данные (`gridImageLink`, `stackMaxSize`) выверить на старте.

---
## ✅ Итог реализации (autopilot 2026-08-22, ветка feat/player-stash-grid)
Грилл закрыл все 7 открытых вопросов (см. блок «Решения грилла» вверху). Реализовано 5 таск-волнами, 7 коммитов, tsc+eslint зелёные по всему проекту, независимое ревью = SHIP.

**Файлы:** `src/lib/stash-types.ts` · `src/app/api/eft/items/meta/route.ts` (мета из зеркала, §4.11) · `src/lib/stash-packer.ts` (skyline-упаковка) · `src/components/features/stash/StashCell.tsx` (переиспускает TrackCell) · `src/lib/stash-capacity.ts` (ёмкость по изданию) · `src/app/eft/progress/stash/{page,StashClient}.tsx` · узел `p-stash` в `headerConfig.ts` + входы из needed/craft-profit.

**Долг/сверка V4DYA:** иконка узла HubNav — заглушка `items-needed.svg`; прямой ввод количества по бейджу не подключён (nice-to-have); `gridImageLink` не зеркалим (иконка = 512px арт) — долг game-data-ingest; виртуализация — задел-флаг `VIRTUALIZE_THRESHOLD=400`, не реализована.

**⚠️ Не догнано локально:** прод-`next build` крашит Turbopack (IPC os error 10054) из-за конкуренции за `.next` с работающим dev-сервером. Не код фичи. **Live-verify** — открыть `/eft/progress/stash` на dev-сервере, добавить пару предметов «В схрон» → проверить сетку/футпринты/бейджи/линки/ёмкость/мобилку. Затем пуш.
