# ЦТА ARCADE — статус реализации

Реализация по спеке [cta-arcade-handoff-final.md](cta-arcade-handoff-final.md) и исходному
дизайн-доку `SaveTheServers.md` (YandexDisk). Дата: 2026-07-25.

## Что сделано (Фазы A–F)

Раздел **«Зал автоматов»** под «Кто ты в Игре» → **`/eft/progress/rookie/arcade`**.
Игра идёт «внутри» фотореалистичного аркадного автомата; движок подключаемый (контракт `ArcadeGame`).

- **Каркас.** `src/lib/arcade-screen.ts` (константы геометрии verbatim из §3/§8),
  `src/components/ui/ArcadeFrame.tsx` (плита в потоке + бокс экрана 4/3 + слой стекла;
  без дырки/маски/transform). Site + клоузап-фуллскрин (`cabinet-front.webp` /
  `arcademachine_cleanplate_closeup_bg.webp`).
- **Хост/движок.** `src/components/features/arcade/`: `ArcadeHost` (оркестрация, site↔fullscreen
  единым инстансом канваса, мобильный флоу), `ArcadeCanvas` (offscreen 2D 640×480 → WebGL CRT-пасс
  `crt.ts` с 2D-фолбэком; rAF+dt; покадровое состояние в ref; пауза visibility+IntersectionObserver;
  Pointer Events одной веткой), `registry.ts`, `types.ts`, `GameSelector`, `SkinShop`, `RotateScreen`.
- **game01 «Спаси сервера».** `games/save-the-servers/`: `config.ts` (ВСЕ константы баланса),
  `state.ts` (физика/спавн/HP/таймлайн/Пельмень/баффы-дебаффы; взмах-свип по траектории курсора +
  заряд ЛКМ; вращение/«поп»), `render.ts` (фон/бутылки/Пельмень/пикапы/HUD в канвасе/эффекты/курсор),
  `sprites.ts`, `index.ts` (сборка + экономика + SFX).
- **Экономика.** `store/useSaveTheServersStore.ts` (кошелёк + 16 скинов, цена index×20, localStorage).
  `store/useArcadeStore.ts` (выбор игры + мьют). Хитбокс чуть растёт с тиром скина.
- **Звук.** Синтез-тоны в `src/lib/sfx.ts` (bounce/thud/powerup/alarm/burst), тумблер у автомата,
  по умолчанию выкл.
- **Мобилка.** В ленте на тач канвас НЕ запускается (постер «Играть») → фуллскрин → портрет = экран
  поворота (`RotateScreen`, есть кнопка «Назад») → ландшафт = игра. `matchMedia('(orientation)')`,
  `wakeLock` (try/catch), safe-area insets, `touch-action:none` только в игре+фуллскрине,
  `orientation.lock` как прогрессив-энхансмент.
- **Навигация.** Ссылка «Зал автоматов» в `RookieHubClient`, breadcrumb `arcade` в `headerConfig.ts`.

## Управление (важно для тестеров)

- **Взмах** = удар считается по траектории движения курсора за кадр (свип-отрезок), один мах бьёт
  несколько бутылок (кулдаун 120 мс на объект). Обычный клик тоже работает.
- **Заряд** = зажатие ЛКМ копит силу (полный за 450 мс, кольцо на курсоре золотеет) → сильнее удар →
  бутылка выше.

## Тюнинг

Весь баланс — [config.ts](../../src/components/features/arcade/games/save-the-servers/config.ts).
Вайб ЭЛТ — пресеты `PRESETS` в [crt.ts](../../src/components/features/arcade/crt.ts).
Спека прямо просит перебалансировать баффы/дебаффы/спавн Пельменя ПОСЛЕ первых тестов.

## Проверка

`npx tsc --noEmit` — чисто. Playwright-скриншоты: десктоп-лента, клоузап-фуллскрин (экран точно в
стекле), мобильный портрет (экран поворота), ландшафт (игра). Консоль без ошибок.

## game01 v1.4 (правки багов по спеке `v1.4 SaveTheServers …`)

Приведено к спеке v1.4 (управление — по решению V4DYA: **простой клик**, «жизнь» оставить):
- **Управление:** откат к простому клику (импульс на нажатии, `CLICK_VY=-12`, `Vx±4`, фикс-хитбокс
  `HIT_RADIUS=44`). Убраны заряд ЛКМ, свайп-взмах, хитбокс-от-тира. Вращение + squash-поп (фидбэк) — оставлены.
- **Размер:** полный отказ от масштабирования — бутылки фикс-размера с 00:00 (убраны shrink-ramp и ×1.5 звезды).
- **HP:** оверхил без потолка (`hp += 1`, спавн хилки всегда); HUD рисует до 6 пипсов + `×N`.
- **Таймлайн (§3):** 30с вступление, далее +1 бутылка каждую минуту (`RAMP_END=30с`, `STAGE_STEP=60с`).
- **Пельмень (§4):** порог 5; убирает НЕ-золотую (золотую бережёт); откат сложности на предыдущую
  стадию +1с (`stageStartMs − STAGE_STEP + 1000`, сверено с примером 03:48→02:31).
- **Звезда (§5):** `zvezda.webp`, 15с, замедляет падение (`GRAVITY×0.5`), без масштаба.
- **Флэш (§5.1):** трек `flashgrenade.mp3` + посекундный визуал (взрыв бел. 0.85 на 3–4с, затухание к 6с),
  мягче под reduced-motion.
- **Ошибки (§5.2):** картинки `errors/webp/error1..7`, opacity 0.8, 3с; приоритет клика по бутылке над OK.
- **Золотая (§5.2.2):** отдельный спрайт `goldpevko.webp` (15% / +2).

Проверка: node-симуляция (13/13 PASS — клик/оверхил/пельмень/звезда/таймлайн/откат) + визуал в автомате;
tsc/eslint чисто.

## game02 «Три в ряд» (Tarkov Match-3)

Реализован как второй подключаемый `ArcadeGame` — `games/tarkov-match3/` (config/layout/state/render/index),
экономика `store/useMatch3Store.ts` (кошелёк ₽ + энергия 5/30мин), общий `arcade/sprites.ts`.
Поле рендерится в том же канвасе под CRT; ввод — свайп/тап-выбор + панель инструментов снизу.
MVP по спеке: матчи 3/4/5/L-T/2×2 → спецфишки (rshg/zaryad/rgo/kerman) + комбо, инструменты
(tool/pestik/vodka, не тратят ход), гравитация с барьерами `wood`, дедлок-перемешивание,
ходы/цель, окно «нет ходов» (купить +5 за ₽ / сдаться −1⚡), уровни 1 (50 ₽) и 2 (18 ящиков wood).
Валидация: node-симуляция (обе цели достигаются, сетка целостна, крашей нет) + визуал в автомате.
Отложено (заложено в архитектуру): звёзды/убежище, пре-левел бустеры, восстановление энергии едой,
тултипы инструментов, плавные твины падения (сейчас логика мгновенная + вспышки).

## game03 «Прибыль бартера» (аркадный порт BarterRush)

Порт мини-игры `/eft/progress/barter` (компонент `BarterRush`) в зал автоматов — `games/barter-rush/`.
Механика: оцени сделку на глаз (свайп ←/→ или кнопки Мимо/Выгода) + **аркадные ставки**: таймер на
решение, 3 жизни, комбо-мультипликатор, game over → счёт. Рекорды — общий `useBarterRushStore`
(рекорд общий с модулем бартера). Расчёт выгоды переиспользован: `calcBarterProfitStandalone`/
`isPlayableBarter` (считаются на СЕРВЕРЕ).

Данные: аркадная страница (RSC) фетчит колоду серверным ридером `src/lib/eft-barter-rush.ts`
(`getRushDeck()` — из нашей Supabase, вердикт/профит считаются на сервере, клиенту летит лёгкий
`RushCard[]`) и прокидывает в игру через расширенный контракт фабрики (`ArcadeGameData.barterDeck`
→ `ArcadeCanvas gameData` → `createGame(data)`).

Иконки предметов на Cloudflare R2 **без CORS** → рисовать их прямо в CRT-канвас нельзя (taint
роняет WebGL). Решение: гоним иконки через `/_next/image` (same-origin proxy), добавлен wildcard
`**.r2.dev` в `next.config.ts remotePatterns`. Портреты торговцев — локальные `/images/traders/eft/`.

Валидация: визуал в автомате (десктоп) — реальный бартер Егеря, ROI/профит, вскрытие, счёт/комбо.
tsc/eslint чисто, консоль без ошибок.

## game04 «Егерь ловит покушать» (JaegerCatchFood)

Ретро-LCD «Ну, погоди!» по GDD `v0.1 JaegerCatchFood …`: Егерь с корзиной в 4 позициях ловит
скатывающийся по 4 скатам лут, не поймав гранату. Модуль `games/jaeger-catch-food/`
(config/state/render/index), рекорд `store/useJaegerStore.ts`, ассеты `public/images/arcade/game03/`.

- **Управление:** клавиши Q/Й·S/Ы (лево), P/З·L/Д (право) + тап по 4 квадрантам (мобилка); подсказка на idle.
- **Механика:** лут пойман в правильной позиции → +1 (btc +5, редкий 1/25–75); граната поймана → промах,
  пропущена → ок; уронил лут → промах. Скорость/плотность растут со счётом (GDD §4: 1→4 предмета,
  rollMs 5000→1450). 3 промаха → game over; сброс промахов на кратных 50 (−1) и 100 (все).
- **Решения (сверх GDD):** пропуск лута = промах (классика); тач-квадранты для мобилки.

Реестр: 4-я карточка `jaeger-catch-food` (селектор адаптивен, `lg:grid-cols-4`).
Проверка: node-симуляция 11/11 PASS (лови/урони/граната/btc/сброс/over) + визуал (Егерь+скаты+предметы+HUD),
tsc/eslint чисто.

## Отложено / открытые вопросы
- **Баланс** game01 — по итогам живых тестов (V4DYA + Дима).
- **Имена скинов** в `config.ts` — рабочие; при желании выверить под канон EFT.
- **Пропорция клоузапа** 1.38148 vs эталон 1.38127 — расхождение 0.0002 (< 0.001, в допуске).
- `public/images/arcade/game01/converted/` — дубликат клоузапа от экспорта, в git НЕ коммитился.
