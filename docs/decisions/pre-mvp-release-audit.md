---
status: 🟡 Проход 1 закрыт · ревизия 14.07: продукт готов, открыты 6 релиз-блокеров (Проход 2)
affects: release, quality, security, mobile, seo, legal
date: 2026-07-04
---
# Масштабная оценка проекта перед релизом MVP

Эпик E11 из [[additional-workout]] (добавлен V4DYA: «хочу запуститься как можно скорей»).
Кросс-сквозной **релиз-гейт**: инвентаризация всего проекта + определение **среза MVP**
(что shippable сейчас, что уезжает в пост-релиз).

## Цель
Оценить готовность всего проекта к запуску MVP и составить чек-лист «до релиза / после релиза»,
чтобы запуститься максимально быстро без стыдных дыр.

## Подход (2 прохода)
### Проход 1 — Инвентаризация `M` 🤖 (сделать ПЕРВЫМ, дёшево)
Фоновые агенты собирают карту состояния:
- Готовые разделы vs заглушки/404 vs «в разработке».
- Битые ссылки, пустые страницы, мёртвые роуты (краул по sitemap/навигации).
- TODO/FIXME/`disabled`-кнопки по коду.
- **Выход:** таблица «фича → статус → нужно ли к MVP».

### Проход 2 — Гейт-чек-лист `L` (перед самим запуском)
Технический смок по осям:
- **Build/типы:** `tsc` чисто, `next build` ок, нет рантайм-ошибок консоли.
- **Безопасность:** прогнать скилл `/security-review`; RLS на всех таблицах (особенно новые: `feedback`, будущие `subscriptions`); секреты не в клиенте; rate-limit на публичных эндпоинтах.
- **Мобилка:** смок ключевых страниц на телефоне (см. `cta-mobile`); нет горизонтального скролла/сломанных сеток.
- **Юридическое:** страницы `/terms` и `/privacy` — сейчас на них ссылается форма фидбэка ([[feedback-form]]), но самих страниц может НЕ быть → создать; куки/согласия если нужно.
- **SEO/мета:** title/description/OG на ключевых страницах, sitemap, robots, favicon.
- **Ошибки/пустые состояния:** 404/500-страницы, skeleton'ы (не спиннеры), обработка оффлайна API.
- **Данные:** кроны синка живы (цены/бартеры/убежище/карты), нет пустых каталогов.
- **Перф:** вес бандла, отсутствие блокирующих SSR-фетчей, ленивая загрузка тяжёлых либ (Leaflet и т.п.).

## Границы
- **В scope:** весь проект — оценка и чек-лист; точечные фиксы «шоу-стопперов».
- **НЕ трогаем:** большие рефакторы (mobile-first аудит, CMS) — они пост-MVP, если не блокеры.

## Влияние на роадмап
Срез MVP из Прохода 1 может **переставить приоритеты**: часть Монетизации/Карт/Mobile может
уехать в пост-релиз, если не критична для первого запуска. Монетизация (Фаза 1) — вероятно
пост-MVP (можно запуститься бесплатным ядром и включить paywall позже).

## Критерий готовности
- [x] Проход 1: таблица состояния всех разделов + список шоу-стопперов (2026-07-05, см. ниже)
- [ ] Проход 2: чек-лист пройден, `/terms` + `/privacy` существуют, `/security-review` без critical
- [ ] `tsc` + `next build` чисто

---

## 📋 Проход 1 — результат (2026-07-05, инлайн-скан кода)

Скан по осям: 50 роутов (`page.tsx`), мёртвые ссылки, `disabled`-кнопки, TODO/FIXME, маркеры «заглушка/в разработке», юр-страницы.

### A. Готово к MVP (реальные данные / рабочий функционал)
| Раздел | Роуты | Комментарий |
|---|---|---|
| Главная / хаб | `/`, `/eft` | лендинг мультигейма + хаб EFT |
| Предметы | `/eft/items` (+ `[...category]`, `item/[slug]`, `loot-rate`, `price-slot`) | каталог, цены из зеркала Supabase |
| Бартеры | `/eft/barters` | |
| Квесты | `/eft/quests` (+ `[slug]` сюжетки, `[slug]/map`, `task/[id]`, `lore-quests`, `side-quests`), `/eft/questmap` | |
| Трекинг | `/eft/progress/*`: quests, achievements(+`[id]`), barter, needed, prestige, tracker, hideout/modules | |
| Кодекс | `/eft/gamesetting/*`: traders(+`[slug]`), bosses(+`[slug]`), characters, materials | |
| Карты | `/eft/maps` (+ `[slug]` где есть SVG-подложка) | Leaflet |
| Аккаунт | `/account`, `/login`, `/reset-password` | Supabase-авторизация |
| Админка/CMS | `/admin` (+ `items`, `items/[id]`) | своя лёгкая CMS, role=admin |

### B. Умные заглушки (навигируемы, не тупик; ждут данных/фазы) → пост-MVP
- `/eft/videos` (+ `[category]`) — видео-раздел (заглушка).
- `/eft/progress/hideout/[metric]`, `craft-profit`, `bitcoin-profit` — экономика убежища (Phase 2.2/2.3).
- `/eft/progress/loadouts` (+ `[action]`) — сборки оружия (заглушка).
- `/eft/maps/[slug]` без SVG · `/eft/gamesetting/[slug]` · `/eft/quests/[slug]` без данных — умные заглушки-фолбэки.
- `/[game]/[[...rest]]` — мультигейминг: 5 игр «В РАЗРАБОТКЕ» (`games.ts`) — умная заглушка второй игры.

### C. Внутреннее (не для паблик-MVP)
- `/eft/styleguide` — тех-паспорт дизайн-системы → решить: скрыть из паблик-нав/индекса.

### 🔴 Шоу-стопперы (к решению V4DYA / фиксу до релиза)
1. **Юр-текст** — `/legal/{offer,terms,privacy,eula}` резолвятся, но это **черновики** с баннером «не имеет юр. силы» (noindex). Ссылки формы фидбэка **не битые**, но для аккаунтов/платежей нужен реальный текст terms/privacy. → правовой риск, не технический блокер.
2. **Мёртвые ссылки в футере (`href="#"`)** — соц VK / X / Reddit + доп-ссылки «Партнёрская программа», «Интеграции», «Экосистема ЦТА». На паблике выглядит недоделанным → проставить URL или скрыть до готовности.
3. ~~**«Связь» — целый непостроенный раздел**~~ ✅ **РЕШЕНО (2026-07-05):** заведён веткой `comlink` в `headerConfig.menuItems` (6 подпунктов: Поиск напарника · Кандидаты · Биржа шерпов · Мастер-классы · Новостной блог · Обновления игры) + навигируемые смарт-заглушки `/eft/comlink` и `/eft/comlink/[section]` (SectionPlaceholder, noindex). Мёртвый footer-`#` убран (COMLINK теперь из общего конфига). Реал-контент 6 подпунктов — отдельный эпик, пост-MVP.
4. ~~**Мультигейминг-заглушки**~~ ✅ **ПРОВЕРЕНО (2026-07-05):** 404/тупиков НЕТ. Все 8 игр консистентны (`GAMES_DATA` = `HEADER_DICTIONARY`), catch-all `/[game]/[[...rest]]` отдаёт `PlaceholderPage` («В разработке») для игры и всех под-путей; неизвестная игра → 404 (корректно). Косметика (не блокер): у `frago`/`abi` подзаголовки не «В РАЗРАБОТКЕ» — на хабе выглядят «доступными», хотя ведут на заглушку.

> ⚠️ **Оговорка к Проходу 1:** скан инвентаризировал **код** (роуты `page.tsx`). Разделы, спроектированные, но ещё не заведённые в коде (живут только в Figma/голове V4DYA — как «Связь»), скан НЕ видит. Для полноты среза MVP нужен от V4DYA список **запланированных, но не начатых** разделов. «Связь» — первый выявленный.

### 🟢 НЕ шоу-стопперы (ок как честная заглушка)
Видео, экономика убежища, сборки оружия — навигируемы, помечены, уезжают в пост-MVP. `disabled`-кнопки все условные (валидация форм, недоступные игры) — постоянных «мёртвых» нет.

### Влияние на срез MVP (предложение)
Запуск **бесплатным ядром** реален уже сейчас (A + честные заглушки B). До релиза закрыть только шоу-стопперы 1–3 (юр-текст, мёртвые ссылки, «Связь»). Монетизация (Фаза 1), маркеры карт (Фаза 2), mobile-first (Фаза 3) — **пост-MVP**, как и предполагал роадмап.

### Осталось на Проход 2 (перед самим запуском)
`tsc`/`next build` · `/security-review` + RLS (`feedback`, будущие `subscriptions`) · мобилка-смок · SEO (sitemap/robots/OG/favicon) · 404/500 · живость кронов · перф (Leaflet lazy).

---

## Гарды
- ⚠ Ручное подтверждение: фиксы, затрагивающие БД/RLS/пуш в main.
- Правило: сначала оценка (дёшево, 🤖), решение о срезе — за V4DYA, потом фиксы.

---
*Процесс: [[engineering-loop]]*


---

## 🔄 Ревизия 14.07.2026 (сверка с кодом `main`)

Полный срез → **[[release-readiness-2026-07]]**. Изменения относительно Прохода 1:

**Закрыто кодом (было в B «пост-MVP», теперь готово):**
- ✅ **Видео** — модуль построен (категории, архив, Twitch/YouTube-фид).
- ✅ **Сборки оружия** — построены (стат-движок, квоты, каталог, Gunsmith-солвер, шаринг).
- ✅ **Связь (Comlink)** — не заглушка: форум, поиск напарника, рейды, биржа шерпов, карма, отчёты, блог, обновления игры (реальные таблицы + API).
- ✅ **Loot-контейнеры** — новый раздел с таблицами лута из SPT.
- Роутов было 50 → **73**.

**Шоу-стопперы — актуальный статус:**
1. 🔴 **Юр-текст** — по-прежнему черновик («не имеет юр. силы»).
2. 🔴 **Мёртвые ссылки футера** (`#`: VK/X/Reddit + Партнёрка/Интеграции/Экосистема) — открыты.
3. ✅ **«Связь»** — закрыт полностью (раздел построен, не заглушка).
4. ✅ **Мультигейминг-заглушки** — ок.

**Новые блокеры Прохода 2 (найдены сверкой):**
5. 🔴 **SEO-минимума нет:** нет `sitemap.ts`, `robots.ts`, `metadataBase`/OG; корневые мета — стайлгайдовские («Технический паспорт дизайн-системы»).
6. 🔴 **Нет `error.tsx` / `global-error.tsx`** (500-страница). `not-found.tsx` есть.
7. 🔴 **UGC без RLS-аудита:** Comlink принимает пользовательский контент, но в `supabase/` нет RLS-скрипта для его таблиц (DDL — через `db/comlink-ddl.ts` + migrate-роут). Обязательно перед паблик-форумом.
8. 🟡 `/eft/styleguide` — публичен, без `noindex`.
9. 🟡 Таблицы `subscriptions` в схеме нет (`fetchTier` мягко → `free`); кроны — только `sync-prices` в `vercel.json`.

**Чек-лист Прохода 2:**
- [x] `sitemap.ts` + `robots.ts` + корневые `metadata`/OG — **сделано 14.07** (`b9374fe6`, `87950a50`, `9440d03c`). Sitemap из дерева меню + карты/сюжетки/контейнеры/юр-док; robots закрывает `/api /admin /account /login /auth /eft/styleguide`.
- [x] `error.tsx` + `global-error.tsx` (500) — **сделано 14.07** (`9b060d48`). Styleguide уже был `noindex`, теперь и в robots.
- [x] Юр-текст privacy + terms — **сделано 14.07** (`b85201de`). Действующая редакция, 152-ФЗ, правила UGC для «Связи», контакт оператора. Оферта/EULA — остались черновиками (noindex), нужны при монетизации.
- [x] Футер: мёртвые `#` убраны — **сделано 14.07** (`e9623d7f`). Соц: TG/Discord/Boosty; доп-ссылки и оферта/EULA скрыты до готовности.
- [x] RLS на comlink — **аудит пройден:** RLS уже в `comlink-ddl.ts` (enable + read-policy для авторизованных; `karma_events`/`reports` — без policy). Все 8 API-роутов проверяют сессию (`getMe` → 401), запись — owner-роль. Модерация есть (`moderator`, pin/lock/hide, очередь жалоб).
- [x] ✅ **`migrate-comlink` прогнан** (V4DYA, 14.07) — таблицы + RLS в проде.
- [x] ✅ **Модератор назначен:** `fullkamen` → `role='moderator'` (14.07). Для этого заведён роут `GET /api/cron/set-role` (за `CRON_SECRET`) + workflow **set-role** (Actions → Run workflow → логин + роль) — назначение ролей с телефона, без SQL Editor. Роль `admin` роутом НЕ выдаётся (утечка секрета не должна давать CMS). `UserRole` расширен: `user | moderator | admin`.
- [ ] `NEXT_PUBLIC_SITE_URL` в Vercel env (пока фолбэк на системный `VERCEL_PROJECT_PRODUCTION_URL` — работает, но кастомный домен лучше задать явно).
- [x] **`/security-review` — пройден 14.07.** Найдено и закрыто 3 дыры:
  - `/api/feedback` — публичный приём вложений (5×500 КБ base64 в БД) **без rate-limit** → спам/раздувание Postgres. Лимит 5/час на IP до чтения тела (`ad81e8a1`).
  - `/api/eft/builds/defs` (POST, до 600 id) и `/api/eft/prices` (GET, до 200 id) — публичные БД-джойны без лимита → 120 / 5 мин на IP (`0e428e7c`).
  - `/api/account/avatar` — запись в Storage без лимита → 10/час на пользователя (`e4fc9ecc`).
  - Проверено и чисто: в клиент уходят только `NEXT_PUBLIC_*` (anon-key, Turnstile, icon-base); `/admin` и `/api/admin/*` закрыты `getAdmin()`; аватар валидирует тип/размер; auth-роуты имеют капчу + лимиты; rate-limit fail-open (сбой лимитера не запирает вход).
- [x] **Кроны живы:** `sync-prices` — ежечасно (GH Actions) + ежедневный Vercel-крон; `sync-patches` — по расписанию; `sync-weapons`/`migrate-*` — по `workflow_dispatch` (правильно, гоняются при патче).
- [x] **Leaflet/ReactFlow lazy:** `next/dynamic` + `ssr:false` (`MapViewerLoader`, `QuestMapLoader`, `QuestMapDynamic`).
- [ ] `next build` — гоняется Vercel при деплое (локально без БД не воспроизвести).
- [ ] Мобилка-смок ключевых страниц на устройстве (за V4DYA).
- [ ] D1: спот-чек категорий предметов с 0 позиций (нужна прод-БД).
- [ ] RLS на будущую `subscriptions` (таблицы ещё нет — при старте монетизации)
