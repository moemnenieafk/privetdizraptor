---
status: 🟡 исполнено, на приёмке
affects: account, achievements
date: 2026-07-02
---
# Вкладка «Трекинг» — единый хаб прогресса игрока

## Цель
Сейчас трекинг игрока разрознен: строка-стата в Аккаунт Центре («Квестов / Бартеров / Достижений») + отдельные страницы по доменам. Нужна **единая вкладка «Трекинг»** в Аккаунт Центре: игрок видит СВОЙ прогресс по каждой игре в одном месте. v1 = достижения (вотчлист + разбивка по редкости — закрывает отложенное из [[done/achievments-redesign]]); квесты/бартеры/убежище подселяются следующими итерациями без переименования.

## Развилки — ЗАКРЫТЫ V4DYA (2026-07-02, два раунда Q&A)
| Развилка | Решение |
|---|---|
| Где живёт | **Вкладка в Аккаунт Центре** (кросс-игровой личный кабинет; там же стата и сброс) |
| Домены v1 | **Только достижения** (квесты/бартеры/убежище — следующие итерации) |
| Режим | **Гибрид**: быстрые тогглы прямо во вкладке + ссылки в разделы за деталями |
| Первый экран | **Вотчлист «отслеживаю»** — «мои цели в одном месте»; обзор ниже |
| Карточка вотчлиста | **С SMART-подсказками**: иконка + имя + бейдж редкости + % + тоггл ✓ + чипы-кросслинки (босс/карта/торговец из `achievement-hints`) |
| Обзор-блок | **Разбивка по редкости**: общий прогресс-бар (X/110) + три счётчика по тирам в цветах редкости (легендарные X/60, редкие X/23, обычные X/27) |
| Название вкладки | **«Трекинг»** (под будущий рост доменов) |
| Игровая обвязка | **Секция-шапка EFT** (лого+название, как GameResetCard): структура = список секций по играм, пока одна. Мультигейм-ready без фейкового переключателя |

## План (v1)
1. **`src/app/account/page.tsx`** (RSC) — дозагрузить `getEftAchievements()` + `getEftMaps()`/`getEftTraders()`, резолвнуть SMART-подсказки для всех 110 server-side (`resolveAchievementHint` → `Record<id, AchievementHint>`), передать в `AccountCenter` (achievements + hints).
2. **`AccountCenter.tsx`** — новый таб `tracking` («Трекинг») в TabId/навигацию; рендер `<TrackingPanel>`.
3. **Новый `src/app/account/TrackingPanel.tsx`** (client, co-located как остальные панели кабинета):
   - секция-шапка EFT (лого + название; список секций по играм — пока одна);
   - **вотчлист**: `trackedIds` из `useAchievementStore` (mounted-гард!) → карточки: иконка (`achievementIconUrl`), имя, бейдж редкости (`rarityMeta.badgeClass`), %, тоггл ✓ (`AchievementTrackToggle` compact — выполнение убирает из вотчлиста по логике стора) + ★ снять с отслеживания + чипы-кросслинки из hints; клик → деталь `/eft/progress/achievements/[id]`;
   - пустое состояние: CTA «Отмечайте ★ на достижениях» → ссылка в раздел;
   - **обзор**: прогресс-бар X/110 + 3 тира (счёт по `normalizedRarity` × `completedIds`), цвета редкости;
   - мобилка по канону `cta-mobile` (карточки 1-в-ряд <640px).
4. Чипы-кросслинки: вынести рендер чипа из `AchievementDetail.tsx` в общий `features/achievements/HintChips.tsx` (DRY) ИЛИ лёгкий локальный рендер в панели — по месту.
5. `tsc` чисто → REVIEW-пауза → коммит docs+code.

## Границы
- **В scope:** вкладка «Трекинг» в Аккаунт Центре (v1 достижения): вотчлист + обзор + шапка EFT; переиспользование готовых либ/компонентов достижений.
- **НЕ трогаем:** сторы/таблицы/API/синки (уже готовы в Фазе 2 достижений — только читаем); БД (**никакого db:push — чистая фронт-агрегация**); квесты/бартеры/убежище (следующие итерации); строку-стату профиля и GameResetCard (остаются как есть).

## Критерий готовности (детерминированный)
- [ ] `tsc` чисто / build ок
- [ ] Вкладка «Трекинг» в Аккаунт Центре: вотчлист показывает отмеченные ★ с чипами-подсказками; тоггл ✓ убирает из вотчлиста и двигает счётчики; разбивка по редкости сходится с реальными данными
- [ ] Состояние переживает релогин (синк Фазы 2 уже есть — проверить сквозняком)
- [ ] Пустой вотчлист — CTA в раздел достижений; мобилка не ломается

## Гарды
- Масштаб: средний фронт-таск (~3 файла + 1 новый), без миграций.
- `AccountCenter.tsx` — большой файл (1000+ строк): править хирургически, панель — отдельным файлом.
- Ручное подтверждение: не требуется (БД не трогаем). Коммит — по «ок» V4DYA после приёмки.

---

## Исполнено (2026-07-02) — на приёмке V4DYA
`tsc` чисто. БД не тронута (чистая фронт-агрегация, как и планировали).

- **Новый `src/components/features/achievements/HintChips.tsx`** — BossLink/ChipLink вынесены из AchievementDetail (DRY) + новый компактный `MiniChip` для вотчлиста. Без "use client" — работают в RSC и клиенте.
- **`AchievementDetail.tsx`** — переведён на общие чипы, локальные дубли и лишние импорты удалены (surgical).
- **`achievement-visuals.ts`** — в `RarityMeta` добавлен `barClass` (заливка мини-баров тиров).
- **`account/page.tsx`** — дозагрузка `getEftAchievements` + maps/traders, SMART-подсказки резолвятся server-side для всех 110 (пустые не передаются — экономия payload).
- **`AccountCenter.tsx`** — таб `tracking` («Трекинг», иконка `icon-eft-progress`, второй после Профиля); пропы `achievements`+`hints` прокинуты в панель.
- **Новый `src/app/account/TrackingPanel.tsx`**:
  - секция-шапка EFT (лого + счётчик «Достижения: X/110» справа); реестр `TRACKING_GAMES` — мультигейм добавится записью;
  - **вотчлист**: карточки с тинтом редкости, иконкой, бейджем, %, тогглом ✓ (выполнение убирает из вотчлиста — логика стора), ★-снять-с-отслеживания, до 4 MiniChip-кросслинков (босс/карта/торговец); клик → деталь;
  - пустое состояние: CTA «К достижениям»;
  - **обзор**: общий прогресс-бар X/110 (`bg-success`) + 3 карточки тиров с мини-барами в цветах редкости;
  - mounted-гард, мобилка (тиры 1-в-ряд <sm, чипы wrap).

**Приёмка (npm run dev, залогиненным):**
- [ ] `/account` → таб «Трекинг»: шапка EFT + счётчик
- [ ] отметить ★ пару достижений в разделе → появились в вотчлисте с чипами-подсказками
- [ ] ✓ на карточке вотчлиста → уходит из вотчлиста, счётчики/бары растут
- [ ] ★ на карточке → снимается с отслеживания
- [ ] пустой вотчлист → CTA в раздел
- [ ] мобилка не ломается

---
*Процесс: [[engineering-loop]]*
