---
status: 🟢 собрано автопилотом на ветке feat/player-progress-profile (2026-08-16) — ждёт живой сверки + merge V4DYA
affects: eft-hub, progress, profile, archetypes, karma, standing, arcade, rookie, homepage, nightfall
date: 2026-08-16
---

# Раздел «Прогресс» и Профиль игрока (автопилот-сборка)

**Статус:** 🟢 собрано на ветке, tsc+eslint зелёные, **в main НЕ влито, НЕ задеплоено** (гейт V4DYA).
**Затрагивает:** [[adaptive-hub-ulta]] · [[companion-gamification]] · [[player-roles-ru-expansion]] · [[feature-gating-entitlements]] · аркада · rookie.
Артефакты прогона: `.autopilot/player-progress-profile/` (brief/manifest/spec/interfaces/tickets/state).

## Контекст
Бриф V4DYA (полный автомат, `/autopilot`): единый раздел «Прогресс и Профиль» — автоопределение архетипа, кастомизация главной по архетипу, очки+карма с гейтом доступа, Зал автоматов на репутации, гайд «10 шагов» с бонусами; современный тактический дизайн + анимации. Источник: `road-to-release/_raw-legacy/2026_07_25_Разделы_«Прогресс»…`.

## Ключевой вывод аудита (5 разведчиков)
**~65% брифа уже в проде** → фича = **связать + дополнить + переодеть**, не строить с нуля:
- Архетипы (R01–R04): система «Ульта» — `role-inference.ts` (11 ролей+оси+`reasons[]`), `RoleAutoWire`, `useRoleStore.effectiveRoleFor`, `RolePicker`, `useBehaviorStore`.
- Очки/карма (R06/R07): XP `useGamificationStore`; карма **×2** — companion (`profiles.companion_karma`) + comlink (`karma_events`, тиры). Единого «рейтинга сайта» не было.
- Аркада (R09): 4 игры в проде, изолированы. Туториал (R11): «Путь Новобранца», 10 этапов.

## Что построено (T2, 4 таска, 2 волны)
- **T01** (`d73df2d4`) — тактический кит `src/components/features/profile/` (dog-tag, ранг-шеврон, бейдж архетипа, SVG-радар из `RoleAxes`, XP-полоса, roll-up, standing-панель) + чистый агрегат `src/lib/player-standing.ts` (`computeStanding` + `unlockedBy`: earned-фичи по рангу, не по плате) + `archetype-visuals.ts` + `useFirstVisitStore`.
- **T02** (`f7f6509c`) — досье `/eft/hub`: `AdaptiveHubClient` → полное досье из кита (hero-band+радар+standing+8 разделов+ролевой хаб+RolePicker+онбординг), серверная карма через RSC, узел в `HEADER_DICTIONARY` («Досье оперативника»), аноним/сбой деградируют. `dossier-view.ts` — чистые хелперы.
- **T03** (`14178247` +аменд) — главная EFT по архетипу: `EftHomeHubClient` — роль-блок сверху + переупорядочивание каталога `EFT_HUB_CARDS` по роль-приоритету (§4.7, данные не JSX).
- **T04** (`0af05644`) — бонус за туториал (10/10 → косметик-бейдж «Адаптирован», `useCosmeticStore`, идемпотентно) + аркада→standing (рекорды в агрегат + `StandingPulse`-тост).

## Решения за пользователя (ASSUMPTIONS, полный автомат)
A1 localStorage-first (без `db:push`) · A2 очки-гейт клиентский (доп-функции+косметика, не security) · A3 репутация = surface comlink + личный standing-агрегат · A4 архетипы = 11 `PlayerRole` · A5 расширяем adaptive-hub · A6 бонус = косметика+standing · A7 досье на `/eft/hub` · A8 «Путь Новобранца» = «10 шагов» · A9 две существующие кармы · A10 авто-инференс + флаг первого входа · A11 NIGHTFALL + существующие keyframes, SVG-радар · A12 первый-вход = основное состояние · A13 аноним → «войди, чтобы связать».

## ⚠️ Отложено — требует ТВОЕГО `db:push`/сервера (не сделано осознанно, гейт)
1. **R08-server:** серверный enforce очки-гейта + реестр `src/lib/features/**` (сейчас клиентская витрина).
2. **R10-DB:** кросс-юзер лидерборд / глобальная таблица рейтинга.
3. **R17i-DB:** кросс-девайс синк досье через БД.
4. **R12-server:** премиум-дни за туториал (серверный idempotency).

## Приёмка (G2 + G4 независимые)
- **G2** (покрытие): нашла 3 даунгрейда (R08→косметика, R10→личный, пустое-состояние) → исправлены в спеке до сборки.
- **G4** (слепая против брифа): **ПРИНЯТО С ОГОВОРКАМИ, 0 критичных провалов.** Все 5 буллетов реализованы+связаны. Partial по буллету 2 (главная переставлялась надстройкой) → **закрыт амендом T03** (каталог теперь переупорядочивается по архетипу). Оговорка буллета 3: гейт — витрина, серверный enforce отложен (см. выше).

## Не проверено живьём
Дев-сервер не запускался (риск фриза на общий `.next`). На живую сверку V4DYA: играбельность 4 Canvas-игр, серверное чтение кармы под логином (гоча ES256-JWT), визуальная посадка анимаций, пересчёт роли при OCR. Иконка бейджа «Адаптирован» = best-guess `story-tour.svg`.

## Ревизия по грилу V4DYA (16.08, та же ветка) — 7 решений
Грил-сессия уточнила фичу до сборки визуала. Решения:
1. **Профиль ЧВК из игры:** загрузка `profile.json` → парсер (shape `docs/15045.json`) → `usePlayerStore` → везде. 15045 = фикстура.
2. **Главная:** одна сетка по архетипу (R05), БЕЗ блока «Под твой стиль».
3. **Аркады:** `rookie`→«Аркады» (аркада на индексе), «Путь Новобранца»→`/eft/progress/rookie/path`; дубль «Кто ты в игре» убран; иконка `.icon-eft-arcade`.
4. **Шапка Досье:** компонент **HubNav** (облик — из Figma V4DYA → зависимость).
5. **Топология:** всё в Досье `/eft/hub` (архетип+standing+полная стата+загрузка); `comlink/players`→редирект (уедет с визуалом Досье); кнопка-профиль→Досье.
6. **Идентичность = 3 явных слоя:** архетип (сайт) · EFT-класс/ур./престиж (игра, справочно) · standing/карма (портал). Не смешиваются.
7. **Порядок:** плуминг сейчас, скин Досье по Figma потом.

### Сделано в плуминг-волне (не-визуальное, tsc+eslint зелёные, на ветке)
- **P1** `41d710e9` — `src/lib/parse-profile.ts` (адаптер над существующим `player-stats`-парсером) + авто-upsert ProfileUpload → `usePlayerStore` + EFT-поля (memberCategory/K-D/kills/deaths).
- **P2** `d6d71b59` — раздел «Аркады» (инверсия вложенности), иконка через `icons.css`, слаг сохранён, `/rookie/arcade`→308-редирект, 10 шагов без смены URL.
- **P3** `a9ee2b87` — главная одной сеткой (`orderCardsByRole`), блок «Под твой стиль» убран.
- **P4** `f4eee237` — 4 кросс-зонные ссылки → `/path`; пункт меню «Кто ты в Игре»→«Путь Новобранца».

### Ждёт Figma V4DYA (§6 дизайн — не трогаю)
Визуал Досье: компонент **HubNav** (шапка) + раскладка полной статы (15045-поля уже в сторе) + 3 слоя идентичности с иконками уровней/престижа (`public/images/character`, `public/icons/eft`) + карма-визуал. С этим же визуалом: редирект `comlink→Досье` + перенос загрузки профиля в Досье. `SyncPmcButton` — решить, убирать ли (стал частично избыточен).

## Следующий шаг (замыкание петли)
Плуминг на ветке (P1–P4) + автопилот-сборка (T01–T04) → **живая сверка + Figma-скин Досье** → merge в main → деплой (всё за V4DYA). После merge — статус ✅, заметку в `done/`.
