---
status: 🟢 журнал автопилот-сессии — верифицированный board по папке
date: 2026-08-03
branch: autopilot/road-to-release
---

# Автопилот-сессия 2026-08-03 — проход по road-to-release

## Контекст
V4DYA ушёл отдыхать (не спал ночь), включил autopilot + дал добро на «логичные и адекватные» решения по развитию ЦТА. Задача: пройти задачи из этой папки по очереди в его отсутствие.

**Рамки, которые я держал:** ветка `autopilot/road-to-release` (не main), чужие незакоммиченные правки не трогаю, `db:push`/схему/пуш в main — не делаю, дизайн-форки вслепую не строю (§5 «участвовать в решениях»).

## ⚠️ Главный вывод
Папка на проверке **живым кодом** оказалась в основном **ЗАКРЫТА** (спринт конца июля). Авто-триаж сабагентом **НЕнадёжен** — выдал ложные «не реализовано». Я верифицировал вручную и **намеренно не строил фичи вслепую**: почти весь остаток — это либо **твоё дизайн-решение**, либо **блок по данным 1.1.0.0**. Ниже — точный board.

## ✅ Проверенные ложные срабатывания триажа (НЕ трогал — уже сделано/не баг)
- **«Строка иконок торговцев» в побочках** — УЖЕ реализована: `QuestFilterBar/index.tsx:102–126` (портреты 32×32, фильтрация, выделение `ring-(--primary)`). Триаж сказал «не найдено» — неверно.
- **Баг «Выполнено»** — логика тоггла в `useQuestStore.ts:40–50` корректна (add/remove из `completedQuests`). Явного бага нет; вероятно уже пофикшен в июле.
- **Токен-синтаксис `text-(--color-text-secondary)`** — НЕ нарушение: §6 благословляет var-форму (`bg-(--color-base)`), обе формы валидны (var-форма: 90+ мест, named-форма `text-text-secondary`: 866). Стандартизация одной — субъективный выбор → твой. Не трогал.

## 🔵 Готово к твоему решению (1 строкой направишь — сделаю быстро, код промаплен)
Это **дизайн/продуктовые форки** — нужен твой вкус или бизнес-развилка, не мои догадки:

| Задача | Заметка | В чём нужна ты | Файлы (промаплено) |
|---|---|---|---|
| **11 архетипов** (авто-детект + смена раскладки хоума) | Прогресс | определить архетипы, триггеры, что перестраивается | `useGamificationStore.ts` (нет поля archetype), `types/gamification.ts` |
| **Редизайн калькулятора бартеров/крафтов** | Прогресс | сам дизайн раскладки, cost-breakdown, связь с УЛ | `progress/barter/BarterPageClient.tsx`, `barter-calc.ts` |
| **Объединить «Важное» + «Трекер предметов»** | Сборки/Трекер | как объединять UX + судьба схемы | `progress/needed/NeededItemsClient.tsx`, `progress/tracker/ItemTrackerClient.tsx` |
| **Полиш карточки квеста** (шрифты/кнопки/prev-next/«на карту») | Карта заданий | размеры, раскладка, какие кнопки | `QuestDetail/index.tsx` |
| **«Я в рейде» OCR-сканер** | Я в рейде | требует Gemini API (деньги) + ML-датасет + пейволл-тиры (монетизация в работе) | нет реализации; нужен `/api/eft/ocr-scan` |
| **Admin role-switcher / пейволл-тиры** | Я в рейде | завязано на монетизацию (`monetization-subscriptions.md` в работе) | `account/`, `types/gamification.ts` |
| **Подсветка меченых комнат на карте** (полигоны) | Меченые | цвета/стиль + есть ли данные `polygon` | `MapViewerClient.tsx` |

## ⏸️ Ждёт данных 1.1.0.0 (не трогаю до синка — §4.11)
- **Двусторонние связи квестов** (вперёд-назад, симметрия) — граф ждёт новой квест-базы. `data/quests/…`, `lib/quest-path.ts`.
- **Логика «Билета»** (авто-зачёт цепочки Смотрителя) — зависит от финальной структуры цепочек после реворка.
- **Полная сверка цепочек с Wiki** — ручной аудит после ингеста.
- (Все три — ровно то, что заморожено в [[BSG 1.1.0.0 — влияние на портал.md]].)

## 🟢 Полностью закрытые заметки (проверено — задач нет)
- **Бартер** — фильтры/аналитика/избранное готовы (`useBarterStore`, `BartersClient`).
- **Карты — near-fullscreen** — готово (`MapFrame` считает `headerOffset`).
- **Меченые комнаты** — бэкенд+страница готовы (см. `docs/decisions/done/marked-rooms.md`); остаток — полиш (в форках выше).
- **Gunsmith caliber-grid** — готово (`BaseSelector`, `flex-wrap`).
- **z-index Завоза при дровере квеста** — готово (июль).
- **Удаление мёртвых вкладок loot-rate/price-slot** — готово (июль).

## Итог сессии
Кода не менял (кроме этого журнала) — **осознанно**: безопасного-объективного-открытого для автономной постройки в папке не нашлось, а строить твой UI/продукт вслепую — против твоего же принципа участия в решениях. Готов исполнить любой пункт из 🔵 сразу, как дашь направление одной строкой. Ветка `autopilot/road-to-release` чистая (только этот файл).
