---
title: Слои карты EFT из клиента — девять слоёв для сверки арта
status: ✅ все 9 слоёв готовы; слой 7 в заказанной форме; конвейер прогнан по ВСЕМ картам (2026-09-04) — собрано 9 карт из 13
tags: [maps, extraction, layers, customs, lighthouse, overlay]
---

# Девять слоёв карты из клиента

**Зачем это.** V4DYA рисует HD-арт карт вручную. Чтобы проверить, правильно ли расставлены
здания, дороги и объекты, нужны **оверлеи, извлечённые из клиента** и положенные в ту же
пиксельную сетку, что растр карты. Кладёшь слоем поверх своего арта — и видишь, где разошлось.

**Это не фича сайта.** Ни базы, ни UI, ни синков. Скрипты + файлы на диске.
Артефакты вне git (`map-exports/OBJECTS-MAPS/gen/<map>/<layer>/`), в git — только инструменты.

## Состав

| # | Слой | Статус | Числа по Таможне |
|---|---|---|---|
| 1 | Высоты | ✅ | перепад 53.0 м, покрытие 100 % |
| 2 | Отмывка (hillshade) | ✅ | свет 315°/45° |
| 3 | Материал террейна | ✅ | 7 семейств: soil 59.5 / gravel-sand 28.0 / rock 6.3 / dirt 6.2 % |
| 4 | **Стены зданий по этажам** | ✅ | 37.1 млн треуг. → 2.33 млн отрезков → **89 237 путей**, 165 с |
| 5 | **Граница игровой зоны** | ✅ | 415 панелей, **31.29 га**, 409 вершин полигона |
| 6 | Растительность | ✅ | 7237 экземпляров поштучно |
| 7 | **Препятствия выше 1 м** | ✅ | ОДИН слой, рез по рельефу: **12 695** экземпляров пересекли, 11 592 контура, 2.63 % рамки |
| 8 | **Дороги** | ✅ | 118 сущностей, 108 068 треуг., **61 749 м²** в зоне |
| 9 | **Камни и скалы** | ✅ | 440 всего, **319** в игровой зоне |
| + | **Рендер объектов сверху** | ✅ | 38/38 прототипов с текстурой, 421 камень + 7237 растений, EEVEE 17 и 41 с |

Слои 1–3 и 6 сделаны раньше — см. `eft-terrain-material-extraction.md`.
Числа в таблице — по Таможне. **Что собралось на остальных восьми картах — в ранбуке**,
раздел «Прогон по ВСЕМ картам».

## 🔑 Правило одной рамки

**Все слои кладутся одной аффиной, и никто не выводит её заново.**

```
px = 10687.228 − 15.3112·gx
py =  4669.899 + 15.2114·gz        (0.0653 м/px, отражение по X)
```

Файл: `map-exports/OBJECTS-MAPS/gen/customs/rooms/customs-rooms-frame.json`.

**Проверена независимо:** двери из клиента против наших замков в зеркале — **33 из 34
в пределах 5 см**, у 30 из 34 расхождение ровно 0.000. Единственный промах — «Ключ
от спецтехники», и это не дверь, а `Trunk`.

Правило вшито в код: скрипты сверяют свою аффину с этим файлом и **падают при расхождении**.
Иначе один слой однажды уедет на метр, и найдётся это в самый неподходящий момент.

## Где что лежит в клиенте

| Слой | Сцена / источник | Что читать |
|---|---|---|
| Комнаты | `<Карта>_Sound` (Таможня `level511`) | `SpatialAudioRoom` — 398 комнат, 570 боксов; `SpatialAudioPortal` — 1347 проёмов |
| Двери | именные сцены | `EFT.Interactive.Door` — 224, из них 35 с `KeyId` = BSG-id ключа |
| Выходы | `custom_DesignMain` (`level514`) | `ExfiltrationPoint` — 26, с человеческими именами |
| Граница | `<Карта>_Scripts` (Таможня `level15`) | корень `<Карта>_LevelBorders` — 450 объектов |
| Мягкие границы | `level514` | `Minefield` × 2, `SniperFiringZone` × 3 |
| Дороги | `<Карта>_Road(s)` (Таможня `level14`) | 530 меш-GO под `OO/ROAD` |
| Камни | россыпью по сценам карты | `LODGroup` + дочерний `_LOD0` |
| Здания | именные сцены, ветка `SOO_LOD0/BUILDING` | меши; габарит — `Mesh.m_LocalAABB` |

**Соглашения общие для карт, словари имён — свои.** `<Карта>_LevelBorders` и `<Карта>_Sound`
работают везде; а камни у Таможни это `Stone_0N`, у Маяка `AM_Rock_Big/Middle/Small` и
`Arid_rock_*_cliff`. Дорожная сцена есть у всех, но у Маяка нет узла `OO/ROAD`.

## ⚠️ ГОЧИ

1. **`UnityPy Mesh.export()` пишет `-pos[0]` — зеркалит X у каждого меша.** Читать вершины
   через `MeshHandler` напрямую. На этом обожглась разведка дорог: насчитала 286 мешей
   и 75 436 м² на зеркальных кусках; верно — 118 сущностей и 108 068 треугольников.
2. **LOD-дубли.** У сущности со своим `MeshFilter` дети `lod[*]` лишние; `lod[0]` берётся
   только там, где своего меша нет. Иначе тройные контуры. Плюс отсекать `_LOD1/2/3`,
   `*_SHADOW*`, `*_BALLISTIC*`.
3. **`coordinateRotation: 180` — отражение по X-индексу, а не поворот.** Разбор в
   `eft-terrain-material-extraction.md`; ошибка жила во всех трёх потребителях террейна.
4. **Бакет культинга ≠ класс объекта.** `Culling_Stones_cliffs` звучит как «скалы», но
   соседний `Stones_smallgroup` держит камни **крупнее** его медианы. Слово в имени группы
   про размер не говорит — а вот словарь имён **ассетов** говорит (см. ниже).
5. **`road_group1..5` — это НЕ дороги.** В `road_group4` девяносто шесть автомобильных колёс,
   в `road_group2` мешки с мусором. Группы реквизита «у дороги». Так же
   `factory_parking_group*`, `steamshop_big_parking`.
6. **`garbage_stone1/2/3` — не камни, а декали:** плоские пятна щебня высотой 0.1–0.5 м.
   Режутся порогом «высота > 0.6 м».
7. **60 % асфальта Таможни лежит СНАРУЖИ игровой зоны** — фоновое шоссе уходит до X −1251.
   Дорожный слой обязательно клипать маской зоны.
8. **У BSG ОПЕЧАТКА В ШЕЙДЕРЕ.** Диффуз камней `Rock_01` лежит в свойстве **`_Aldebo`**
   (не `_Albedo`), нормаль — в `_Normalmap`. Без списка кандидатов имён свойств текстура
   не разрешается и объект рендерится серым. С ним — 38 прототипов из 38.
9. **Теневые прокси проходят LOD-фильтр.** Пни `Stump0*` подсовывали `stump01_SHADOW_LOD0` —
   имя кончается на `_LOD0`. Отсев `SHADOW`/`COLLIDER`/`BALLISTIC` должен быть **безусловным**.
10. ~~**Нормализация высот на 32766 — не константа формата.**~~ **ОПРОВЕРГНУТО 2026-09-04**
   (см. гочу 15): делитель тот же, у Леса за него уходят 9 значений из 1 050 625 — это край
   int16, а не иная нормализация. Прежняя формулировка держала Лес заблокированным сутки.
11. **Игра на IL2CPP**, типтри у `MonoBehaviour` нет — поля читаются ручным разбором байтов
   по сигнатуре. Формат стабилен, но патч BSG может его сдвинуть → **смоук-тест на счётчиках
   обязателен** (398 комнат / 1347 проёмов / 224 двери / 26 выходов), иначе сломается молча.
12. **Купол неба пересекает ЛЮБОЙ рез.** `Sky Dome/Space`, `/Clear`, `/Atmosphere` — сферы
   размером с карту; в слое препятствий каждая давала **578 000 м², 97 % рамки**. Глазом
   не видно (PNG «просто залит»), ловится только счётчиком площади. Отсюда предохранитель
   «экземпляр крупнее 1.5 % рамки — не препятствие». Туда же `stencil` (прямоугольники
   рендера в пол-локации) и `decal` (12 271 штука на Таможне, 4 433 из них в ветке `BUILDING`).
13. **ВТОРАЯ ОПЕЧАТКА BSG, того же сорта что `_Aldebo`:** узел `SOO_LOD0/OUTDOOR/`**`balistic`**
   написан с ОДНОЙ «л» и мимо строгого фильтра `BALLISTIC` проходил как реквизит (793 м²
   силуэта). Фильтр прокси-геометрии теперь `BALL?ISTIC`. Мораль: имена BSG проверять
   регуляркой с допуском, а счётчики отсева — печатать вслух.
14. **Центр камня — НЕ точка внутри его сечения на высоте метра.** Валун сужается кверху,
   pivot ложится мимо; проверка «центр в силуэте» дала 100 из 264 там, где по габариту
   обводки честные 79 из 83. Кросс-проверки слоёв строить на **обводке**, а не на центре.
15. **Выброс на шаг квантования ≠ другая нормализация.** У Леса высоты уходили за делитель
   32766 — реестр объяснял это «делитель есть свойство карты». Замер: **9 значений из
   1 050 625** (0.0009 %), хвост гладкий. Это край int16. Отличать надо **долей** значений
   выше делителя, а не максимумом: при настоящей другой нормализации выше делителя окажется
   заметная часть карты, а не девять точек.
16. **Контроль сетки слайсов проверяет ОТНОСИТЕЛЬНУЮ структуру.** У Леса, Берега и Резерва
   формула промахивалась на 2152.7 / 2852.1 / 1759.1 м — но **одинаково у всех слайсов карты
   до сантиметра**, при шаге между слайсами ровно 700 м. Имена слайсов адресуют ОБЩУЮ мировую
   сетку EFT, а якорь у карты свой. Ошибка — это разъезд слайсов между собой.
17. **Ориентация splat — свойство ФОРМАТА, а не слайса.** Поштучный вердикт ронял прогон
   Леса и Берега из-за одного слайса из четырёх-пяти. Решение выносится по КАРТЕ (среднее
   баллов): Лес 2.6× за «как есть», Резерв 3.7×. Агрегат строже поштучного — настоящее
   зеркало проявилось бы на всех слайсах сразу.
18. **Слайс, на который ссылается сцена карты, — часть её террейна, где бы он ни лежал.**
   У Резерва 3 из 4 слайсов физически в `sharedassets` Таможни и Берега; без них земли не
   было под 78 411 объектами из 88 715. `with-neighbours` теперь дефолт, а не признак реестра.
19. **Зона может быть КАРМАНОМ, а не игровой зоной.** Если панели границы не образуют
   замкнутого кольца, заливка находит случайный внутренний карман: у Леса 2.45 га при карте
   около 200 га, у Резерва 0.32 га. Полигон при этом замкнут, площадь посчитана, JSON валиден —
   неправда видна ТОЛЬКО в отношении к рамке. Гарда: доля площади зоны от рамки (`plausible`,
   порог 5 %; Таможня 54 %, Маяк 36 %, Ледокол 30 %). Такой маской нельзя клипать слои.
20. **Шаг может вернуть 0 и не сделать ничего.** У Резерва и Ground Zero дорожный слой
   отработал успешно и дал **0 м² полотна**: нет ветки `OO/ROAD` (у Резерва есть `OO/ROADS`
   на 421 объект, дефолтный отбор по ней не идёт). Код возврата — не доказательство.
21. 🔴 **Неквадратный пиксель рамки НЕЛЬЗЯ отдавать Blender'у через `pixel_aspect_y`.**
   Движок выводит вертикальный охват ортокамеры не так, как предполагал код: внутри каждой
   плитки вертикальный масштаб уезжал на **1.6 %** (замер по 459 камням с известными
   координатами: +7.1 px у верхнего края плитки, −6.5 px у нижнего, −33 px за высоту плитки),
   и на КАЖДОЙ границе рядов плиток вылезал шов с повтором содержимого на 61 px. По столбцам
   швов не было — только по строкам, потому что неквадратность именно вертикальная.
   Лечение: рендерить плитку КВАДРАТНЫМИ пикселями в разрешении `(w, h·ppmX/ppmY)`, где
   вертикальный охват выводится однозначно (`ortho·res_y/res_x`), а сшивка растягивает плитку
   до её высоты в рамке. Один явный ресэмпл вместо неявной семантики движка. После починки:
   наклон −0.0005 px/px, швы −0.8…+4.1 сигмы против 14–82 до неё.
   **Как ловить:** средний скачок альфы между соседними строками на границах плиток против
   фонового уровня; на глаз шов виден только на крупном объекте, а в числах — сразу.
22. **Дефолт-путь к арту КОНКРЕТНОЙ карты в общем скрипте — мина.** У `--base-art` стоял арт
   Таможни, поэтому превью Леса композилось поверх чужой картинки и выходило в её пропорциях
   (8192×4138 при рамке 16174×16384). Рендер был верным, врало только превью. Подложка
   берётся из манифеста: у Леса главный слой `main_0.16`, у Берега `main_summer`.
23. 🔴 **ТРЕТЬЯ находка про словарь имён BSG, после `_Aldebo` и `balistic`: шейдер мха.**
   У камней семейств `AM_Rock_*` и `Arid_rock_*` материал — шейдер смешивания камня со мхом,
   и словарь свойств у него ПОЛНОСТЬЮ свой: диффуз в **`_BaseAlbedoASmoothness`**, мох
   в `_TopAlbedoASmoothness`, нормали в `_BaseNormalMap` / `_TopNormalMap` / `_DetailNormalMap`.
   Ни одного привычного `_MainTex` / `_Albedo` / `_BumpMap` там не заполнено, поэтому камни
   уходили в рендер БЕЛЫМИ: Маяк 50 прототипов из 84, Берег 41 из 98, Развязка 24 из 49,
   Лес 12 из 28. Материал при этом резолвился нормально — молчала только текстура.
   `Base` в списке кандидатов обязан идти РАНЬШЕ `Top`: нужен камень, а не мох поверх него.
   В альфе этих карт лежит **гладкость, а не прозрачность** — она безопасна лишь потому,
   что alphaClip включается очередью рендера, а у них она −1.
   **Мораль:** единого словаря имён у BSG нет; списки кандидатов расширяются по факту, а
   счётчик «без текстуры» надо печатать вслух — он единственный, кто про это говорит.
24. **Растительность лежит ВНУТРИ TerrainData — читать её надо из ТЕХ ЖЕ файлов, что дали
   слайсы.** У Резерва список файлов брался из признака `neighbourSlices.dataIn` в реестре,
   которого там не было: извлеклись 679 растений вместо 18 513, и все в левом верхнем
   квадранте — ровно там, где лежит единственный «свой» слайс. Признак в реестре как источник
   правды ненадёжен (заполняется руками), поэтому дампер теперь пишет сайдкар
   `<map>-terrain-sources.json` по ФАКТУ прогона, а конвейер читает его.
   **Это ВТОРОЙ случай, когда недобор соседних слайсов Резерва ломает слой** (первый —
   без высот сыпались слой 7 и зона). Один и тот же вход, три разных симптома.
25. 🔴 **ЧЕТВЁРТАЯ опечатка BSG и урок про то, как её ловить.** После `_Aldebo`, `balistic`
   и `shorline` — узел `colider` с ОДНОЙ «л». Фильтры прокси-геометрии должны писаться
   с допуском: `BALL?ISTIC`, `COLL?IDER`. Проверять — пересчётом ВЫХОДА тем же регэкспом
   снаружи скрипта: у Маяка 130 узлов из 525 матчили фильтр уже ПОСЛЕ фильтрации, и это
   единственное, что вскрыло дефект (в исходник при патче попал реальный символ backspace
   вместо ``, и все `_LOD1/2/3` проезжали молча).
26. **Отбор по ИМЕНАМ матчит ПУТЬ, а путь начинается с имени карты.** У Маяка профиль дорог
   искал `(road|serpentine|asphalt)`, а путь каждого узла начинается с `SBG_Lighthouse_Roads` —
   в «полотно» попало всё содержимое сцены: 1 285 сущностей вместо 19, из них 512 теней и
   437 барьеров с конусами. Отбирать надо ТОЧНЫМ СЕГМЕНТОМ пути (`(^|/)ROADS?(/|$)`), а не
   вхождением. Та же ловушка в другую сторону: мой фильтр реквизита со словом `light`
   совпал с `Lighthouse` и выкосил слой целиком. Границы слова через `` не работают —
   подчёркивание словесный символ, нужны явные `(?<![A-Za-z])…(?![A-Za-z])`.
27. **`bounds` в конфиге могут описывать НЕ ЭТУ КАРТУ.** У Терминала они откалиброваны по
   спавнам зеркала и не пересекаются с геометрией клиента НИ ПО ОДНОЙ оси (~1100 м по X,
   ~900 м по Z). Для карты без растра рамку честнее строить по габариту её содержимого:
   `make-manifest.py --from-rooms <rooms.json>` (комнаты + двери + проёмы, запас 8 %).
   Режим печатает перекрытие с границами конфига и кричит, если его нет.
28. 🔴 **ОДНА СЦЕНА МОЖЕТ БЫТЬ В `BuildSettings` ДВАЖДЫ.** У Терминала клиент везёт каждую
   из 36 сцен в двух копиях (`level600-635` и `level651-686`, смещение ровно +51): имена
   совпадают, объекты стоят в тех же координатах. Из 18 групп реестра дубли только у него.
   Без отсева по имени удваивается ВСЁ: 208 «комнат» вместо 104, 690 проёмов вместо 345,
   179 064 экземпляра меша вместо 89 532. **Картинка при этом верная** — копии ложатся ровно
   друг на друга, площадь силуэтов до и после починки одна и та же. Врут только счётчики,
   и работа делается дважды. Отсев по имени — в `mapgeom.scene_list`, `dump-rooms`, `dump-stones`.
29. **Растительность есть НЕ ТОЛЬКО в террейне.** Наш слой берёт деревья из `TerrainData`,
   поэтому на картах без террейна его нет вовсе. Но объектами сцены растительность там
   стоит: у Терминала ~20 600 узлов (`pine01..05`, `tree02`, `plant_wolf*`, кусты), из них
   **13 124 в сцене `Terminal_Background`**, которую слои пропускают как декор горизонта.
   Часть остальных уже попадает в слой препятствий классом `props`. Отдельный слой
   растительности для таких карт = экстрактор из объектов сцены по именам, как `dump-stones`
   для камней. Не сделано.
30. **Полоса высот из манифеста — это НЕ пол этажа.** У Улиц наземная полоса `[-6, 10]` задаёт
   диапазон для фильтра маркеров, а пол там 3.50 м; у Таможни полоса `2nd` = `[3, 6]`, пол 3.70.
   Резать по низу полосы значит резать мимо. Уровень пола каждого этажа ОЦЕНИВАЕТСЯ по данным —
   мода низов геометрии, попавшей в полосу, — и печатается вслух как оценка, а не как факт.
   Тот же приём слой стен применял только к подземке; теперь он общий для всех этажей.
31. **Растительность бывает НЕ в террейне.** У Улиц 3 492 объекта сцены (`ash_bush01`,
   `privet_hedge`, `olive_tree`, `birch02`), у Терминала ~20 600. Штатный экстрактор читает
   только деревья `TerrainData`, поэтому карты без террейна оставались без слоя —
   для них `dump-vegetation-objects.py`. Радиус кроны берётся из мирового габарита меша,
   а не константой: куст и сосна должны отличаться размером, иначе круги не обвести.
32. **На верхних этажах кроны деревьев лезут в планировку.** Высокое дерево пересекает
   плоскость реза 2-го и 3-го этажа и рисуется «звёздочкой» поверх стен. Отсюда отдельный
   класс `plant` в слое 7: выключается одной группой, данные не теряются.
33. **Карта без террейна — не отказ.** У Завода, Лаборатории, Лабиринта, Терминала и Ледокола
   в сценах нет ни одной terrain-ноды: «земля» там это пол, уровень оценивается модой низов
   геометрии. Но на многоуровневой карте плоский рез показывает только уровень основания.
34. 🔴 **ЧЕТВЁРТЫЙ словарь свойств, после `_Aldebo`, `balistic` и шейдера мха: ВЕРШИННАЯ
   ПОКРАСКА С ИНДЕКСИРОВАННЫМИ СЛОТАМИ.** У городских газонов Улиц (`stone_lawn_01..06`,
   `Staircase_stone_01`) материал `City_Vertex_Paint_Ground_Grass` / `…_Grass_2` не имеет
   `_MainTex` ВООБЩЕ: слоты пронумерованы — `_MainTex0` (`City_Grass_03_D`), `_MainTex1`
   (`City_Grass_02_D`), `_MainTex2` (`City_Asphalt_Ground_01_D`), нормали `_BumpMap0..2`.
   37 экземпляров из 126 рендерились белыми, и это было самым заметным пятном слоя.
   Индексы дописаны в `BASE_PROPS`/`NORMAL_PROPS` ПОСЛЕ обычных имён — приоритет `_MainTex`
   не тронут. Замер: средний цвет слоя RGB [204, 201, 198] → [92, 87, 63], белых 0.0 %.
   **Какой слой брать — выводится, а не гадается.** Вершинные краски меша тут НЕ ЦВЕТ,
   а веса смешивания (0…255 по R/G/B): на газонном сабмеше R 0.76 / G 0.21 / B 0.03,
   то есть 76 % это слой 0. Проверять так и надо — прочитать `m_Colors` и посмотреть веса.
   Отдельно: `City_rock_wall_just_normal` НЕ лечится словарём — имя буквально «только
   нормаль», альбедо у материала нет, честный выход это серый `_Color`.
35. 🔴 **`leaf` у BSG — это СТВОРКА окна или двери, а не лист.** Словарь растительности
   `VEG_RX` со словом `leaf|leaves` затащил на Улицах **532 экземпляра из 3 492 (15 %)**:
   164 створки (`Window_wood_03-…_leaf_glass_A`, `TD_Klimova_sliding_door_01_leaf_left`,
   медиана высоты 2.11 м), 173 декали (высота 0.00 м) и 195 `Scattered_Leaves_*` — опавшая
   листва, в том числе НА КАПОТАХ машин (`…_Car_Cruze_Glass`).
   **Чинить дописыванием в `VEG_NOT_RX` нельзя:** тот словарь проверяется по ПУТИ, и слово
   `door` выкосило бы всё в ветках `*_indoor_*`/`*_outdoor_*`. Нужен отдельный словарь
   по ИМЕНИ узла (`VEG_NOT_NAME_RX`). Порог высоты сам по себе тоже не спасает — декали
   дотягиваются до 0.64 м, а настоящий `ash_bush01` начинается с 0.25 м, диапазоны
   пересекаются. Проверка правки — арифметикой по группам: `bush` не изменилась вовсе,
   `broadleaf` −164 (ровно декали пальмы), `other` −368 = 164 + 195 + 9.
   Тот же корень у камней: `dump-stones.py` по `rock` ловит детские качалки из ТЦ
   (`Rocking_F1`, `rocking_loco`, `mall_rockling_safari`) — Улицы 4, Развязка 8, остальные чисты.
36. **Прототип вида для карты БЕЗ ТЕРРЕЙНА берётся из сцены, а не из списка ассетов.**
   `veg_prototypes()` ходит в `TerrainData.m_TreePrototypes`; у Улиц, Терминала, Завода,
   Лаборатории и Ледокола `TerrainData` нет НИ ОДНОЙ, поэтому расширение `--veg-assets`
   не помогает — искать нечего ни в одном файле. Рабочий путь — двойник `stone_prototypes()`:
   `veg_prototypes_scene()` берёт узел по паре (`levelN`, `path_id`), которую пишет
   `dump-vegetation-objects.py`. Пара адресует узел ТОЧНО, вместе с MeshFilter и
   MeshRenderer, то есть с материалами — поиска по имени нет вовсе.
37. **Экземпляр объекта сцены несёт ПОЛНЫЙ TRS, и выбрасывать его нельзя.** Формат
   растительности из `TerrainData` знает только рыскание и единый масштаб, и первая версия
   `dump-vegetation-objects.py` писала в него `rot=0, scale=1`, а позицию — центром габарита.
   Замер на Улицах: **76 % узлов повёрнуты** более чем на 1°, 769 имеют настоящий НАКЛОН
   (рыскания мало, нужен кватернион), **у 50 % масштаб не единичный** (0.09…2.68), а у
   `privet_hedge`/`fibert_hedge_*` медиана рыскания ровно **90°** — 752 живые изгороди легли
   бы поперёк улиц. Смещение пивота от центра габарита: медиана 0.11 м, макс 3.16 м.
   Настоящие `quat`/`scale`/пивот пишутся отдельными полями, поля старого формата остаются
   нулевыми ради совместимости, а потребитель выбирает по наличию `quat`.
38. 🔴 **ПОЛОСА ВЫСОТ САМА ПО СЕБЕ НЕ ПРИЗНАК ЭТАЖА — нужна вторая координата.** Развитие гочи 30
   («полоса — не пол»). У Развязки рельеф доходит до 39.3 м, а полоса «2-й этаж» из конфига —
   [25, 34]: **15.65 % площади рельефа лежит выше 25 м**. Замер на свидетелях, про которых точно
   известно, что они стоят на земле: из 11 941 экземпляра растительности **2 963 (24.8 %) попадают
   в полосу второго этажа**, и превышение над рельефом у них медиана **0.00 м**. Без гарды
   четверть наземного хлама уехала бы в «этаж» и была бы срезана плоскостью не на своей высоте —
   а хуже того, отравилась бы сама ОЦЕНКА пола, потому что мода низов в полосе считалась бы
   по этому хламу.
   **Разделитель — высота основания над МЕСТНЫМ рельефом.** Коридор измеряется, а не выбирается:
   наземные объекты в полосе дают p95 = 0.02 м, комнаты ТЦ с меткой уровня — минимум 5.75 м;
   любой порог внутри коридора отсеивает 97 % земли при 0 % потерь этажей. Порог живёт
   в `layers[].minAboveGround` манифеста, гарда работает ТОЛЬКО при наличии террейна и ТОЛЬКО
   на не-главных полосах: наземный слой обязан покрывать всю карту без потолка.
   ⚠️ Карта высот ПОД зданием бессмысленна: под комнатами ТЦ она отдаёт 9.3 м — глобальный
   минимум рельефа, то есть заполнитель. Гарда работает снаружи, где рельеф настоящий.
39. **Разметка звуковых сцен клиента — бесплатный эталон для этажей.** BSG пишет уровень прямо
   в путь комнаты (`SpatialAudioSystem/MALL_ZONE_ALL/MALL_1STLVL_ALL/…`). Это независимая
   проверка полос из `EFT_MAP_CONFIG`: у Развязки 79 из 83 комнат уровня 1 попали в [25, 34),
   101 из 101 уровня 2 — в [34, 1000]. И оценка пола сошлась с ними до 0.1 м (27.00 против 27.1,
   36.50 против 36.6). Прежде чем верить полосе — сверься с тем, что клиент говорит сам.
40. **Манифест из НАСТОЯЩЕГО растра правят точечно, а не пересобирают.** У Развязки этажи есть
   в конфиге, но в манифесте `layers[]` тайловый (один `main` без полос), потому что
   `heightRange` карты = null. Пересборка через `make-manifest.py` пересчитала бы `crop`
   и `bounds`, а на них завязаны ВСЕ слои карты — сдвинулась бы рамка. Дописывать полосы
   в существующий файл, `crop`/`boundsFromConfig` не трогать, рядом класть `.bak`.
41. **Поэтажность проверяется НЕПЕРЕСЕЧЕНИЕМ слоёв, а не красотой картинки.** Если «2-й этаж»
   на самом деле наземный хлам, он весь окажется и в наземном слое. У Развязки пересечение
   вышло 0.7 % (2-й внутри ТЦ), 2.8 % (2-й снаружи), 1.1 % и 4.2 % у 3-го — слои почти
   непересекающиеся, значит этажи настоящие. Второй ориентир — доля внутри габарита здания:
   у 3-го этажа 99.2 %. У 2-го только 30.4 %, и это НЕ дефект — звуковые зоны покрывают
   интерьер, а не эстакаду-парковку; проверять надо непересечением, а не габаритом.
42. **Перевод карты на этажи не имеет права менять наземный слой.** Контроль — попиксельное
   сравнение с прежним одиночным прогоном: у Развязки **IoU 1.0000**, самопроверка камнями
   11 из 11 как была. Если наземный слой поехал, полосы построены неверно.
43. 🔴 **ПОДЗЕМНОМУ ЭТАЖУ ГАРДА «НАД РЕЛЬЕФОМ» НЕ ПОМОГАЕТ — фильтровать надо КЛАССЫ.**
   Гоча 38 спасла Развязку, но на Резерве вредна. Полоса бункеров [−10000, −7.27] проходит
   по медиане рельефа (−7.0), 17.52 % площади рельефа внутри неё, а разделить по превышению
   нельзя: земля-свидетели дают p50 = 0.00 м, подземные комнаты p50 = −4.14 м при p90 = −1.16.
   Коридора нет. Лучший размен отсекает 10 % подземелья, и это НАСТОЯЩИЕ подвалы
   (`Uchebka_basement` ×10, `Kitchens_basement` ×5): карта высот под постройками идёт
   ПО ПОЛУ ПОДВАЛА, а не по поверхности (под −9.51 она отдаёт −9.48).
   **Работает другое правило: классы-ландшафт на этаж не берутся** — `stone` ни на одной
   не-наземной полосе, `plant` дополнительно на подземных (см. гочу 51). Дерево не растёт в бункере, скала не стоит в подвале — их
   попадание значит лишь, что плоскость реза прошла сквозь ствол или склон. Замер на Резерве:
   в бункерах было 512 растений и 74 камня, и камни (скалы южного склона, залитые силуэтом)
   давали слою **8.17 % рамки при 85.5 % пересечения с наземным**. После правила —
   **0.341 % и 18.2 %**, наземный слой не шелохнулся (IoU 1.0000). К верхним этажам правило
   НЕ применяется: там крона — настоящее препятствие (см. гочу 32). Перебивается полем
   `dropClasses` в манифесте.
44. **Пересечение поэтажных слоёв само по себе не дефект — смотреть надо, чем оно набрано.**
   Стена здания идёт от земли до крыши и честно пересекает плоскость каждого этажа: у Резерва
   это даёт 8–26 % и это норма. Тревога — когда пересечение набрано ПЛОЩАДЬЮ залитых силуэтов
   (85.5 % у бункеров до правки). Считать долю пикселей слоя, которые есть и в наземном,
   и смотреть на крупнейшие контуры, а не на сам факт перекрытия.
45. **Карта высот под постройками недостоверна — и врёт по-разному.** У ТЦ Развязки она
   отдаёт 9.3 м = глобальный МИНИМУМ рельефа (заполнитель), у построек Резерва идёт по полу
   подвала. Любая гарда, опирающаяся на «высоту над рельефом», работает только СНАРУЖИ зданий.
   Проверять надо там, где рельеф настоящий, а внутри — другим признаком.
46. 🔴 **ПОЛОСА БЕЗ НИЖНЕГО ПРЕДЕЛА НА КАРТЕ С МОРЕМ БЕССМЫСЛЕННА.** «Подземелье» Берега в
   конфиге — [−1000, −5]. Но бо́льшая часть рамки Берега это МОРЕ: медиана рельефа −47.2 м,
   суша 5.7 % площади, и **98.35 % рельефа лежит внутри этой полосы** (там же 98.9 %
   растительности и 99.0 % камней). Мода низов ушла на **−55 м** — подземелье собиралось
   на морском дне вместо подвала санатория. Нижний предел выводится ИЗ ГЕОМЕТРИИ: комнаты
   с меткой подвала лежат в −16.5…−3.8, полоса сужена до [−18, −5], пол встал на −7.50
   против −6.7 по комнатам. Признак беды виден сразу — оценка пола далеко от разметки клиента.
47. **Потолок наземной полосы из `heightRange` снимать обязательно.** У Берега он −1, а рельеф
   доходит до +19 м: наземный слой потерял бы всё выше −1 (0.96 % площади рельефа, около 17 %
   суши). Развитие гочи 38: «наземный слой покрывает всю карту без потолка» — не пожелание,
   а требование к манифесту. Контроль прежний: IoU с одиночным прогоном.
48. **«Подземелье» может лежать ВЫШЕ рельефа — гарду ставить по замеру, а не по названию.**
   Подвал санатория Берега на 3.27 м выше карты высот под ним (рельеф там промоделирован метров
   на десять ниже полов здания), поэтому там работает та же гарда «выше рельефа», что и на
   верхних этажах Развязки. На Резерве в такой же по названию полосе она, наоборот, вредна
   (гоча 43). Название полосы не говорит ничего — считать корридор каждый раз.
49. **Здание, чей рельеф ниже его полов, заливается наземным слоем СПЛОШНЫМ БЛОКОМ.** Рез идёт
   по рельефу, проходит под полом и сечёт монолит фундамента; замкнутый контур заливается.
   Отсюда пересечение поэтажных слоёв Берега с наземным 87–95 % при ничтожном их покрытии:
   этажи кладут интерьер ПОВЕРХ залитого блока. Это не дефект и не «стены сквозь этажи»,
   но и полезной информации наземный слой про такое здание не несёт — план даёт только этаж.
50. 🔴 **ПОЛОСА ГОВОРИТ, ГДЕ ПОЛ, А НЕ ГДЕ НАЧИНАЕТСЯ ОБЪЕКТ — фильтровать отбор на рез
   по основанию НЕЛЬЗЯ.** Признак принадлежности объекта этажу ровно один: он ПЕРЕСЕКАЕТ
   плоскость реза этого этажа. Я на время подставил в отбор маску с фильтром «основание
   внутри полосы» (она правильна для ОЦЕНКИ ПОЛА) — и стены перестали попадать на этажи:
   стена санатория Берега идёт от фундамента до крыши, её основание лежит в наземной полосе.
   Планы этажей выходили из одной мебели, без единой замкнутой комнаты. Замер A/B: 2-й этаж
   ×1.84 по пикселям, 3-й ×2.25, потеряно 0. Маски разделены — `band_ok()` (основание в полосе
   + гарда) только на оценку пола, `guard_ok()` (одна гарда) на рез.
   **Симптом, по которому это ловится:** на плане этажа нет НИ ОДНОЙ замкнутой комнаты,
   только пунктир из мебели. Смотреть на картинку этажа, а не на счётчик экземпляров.
51. 🔴 **СКАЛЫ РЕШАЮТ ПЛОЩАДЬЮ, А НЕ СЧЁТОМ — считать надо площадь.** Вернув стены (гоча 50),
   я вернул и скалы: полоса «5th» Резерва подскочила до 7.48 % рамки при 85.5 % пересечения
   с наземным. По СЧЁТУ виноватыми выглядели деревья (70 % экземпляров), но разбор по
   ПЛОЩАДИ показал: камней 71 из 1 522, и они дают **98.8 % залитой площади слоя**
   (в полосе «2nd» — 19 из 516 и 99.5 %). Отсюда правило: **`stone` не берётся ни на одной
   не-наземной полосе** — природная скала не бывает этажом ни на какой высоте.
   `plant` убирается только под землёй: наверху крона реальное препятствие (гоча 32),
   и по площади это 0.1 %. Итог: 5th 7.48 % → 0.17 %, 2nd 1.89 % → 0.04 %.
   Мораль общая: разбирая раздутый слой, считай вклад класса В ПЛОЩАДЬ. По числу экземпляров
   виновник теряется на фоне многочисленной мелочи.
52. 🔴 **НА КАРТЕ БЕЗ ТЕРРЕЙНА ПОЭТАЖНОСТЬ ИДЁТ БЕЗ ГАРДЫ — и нижняя полоса ловит склон.**
   У Эпицентра карты высот нет вовсе, `minAboveGround` сравнивать не с чем. При этом карта
   стоит на склоне: медианы низа комнат `1F` по областям идут от 19.8 м (Area_04) до 24.4 м
   (Area_03). Плоскость полосы «Гараж» (17.00 м) ниже пола на востоке, но задевает наземку
   на западной низкой полосе. Замер: **55.5 % пикселей слоя лежат вне обеих зон гаража**
   (гаражей на карте два, в разных концах — проверка «внутри габарита» тут почти бессильна:
   общий габарит и так 36 % рамки). Состав слоя честный (`props` 56.9 % площади,
   `building` 42.8 %, ландшафта нет) — это не мусор, а геометрия зданий на 17 м, просто
   часть её относится к первому этажу низкой части карты.
   **Лечится ПСЕВДО-РЕЛЬЕФОМ:** поверхность земли восстанавливается из самой геометрии
   (низкий перцентиль оснований объектов по сетке), после чего штатная гарда работает.
   Дыра общая для Эпицентра, Улиц, Терминала, Ледокола, Лаборатории и Завода. Не сделано.
53. **Разметку этажей клиент пишет по-разному — шаблон подбирается под карту.** У Развязки
   и Резерва это `MALL_1STLVL_ALL` / `_1st_`, у Берега `_f1`, у Эпицентра `_1F` и `_-2`
   для подземных уровней. Регэксп, сработавший на одной карте, на другой даёт ноль совпадений
   и создаёт ложное «сверять не с чем». Посмотреть НА ПУТИ комнат глазами, потом писать шаблон.
54. 🔴 **ПУСТОЙ `layers[]` В МАНИФЕСТЕ = СЛОЙ СТЕН НЕ ДЕЛАЕТ НИЧЕГО.** `cut-walls.py` берёт
   высоты реза ТОЛЬКО из `manifest.layers[].heights`; нет полос — нет ни одного реза.
   У Терминала в `EFT_MAP_CONFIG` стоит `layers: []` и `heightRange: null`, поэтому
   `terminal-walls.json` содержал `layers = {}`, **`meshesRead = 0`, `trianglesProcessed = 0`**
   и НИ ОДНОГО SVG. Формулировка «манифест синтетический, стены дают сводку без поэтажных SVG»
   описывала следствие и маскировала то, что слой пуст ЦЕЛИКОМ. После добавления полос:
   1 568 мешей, 97.4 млн треугольников, 4 SVG, 486 зданий, 14 474 проёма.
   **Проверять надо `meshesRead`,** а не наличие файла: JSON пишется и при нулевой работе.
55. 🔴 **ПИК РАСПРЕДЕЛЕНИЯ ВЫСОТ — НЕ ОБЯЗАТЕЛЬНО ЭТАЖ.** У Терминала пики оснований
   на пол+2.6, +5.2, +7.8 выглядят как безупречная этажность. Проверка ПО ИМЕНАМ показала:
   это ярусы штабелированных морских контейнеров (`container_6m_close_LOD`,
   `container_12m_close_LOD`) и трубопроводные эстакады; шаг 2.6 м — высота контейнера.
   Архитектурных этажей на единой отметке у карты нет вовсе: 5 349 окон дают отметки
   пол+1, +4, +6, +8, +13, но у РАЗНЫХ зданий этажи на разных высотах, и горизонтальная
   полоса смешала бы этаж одного с крышей другого. Комнаты клиента тут не эталон — они
   размечают палатки и контейнеры на одном уровне.
   **Правило: прежде чем объявить пик этажом, спроси, ЧТО в нём стоит** (топ имён узлов).
   Если этажей нет — резать всё равно можно, но называть честно: ЯРУСЫ по высоте.
   Слой при этом полезен: по контейнерам лазают, эстакады блокируют проход.
56. **Слой 7 и слой стен обязаны сойтись по длине контура зданий.** У Терминала на наземном
   уровне 13 013 м против 13 226 м = **1.02×** при РАЗНЫХ высотах реза (пол+1.0 против
   пол+2.15) и полностью разных путях кода. Это сильнейшая из имеющихся проверок: расхождение
   в разы означает, что один из слоёв собран не там. На ярусах допустимо 1.1–1.3× — резы
   разъезжаются (слой 7 режет на «мода низов в полосе + 1.0», стены на «низ полосы + 1.2»).
57. **Долг: на карте без террейна пол оценивается ПО-РАЗНОМУ в двух скриптах.** `cut-walls.py`
   берёт медиану низов зданий (Терминал: −59.55, пояс −58.35 = пол+2.15), `cut-obstacles.py` —
   моду низов всех мешей (−60.50, рез пол+1.0). Обе высоты режут стену насквозь и план верен,
   но приёмы стоит свести к моде: медиана съезжает вверх на картах с многоярусной застройкой.
## Пороги классификации камней — измерены, а не выбраны

Словарь имён Маяка даёт границы **в метрах**, и они же применяются к картам без словаря:

```
*_Small  p75 = 4.1  |  *_Middle p25 =  5.8   → граница декор/препятствие  4.5 м
*_Middle p75 = 14.3 |  *_Big    p25 = 15.7   → граница препятствие/скала   15 м
*_cliff  min = 19.4,  медиана 46.8           → отдельное семейство, не ступень размера
```

Класс определяется по приоритету **группа BSG → имя ассета → габарит**, источник пишется
в JSON полем `classSource`. На Маяке имя совпадает с габаритом в 75 %, расхождения симметричны.

## Слои проверяют друг друга

Это оказалось ценнее, чем ожидалось, — и стоит продолжать:

- **Граница зоны** определяет «приземность» панели не порогом по Y, а **картой высот**: панели
  на глубине −25…−45 м оказались настоящими стенами по кромке прудов (земля под ними −12…−18),
  а панели на −1 м — подземными. Грубый порог терял первые и брал вторые.
- **Камни** сверены со **слоем комнат**: 0 из 420 центров внутри помещений.
- **Один блокер ломал ТРИ слоя Резерва.** Недобранные соседние слайсы террейна → нет высот →
  (1) слой 7 собрал 0.23 % рамки, (2) зона упала с «панели не образуют замкнутого контура»,
  потому что фильтр приземности врал без высот, (3) числа камней и стен были заведомо неполны.
  После починки одного входа зона собралась за 5.3 с, а препятствий стало 10 025 против 1 214.
  Вывод для следующей карты: **сперва убедиться, что земля покрывает рамку**, и только потом
  разбирать «странные» падения слоёв выше по конвейеру.
- **Препятствия сверены со стенами и камнями.** Длина контура `building` слоя 7 (23 896 м,
  рез «земля + 1 м») против стен этажа `main` слоя 4 (25 689 м, рез «земля + 1.2 м») —
  сошлось на разных резах и разных кодовых путях. Камни: под 79 из 83 камней выше 4.5 м
  в зоне есть силуэт (см. гочу 14 про центр).
- **Дороги** сверены со слоем комнат при разборе локального расхождения (ниже).
- **Стены** обязаны идти вокруг комнат — бесплатная проверка слоя 4.

## Находки для арта

Ради этого всё и делалось — два расхождения арта с клиентом, найденные слоями:

1. **Северо-восточная лента вдоль воды**: дорожный слой смещён **~5 м западнее** нарисованной
   дороги. Перекрёстная проверка слоем комнат по той же аффине: комнаты и выход
   `exit_scav_RUAFroadblock` смещены **ровно так же**, а здания совпадают. Значит расходится
   **арт локально**, а не извлечённая геометрия.
2. **У 2-этажной общаги не нарисован западный ряд комнат** — ни на `main`, ни на `2nd`.
   В клиенте он есть, и слой комнат его видит. Коридор и восточный ряд совпадают пиксель в пиксель.
3. **Камни на юго-востоке Таможни смещены влево-вверх на 6–12 м** относительно нарисованных,
   при этом заборы и трубы совпадают пиксель в пиксель. Кроп для глаз:
   `gen/customs/obstacles/customs-obstacles-stones-offset.jpg`. Не вердикт — вопрос к V4DYA.
4. ⚠️ **Расхождение рамки слоёв с калибровкой сайта (НЕ ПРОВЕРЕНО).** `worldTransform` Таможни
   из `EFT_MAP_CONFIG` даёт `canvasX = 10803.2 − 15.313·gx`, рамка слоёв — `10687.2 − 15.3112·gx`:
   **116 px ≈ 7.6 м по X**. По Y сходится (4054 px — центрирование полосы арта в квадратном
   холсте тайлов). Наши оверлеи ложатся на арт пиксель в пиксель, значит подозрение на
   калибровку сайта: если она врёт, синканные маркеры Таможни смещены на 7.6 м.

## Что осталось несделанным

- **Наружные/внутренние стены разделены приблизительно** (заливка снаружи + утолщение проёмов
  на 1.1 м). Часть перегородок общаги помечена наружными. Точное разделение — только
  по ветке-источнику иерархии.
- **Рез `underground` на 0.20 м — оценка** (мода низов 636 building-мешей); в манифесте
  низ полосы стоит −1000.
- **Вторая карта — не просто смена аргумента.** Камни на Маяке отработали (2 043 экземпляра,
  словарь имён даже чище), а дорожный конфиг — заготовка: у `Lighthouse_Roads` нет узла
  `OO/ROAD`, отбор по именам тянет ~1 млн треугольников вместе с реквизитом. Нужен свой
  разведочный проход по сценам, как делали для Таможни.

## Инструменты

| Файл | Роль |
|---|---|
| `scripts/eft-rooms/dump-rooms.py` | комнаты, проёмы, двери, выходы, indoor-объёмы, габариты зданий |
| `scripts/eft-rooms/render-rooms.py` | оверлей комнат: PNG в сетке растра + SVG с подписями, по этажам |
| `scripts/eft-map-layers/dump-zone.py` | граница игровой зоны: полигон + маска + минные/снайперские зоны |
| `scripts/eft-map-layers/dump-roads.py` | дороги: полотно, разметка, площадки, тротуары; клип по зоне |
| `scripts/eft-map-layers/dump-stones.py` | камни поштучно с классом размера и флагами охвата |
| `scripts/eft-map-layers/cut-walls.py` | стены сечением по этажам; группы `walls-outer/inner`, `openings`, `fence`, `props` с `data-source` |
| `scripts/eft-map-layers/cut-obstacles.py` | **слой 7**: препятствия выше 1 м одним слоем, рез поверхностью «земля + 1 м»; PNG-силуэты + SVG |
| `scripts/eft-map-layers/mapgeom.py` | **общее ядро слоёв**: сцены, меши без `export()`, рамка, земля, сечение, сшивка. Три грабли живут здесь и только здесь |

| `scripts/eft-map-layers/render-objects.py` + `render-objects-blender.py` | рендер камней и растительности сверху с текстурами и светом (Blender EEVEE, headless) |
| `scripts/eft-map-layers/run-all.py` | **весь конвейер одной командой** + шаг `recon` для новой карты |

▶️ **Порядок, точные команды, зависимости и что разведать на новой карте — `eft-map-layers-runbook.md`.**
▶️ **Что собрано по всем картам, где лежит и чему верить — `docs/state/eft-map-layers-all-maps-2026-09-04.md`.**
Запуск: `python scripts/eft-map-layers/run-all.py <карта>`.

Все универсальные — карта аргументом. Сцены берутся из `docs/registry/eft-scenes.json`
либо из `BuildSettings` напрямую.

⚠️ Объекты карты разбросаны по **всем** её сценам, а не по одной: слой камней нашёл экземпляры
в семи неочевидных сценах помимо основной. Не ограничиваться `levelN` террейна.
