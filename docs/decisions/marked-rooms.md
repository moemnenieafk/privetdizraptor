---
status: 🟢 фича готова (кроме трекер-эпика) — Фаза 0/1/1.5 ✅ + ридер+вердикт ✅ + СТРАНИЦА ✅ + КАРТА↔КОМНАТА ✅ live-verify (2026-08-03); осталось: UI подачи открытий (room_opens) + модерация
affects: maps, backend, community, moderation
date: 2026-08-02
source: "[[Меченые комнаты]]"
---
# Меченые и цветные комнаты — трекер открытий

## Цель
Меченые комнаты и цветные комнаты Лаборатории получают на карте подсвеченные границы, а по клику — отдельную страницу с экономикой ключа, лут-таблицей и живой статистикой открытий от сообщества. «Какой ключ брать и стоит ли» → один экран с готовым вердиктом.

## Границы
- **В scope сейчас (бэкенд-first, от макета не зависит):** модель данных → таблицы (DDL-модуль) → RLS → сид реестра из `map_markers` → ридеры страницы (лут-таблица + экономика ключа).
- **Ждёт макета V4DYA (Фаза 2):** страница комнаты, подсветка границ на карте, лента открытий, UI модерации.
- **НЕ трогаем:** обмен репутации на Premium (гейтит [[feature-gating-entitlements]]); раздел модерации/«колокольчик» целиком (аккаунт-центр); синканный рендер карты; near-fullscreen раскладку карты (сырой источник 2026-07-25 содержит и её — это отдельная задача [[Карты — интерактивные]]); схему через `db:push` (идём DDL-модулем).

## Контекст (чтобы не гадать)
- Дизайн-источник: [[Меченые комнаты]] (backend-дизайн 2026-08-02) · голосовое 2026-07-28.
- **Паттерн таблиц:** DDL-модуль `src/db/marked-rooms-ddl.ts` (+ `db:migrate-all`, auto-discovery) + query-def `schema-marked-rooms.ts` — как `markers-ddl.ts`/`editorial-markers-ddl.ts`. **НЕ** `schema.ts`+`db:push` (тот роняет RLS не-schema.ts таблиц).
- **Переиспользуем (не строить заново):** экономика ключа = карточка Замок→Ключ + зеркало цен (`getEftPriceIndex`); лут-таблица; каталог `getEftCatalog`; паттерн очереди/ролей модерации из Comlink `reports`; подсветка полигоном = рецепт 2 `/map-interactions`.
- **Сид `marked_rooms`:** из `map_markers` type='lock' + `meta.lockKind='marked'` (linked_item_id = ключ); цветные Лабы курируются вручную (их немного).
- §4.11: `room_opens` — НАША community-таблица (UGC-инжест через API после модерации), НЕ зеркало внешнего API → легальная запись, UI читает только её; рантайм-фетча наружу нет.
- **Лут-таблица комнаты** = фильтр `loot_spawns` (loose-точки SPT) по полигону комнаты → предметы + базовые шансы, join живых цен. Источник — SPT-зеркало. Свежий **SPT 4.1.0** (Tarkov `0.16.9.40743`) лежит в `D:\Games\SPT\SPT_Runtime\SPT_Data\database` → перед постройкой лут-таблиц пере-ингест `db:ingest-loot` (обновить env `SPT_DB_PATH` на новый путь — структура сменилась на `SPT_Runtime\SPT_Data`; заодно освежает heatmap). ✅ Проверено (2026-08-02): generic loose-пул СОДЕРЖИТ спец-лут меченок — в радиусе ~12м вокруг замка топ-EV = LEDX, ключ-карты TerraGroup, Red Rebel, кейсы (канон). Рецепт ридера: loose-точки в полигоне (если задан) ИЛИ в радиусе вокруг якоря-замка → агрегат EV (формула heatmap) → топ-N. Радиус — грубый черновик; полигон V4DYA уточнит. Якорь (x,z замка) — join `marked_rooms.key_item_id`→`markers` либо денормализация.

## Модель данных (ратифицирована, DDL-ready)
Конвенции — по `markers-ddl.ts`: uuid PK, FK→games/maps/profiles, `create if not exists`.

**`marked_rooms`** — реестр (сущность страницы + подсветка):
`id uuid pk` · `game_id`→games · `map_id`→maps · `slug` (маршрут `/eft/maps/<map>/rooms/<slug>`) · `kind` ('marked'|'lab_color') · `color?` (цветные Лабы) · `key_item_id?` (id зеркала цен → экономика ключа) · `polygon jsonb?` (границы) · `title` · `description?` · `created_at/updated_at`.
Индексы: `unique (game_id, map_id, slug)`, `(map_id)`.

**`room_opens`** — одно открытие (заявка сообщества):
`id uuid pk` · `room_id`→marked_rooms (cascade) · `author_id`→profiles? · `is_anonymous bool default false` (юзер выбрал аноним — `author_id` храним для модерации/антиабуза, на странице скрываем) · `status` ('pending'|'approved'|'rejected', default pending) · `moderator_id`→profiles? · `moderated_at?` · `proof_youtube_url?` · `proof_screenshot_key?` (ключ ВРЕМЕННОГО бакета; →null после авто-удаления) · `game_version?` · `opened_at?` · `created_at`.
Индекс: `(room_id, status, created_at desc)` — витрина «последние N».

**`room_open_items`** — предметы открытия (M:N, для ленты + пересчёта шанса):
`open_id`→room_opens (cascade) · `item_id` (id зеркала) · `qty int default 1`. PK `(open_id, item_id)`.

**`reputation_events`** — леджер (не денормализованный баланс):
`id uuid pk` · `user_id`→profiles (cascade) · `delta int` · `reason` ('room_open_approved'…) · `ref_type?` · `ref_id?` · `created_at`. Индекс `(user_id)`. Баланс = `sum(delta)`.

## RLS (defense-in-depth; пишет сервер owner-ролью, обходит RLS)
- `marked_rooms` — read-all публично (`using(true)`).
- `room_opens` — read публично ТОЛЬКО `status='approved'`; INSERT заявки — авторизованный юзер (rate-limit через mirror-таблицу, §4.11); модерация/начисление — owner-роль (сервер).
- `room_open_items` — read-all (значимы только в join с approved-открытием).
- `reputation_events` — read своей строки (`auth.uid() = user_id`); лидерборд-публичность — позже, если понадобится.

## Экономика страницы (вычисляется на рендере, не хранится)
Заголовочный вывод страницы — «выгодно ли брать ключ прямо сейчас»:
- **Цена ключа на барахолке** + **способы получения** (бартер / крафт / места спавна) — из зеркала цен и item-ридеров; переиспользуем карточку Замок→Ключ.
- **Средняя стоимость одного открытия** = средний суммарный ₽ по **approved**-открытиям (join `room_open_items` → цены, avg по открытию). Не хранится — считается на рендере, как EV heatmap.
- **Вердикт окупаемости** «Выгодно» / «В минус» = `средняя стоимость открытия − цена ключа`. Динамический, на живых ценах. 0 открытий → «нет данных» (graceful).
- **Динамический шанс редких предметов** — частоты по последним 100 approved (см. ниже).

## Витрина «последние 100» → шанс выпадения
Пересчёт по последним 100 **approved**-открытиям комнаты: `limit 100` + агрегат частот `room_open_items`. Materialized view на старте НЕ нужен; если ленты станут горячими → materialized + refresh по факту принятия. Лента «1 мин назад — …» = approved-опены `order by moderated_at desc`.

## Пайплайн модерации медиа
Скрин → приватный **временный бакет** Storage (`room-proofs/`), ключ в `proof_screenshot_key`; после вердикта — **немедленно удалить объект**, ключ→null («даже гиг много»). Видео — только `proof_youtube_url`. Модератор: заявка+медиа → выбор найденных предметов из `getEftCatalog` → «Принять» (INSERT `room_open_items` + `reputation_events` +N, `status=approved`) / «Отклонить».

## Регламент пруфов (критерии модерации — что проверяет модератор)
Накрутка отсекается строгой валидацией; на слово не верим. Открытие засчитывается, если пруф показывает:
1. **Дверь до отпирания** — в руках/контекстном меню виден ключ или карта ПЕРЕД открытием.
2. **Процесс** — заход в комнату и подбор всех предметов.
3. **FIR** — открыт инвентарь, у всех найденных предметов галка «Найдено в рейде».
4. **Тех-данные** — на видео/скрине видны текущая **версия игры, дата и время** (→ поле `game_version`).
Видео — только ссылкой на YouTube; скрин — временно, удаляется сразу после вердикта.

## Критерий готовности (детерминированный)
**Фаза 1 (бэкенд-фундамент):**
- [x] `db:migrate-all` создаёт 4 таблицы; `db:audit-rls` — RLS 60/60, все 4 с политиками ✅ (2026-08-02)
- [x] `npx tsc --noEmit` чисто
- [x] сид реестра отработал — 7 меченок засеяно (`db:seed-marked-rooms`, классификатор `lockKind`), 0 пропущено ✅ (2026-08-02)
- [x] ридер `getMarkedRoomBySlug` — построен + смоук на живой Комнате 314 (anchor, лут-таблица 24 поз. с шансом/EV, `tsc` 0) ✅ (2026-08-02)
- [x] экономика ключа решена — дешевейшее из бартер(Σ требуемых)/flea, noFlea-aware; `breakeven`=⌈keyCost/sumEv⌉ (314 бартер Егерь 4.72M→~10 откр.; РБ-ВО flea 2.36M→~6). `tsc` 0 ✅ (2026-08-02)
- [x] вердикт — «оба вместе» (V4DYA): `breakeven` (⌈keyCost/sumEv⌉) + `lifetimeProfit` (keyUses·sumEv−keyCost, uses из каталога). Исходы: РБ-ВО +2.23M (6/10) · 314 +112k (10/10 впритык) · РБ-БК **−872k** (12>10, не отобьётся). `tsc` 0 ✅ (2026-08-02)
**Фаза 2 (UI):** — после макета V4DYA.

## Развилки
- Термины → в глоссарий: ✅ сделано (меченая/цветная комната, открытие, пруф, репутация).
- Имена таблиц: `marked_rooms` / `room_opens` / `room_open_items` / `reputation_events` — ратифицировано.
- `reputation_events` read-scope: своя строка (лидерборд — позже).
- Полигон границ комнаты: одно `jsonb polygon` (как `markers.polygon`).

## Гарды
- Масштаб: **L-эпик, многофазный.** Фаза 1 = ~4 файла (ddl-модуль, schema-def, rls-в-ddl, reader) + сид-скрипт.
- **Ручное подтверждение (§5): создание таблиц.** `db:migrate-all`+`db:sql` пишут в prod-Supabase → жду «ок» V4DYA перед накатом. Схема аддитивна (`create if not exists`) → безопасна, RLS не роняет (в отличие от `db:push --force`).

## Исполнено — Фаза 1 таблицы (2026-08-02)
- **Loot re-ingest SPT 4.1.0** (Tarkov `0.16.9.40743`) → зеркало обновлено (28484 loose + 228 container-пулов, 11 карт; `SPT_DB_PATH=D:/Games/SPT/SPT_Runtime/SPT_Data/database`). Объём идентичен прошлому ингесту — счётчик точек спавна не сменился, но зеркало гарантированно на текущем патче (освежает и heatmap).
- **Таблицы:** `src/db/marked-rooms-ddl.ts` (4 таблицы + индексы + RLS) + `src/db/schema-marked-rooms.ts` (Drizzle-дефиниции, форма 1:1 с DDL). Накат `db:migrate-all` (178 стмтов, 0 ошибок). `db:audit-rls` = 60/60 RLS, все 4 с read-политиками. `tsc` чист. ⚠️ Файлы НЕ закоммичены (шаред-дерево — коммит по просьбе).
- **Фаза 1.5 ✅ (2026-08-02):** сид `marked_rooms` — 7 меченок (Customs 314, Woods общая спальня, Reserve РБ-ВО/ПКПМ/БК, Interchange странная/заброшенный завод) через классификатор `lockKind`; лут-подход проверён пробой — loose-лут в радиусе ~12м вокруг замка = канон меченки (LEDX, ключ-карты TerraGroup, Red Rebel, кейсы), лут-таблица feasible без полигона. Пробы `_inspect-marked-locks`/`_probe-room-loot` удалены после разбора; постоянный `scripts/seed-marked-rooms.ts` (+npm `db:seed-marked-rooms`) оставлен.
- **Фаза 2 — старт (2026-08-02):** ридер `src/db/marked-rooms.ts` (`getMarkedRoomBySlug` + `getMarkedRoomsForMap`) построен и смоук-проверен на живой Комнате 314 (anchor из `markers`, лут-таблица радиус 12м = 24 поз. с шансом/EV, `tsc` 0). Макет страницы одобрен V4DYA (`docs/mockups/marked-room/`). ⚠️→✅ **Находка решена:** flea-цена меченых ключей ненадёжна (314 = 5M cap). Ридер считает цену получения = дешевейшее из **бартер** (Σ требуемых, jsonb-containment по rewardItems) / **flea** (только не-`noFlea`): 314 = бартер Егерь **4 722 200₽**, РБ-ВО = flea **2 360 000₽** (бартера нет). Добавлен `breakeven`=⌈keyCost/sumEv⌉ (314 ≈ **10 открытий**, РБ-ВО ≈ **6**). `tsc` 0, смоук ок.
- **✅ Вердикт «оба вместе» (V4DYA, реализован):** `breakeven` (за N открытий отбивается) + `lifetimeProfit` (keyUses·sumEv−keyCost; uses ключа из каталога `properties.uses`). Меченые ключи = **10 использований**. Реальные исходы: **РБ-ВО +2 232 260₽** (окуп 6/10 — брать) · **314 +112 380₽** (10/10 — впритык) · **РБ-БК −871 509₽** (breakeven 12 > 10 uses — НЕ отобьётся). Только связка breakeven+lifetime вскрывает убыточность дорогого ключа РБ-БК; per-open и breakeven-alone это скрывают. Мокап «ВЫГОДНО +268k» заменить на этот блок.
- **✅ Страница построена (2026-08-02):** `src/app/eft/maps/[slug]/rooms/[roomSlug]/page.tsx` (RSC) — шапка, экономика+вердикт «оба вместе» (breakeven + профит за срок), лут-таблица (item-cell + шанс + EV), заглушка трекера. Токены NIGHTFALL, `tsc`+eslint 0. **Live-verify ✅** — рендер на запущенном dev :3000 (роут скомпилился, item-иконки грузятся, вердикт «ВЫГОДНО · окуп 10/10 · +182k за срок ключа»); скрин `docs/mockups/marked-room/live-314.png`.
- **✅ КАРТА↔КОМНАТА (2026-08-03):** V4DYA разметил комнаты в Figma → 4 marked-room SVG (Customs/Reserve/Streets/Lighthouse, тот же viewBox что арт) → `public/images/maps/eft/marked-rooms/`, размечены слагами (`scripts/annotate-marked-rooms.mjs`, по координатам фигур). MapViewerClient: 2-й `L.svgOverlay` по `imgBounds` (interactive:false, класс `cta-marked-rooms`) — сквозной кроме силуэтов комнат, клик→`/eft/maps/<map>/rooms/<slug>`; ховер-подсветка в globals.css. Live-verify ✅ (метка 314 в DOM + dispatch-клик → навигация на страницу). **Гоча:** lock-маркер сидит НА комнате (сид из него) → точный центр перекрыт пином (клик по остальному силуэту работает; пин = карточка ключа) — приемлемо для v1.
- **✅ ВХОДЫ на страницу (2026-08-03):** три пути — (1) **секция «Меченые комнаты» в левом drawer** «Поиск на локации» (список карты, always-on, самый явный); (2) **силуэт на карте** (клик); (3) **карточка Замок→Ключ → «Открыть комнату»** (приоритетный CTA). Сервер вешает `roomHref` на lock-маркеры (ключ→slug из `marked_rooms`); drawer и карточка выводят из маркеров БЕЗ плюмбинга. tsc/eslint 0.
- **✅ Трекер v1 — admin-add (2026-08-03):** `addRoomOpen` + `loadRoomOpens` (лента approved + динамический шанс, окно 100) в ридере; `communityChance` мёржится в лут-таблицу; `opens`/`totalOpens` в `MarkedRoomView`. API `POST /api/admin/room-opens` (`canEditContent`). Клиент `RoomOpenAdder` (модератор: чипы лут-таблицы → открытие). Страница: лента открытий + «сообщество N%» на луте + форма (isAdmin). Сквозняк verify ✅ (write→feed→chance→cleanup). Предметы пока из лут-таблицы комнаты (частый случай).
- **Осталось (трекер v2):** публичная подача открытия + пруф (YouTube/скрин → временный бакет) → очередь модерации (`pending`→`approved`) + репутация на аппруве; поиск предметов вне лут-таблицы; денормализация якоря (x,z) в `marked_rooms`.

---
*Процесс: [[engineering-loop]]*
