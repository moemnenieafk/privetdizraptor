---
status: 🟢 раскладка согласована грилом (V4DYA 2026-08-16) — к сборке
affects: dossier, archetypes, role-hubs, feature-catalog, routing
date: 2026-08-16
---

# Раскладка фич портала по архетипам («Избранные разделы архетипа»)

Грид+лист (Figma 2868-5301) **заменяет** карточки «Разделы архетипа» в Досье. У каждого архетипа —
свой набор «избранных» фич, подсвечен амбером (tactical-amber) в сетке иконок И в списке названий.
Набор зависит от активного архетипа. Счётчик в заголовке динамический («− N»).

## Каталог (31 фича, порядок из макета)
`1` Карты локаций · `2` Карта заданий · `3` Предметы · `4` Сюжетные · `5` Побочные · `6` События ·
`7` Прогресс · `8` Аркады · `9` Убежище ЧВК · `10` Прибыль убежища · `11` Сезоны · `12` Battlepass-трекер ·
`13` Сборки оружия · `14` Трекер предметов · `15` Важные предметы · `16` Кодекс · `17` Обновления игры ·
`18` Достижения · `19` Престиж · `20` Гайды · `21` Стримы · `22` Советы · `23` Новости ·
`24` Поиск напарника\* · `25` Кандидаты\* · `26` Биржа шерпов\* · `27` Обсуждения\* · `28` Мастер-классы\* ·
`29` Блог ЦТА\* · `30` Сообщения об ошибках\* · `31` Компаньон барахолки\*
*(\* — ещё не построены → в каталоге есть, подсвечиваются, клик ведёт на заглушку «скоро»)*

## Правила раскладки (V4DYA)
1. **`1` Карты и `3` Предметы — всегда первыми** (самые востребованные разделы).
2. **`8` Аркады — универсальный филлер «в ожидании рейда»** (кроме чисто-читающих архетипов).
3. **Контент-знания** (Гайды/Советы/Стримы/Новости/Мастер-классы/Обсуждения/Блог) + **соц-мэтчинг**
   (Поиск напарника/Кандидаты/Биржа шерпов) — дом у **Зрителя, Соклана, Шерпа**; на прочие только тематически.

## Переименования (id в коде НЕ меняются)
- `progressor`: имя «Прогрессор», кнопка → **«Я Квестовичок»**.
- `rat`: имя → **«Посижуркин»**, кнопка → **«Я посижуркин»** (было «Крыса» — самоассоциация кольнула).

## Итоговая раскладка (11/11)
| Архетип (id) | Личность | Фичи (id, порядок значим) |
|---|---|---|
| Новобранец (`rookie`) | учит мир руками | 1, 3, 8, 16, 4, 20, 7 |
| Квестовичок (`progressor`) | гриндит квесты | 1, 3, 2, 4, 5, 14, 15, 8 |
| Барыга (`trader`) | экономика/цены | 1, 3, 10, 31, 9, 15, 8 |
| Оружейник (`gunsmith`) | сборки/оружие | 1, 3, 13, 20, 22, 18, 8 |
| Инженер базы (`engineer`) | оптимизация убежища | 1, 3, 9, 10, 15, 14, 8 |
| Рейдер (`raider`) | бой/эндгейм/лут | 1, 3, 2, 5, 6, 11, 18, 15, 21, 23, 22, 8 |
| Зритель (`viewer`) | потребляет контент | 1, 3, 20, 21, 22, 23, 29, 27, 28, 8 |
| Посижуркин (`rat`) | соло-нычки, наблюдатель | 1, 3, 15, 9, 14, 31, 8 |
| Шерп (`sherpa`) | ментор, привратник знаний | 1, 3, 16, 20, 22, 28, 26, 24, 27 |
| Лорник (`lore`) | история/мир | 1, 3, 16, 4, 6, 17, 23, 29 |
| Сокланы (`squad`) | команда/мэтчинг | 1, 3, 24, 25, 27, 21, 6, 8 |

## Сборка (следующий шаг)
1. **Каталог-данные** `src/data/feature-catalog.ts`: 31 фича → `{ id, name, iconPath, href, ready:boolean }`.
   Роуты берём из `headerConfig`/существующих; будущие (`ready:false`) → заглушка «скоро».
2. **Маппинг** `ARCHETYPE_FEATURES: Record<PlayerRole, featureId[]>` (таблица выше, порядок значим).
3. **UI** грид иконок (6-кол) + список названий, амбер-подсветка избранных активного архетипа;
   **заменяет** секцию «Разделы архетипа» в `AdaptiveHubClient`. Готовые → ссылка, будущие → «скоро».
4. Правка `ROLE_LABELS`: `progressor.button`, `rat.name`+`rat.button`.
5. Заглушка «скоро» — общий роут/страница для `ready:false`.

## Расширение: +4 архетипа (11 → 15) + пикер
Имена рабочие (V4DYA причешет). Наборы по правилам (Карты+Предметы первыми, Аркады-филлер).

| id | Имя (раб.) | Кнопка | Фичи (id) | Инференс-лин |
|---|---|---|---|---|
| `seasonal` | Сезонник | «Я за сезоном» | maps, items, seasons, battlepass, events, achievements, progress, arcade | визиты seasons/battlepass/events |
| `collector` | Каппавод | «Я за каппой» | maps, items, achievements, questmap, side, tracker, needed, prestige | достижения+квесты, факт prestige>0 |
| `tryhard` | Тащер | «Я тащу» | maps, items, loadouts, streams, achievements, events, arcade | loadouts/items(бой) + высокий K/D |
| `casual` | Чилловый | «Я чиллю» | maps, items, arcade, events, streams, news, codex | низкий объём поведения + аркады |

Каждый требует: `PlayerRole`+`PLAYER_ROLES`+`ROLE_LABELS`+инференс-правило в `inferRole`,
`ARCHETYPE_VISUALS` (акцент + иконка-заглушка → V4DYA дорисует), `ROLE_HUBS`, `ARCHETYPE_FEATURES`.

## Пикер архетипа (Figma 2861-1952 триггер + 2869-5533 список)
- **Триггер** — иконка `select-archetype-icon.svg` в правом-верхнем углу правой панели Досье (у гекс-кольца архетипа). Клик → открыть пикер.
- **Пикер** (оверлей «в том же фрейме», ~348px): вертикальный список ВСЕХ архетипов. Строка 108px =
  `HexRingProgress` (80px, иконка архетипа) + кнопка-лейбл (`ROLE_LABELS[role].button`, uppercase).
  Текущий (`effectiveRole`) помечен «ВАШ» + `title_archetype`-бейдж + амбер-подсветка строки.
  Клик по строке → `setManualOverride(role)`; повторный клик по текущему — сброс. Разделители-линии.
- **Заменяет** плоский `RolePicker` (обёртка текст-кнопок «Кто ты в Таркове»).
