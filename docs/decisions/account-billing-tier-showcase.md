---
status: ✅ исполнено и запушено (ветка feat/billing-tier-showcase, live-verify пройден, Ш0 + грант + auto_renew накатаны на прод). Осталось вне кода: §2 публичной оферты → включение тумблера; E5 (адаптер оплаты)
affects: account, billing, monetization, admin, pricing, gating
date: 2026-09-06
parent: billing-subscription-management
related: monetization-subscriptions, payment-provider-yookassa, feature-gating-entitlements, account-center-redesign-2026
---

# Витрина тарифов: закрыть «Платежи» и «PRO-статус» в Аккаунт-центре

## Цель
Тариф перестаёт быть надписью «Не настроено». Пользователь видит, **что он купил и что купит**, а V4DYA меняет состав тарифа **из админки без деплоя** — потому что витрина считается из той же матрицы гейтов, по которой работает пейвол.

## Контекст: 90 % уже построено — это НЕ стройка с нуля

Инфраструктура E4 в main (merge `1ad928d8`, 25.08), БД накатана на прод. **Не переделывать:**

| Слой | Где | Состояние |
|---|---|---|
| Таблицы `tiers` / `feature_gates` / `billing_events` / `subscriptions` | `src/db/*-ddl.ts`, накат через `/api/cron/migrate-billing` | ✅ на проде, RLS 66/66 |
| Ридеры/писатели | `src/db/billing.ts` (298 стр.) | ✅ `getTiersFromDb`, `getGatesFromDb`, `setUserTier` (умеет `months` + `extend`), `writeBillingEvent` (идемпотентно) |
| Движок гейтинга | `src/lib/gating/{tiers,resolve,nav-visibility}.ts` | ✅ лестница рангов, `unstable_cache` с тегами `billing-tiers`/`billing-gates`, `invalidateGating()` |
| Реестр ключей | `src/data/gate-registry.ts` | ✅ 10 фич + автогенерация секций `sec:eft:<path>` из `HEADER_DICTIONARY` |
| Пейвол | `Paywall.tsx`, `SectionPaywall.tsx`, `GatingBoundary`, `useSubscription` | ✅ |
| Админка биллинга | `src/app/admin/billing/*` | ✅ матрица гейтов, редактор тиров (вкл. `perks`), выдача подписок, леджер |
| Вебхук платежей | `POST /api/billing/webhook/[provider]` | ✅ провайдер-независимый; без адаптера отдаёт 501 |

**Ровно три дыры** между этим и живой монетизацией:

1. **Витрины нет.** Колонка `tiers.perks` (jsonb, «буллеты витрины») **редактируется в админке и не читается нигде на сайте**. RLS-политика `tiers_read` открыта для `anon` — публичная страница цен была спроектирована, но не написана.
2. **Кабинет — заглушка.** `BillingPanel` (`AccountCenter.tsx:1126`) — одна строка «Выбранный тариф / Не настроено», в БД не ходит, даже пропа `tier` не получает. Кнопка «ИЗМЕНИТЬ» ведёт в `PlanView` (`:857`) — текст «будет доступно после запуска платёжного модуля». `ProStatusPanel` данные читает честно, но обе его кнопки упираются в тот же тупик.
3. **Адаптер ЮKassa** — `ADAPTERS = {}`. Внешний блокер (договор, касса 54-ФЗ, ключи — за Димой). **В эту задачу не входит.**

## Решения грила (V4DYA, 06.09)

Первый заход дал 4 решения, грил добавил ещё 3 и снял 2 «развилки» фактами из кода.

1. **Состав тарифа — гибрид.** Основа выводится из `feature_gates`, editorial-перки из `tiers.perks` кладутся сверху. Иначе витрина врёт: гейт поменяли — маркетинговый текст остался старым.
2. **«ИЗМЕНИТЬ» ведёт на полноценный экран сравнения**; кнопка оплаты неактивна с честной подписью. UI верстаем сейчас — на E5 останется вкрутить адаптер, а не рисовать.
3. **Публичная `/pricing`** — теми же карточками, что в кабинете.
4. **Оплата (адаптер ЮKassa) — вне задачи**, отдельным E5.
5. **Витрина выходит переключателем в админке, не деплоем.** Выключен → `/pricing` отдаёт 404. Причина: `/legal/offer` сейчас `draft: true` со штампом «не имеет юридической силы», а раздел «2. Стоимость и порядок оплаты» — пустая рыба. Публиковать цену раньше оферты нельзя, но и держать готовый код в ящике до E5 незачем.
6. **Флаг один — на все цены.** Выключен → `/pricing` 404, а в кабинете виден **состав** тарифов без цифр («цена — скоро»). Включён → цены везде. Запуск атомарный: ни одного места, где цена утекла раньше оферты. Полумеры вида «в кабинете можно, публично нельзя» отвергнуты — регистрация открытая, скриншот уйдёт в чаты за час.
7. **Запуск на ДВУХ тирах.** Грил вскрыл: в `FEATURE_MIN_TIER` **8 фич на «Оперативнике» и ровно 2 на «Ветеране»** — витрина обнажила бы карточку из двух строк за +250 ₽. Раньше это было незаметно, потому что состав тарифа нигде не отрисован. «Ветеран» архивируется и вернётся **новым** содержимым. Тиры живут в БД → третий добавляется формой в админке, без деплоя.
8. **Осиротевшие ключи опускаются на `operative`.** `advanced_analytics` и `early_access` указывали на ранг, которого после архивации никто не достигнет — две фичи заперлись бы навсегда. Вариант «отключить гейты» отвергнут: `enabled: false` в `resolve.ts:216` → `{ ok: true }`, то есть фича открывается **всем бесплатно**, а потом её пришлось бы отбирать. Никогда не запирать уже отданное.
9. **Цена — 199 ₽/мес, без изменений.** Набор фич фактически не вырос, поменялась упаковка. На запуске важнее объём конверсий и доверие, чем ARPU; рекуррента ещё нет → поднять позже дёшево и без обиженных старых подписчиков.
10. **Годовой тариф — после запуска.** Механика срока уже есть (`setUserTier({months})` → `make_interval`), не хватает только второй цены в `tiers`. Но годовая предоплата — обязательство на год при недостроенном сервисе и ненаписанной политике возврата.
11. **Тон — сухой тактический брифинг.** «Доступ по подписке „Оперативник“. Ядро сайта остаётся бесплатным.» Без восклицаний и продающего нажима: аудитория хардкор-шутеров его остро чует.

### Второй грил — риски исполнения (06.09)

12. **Работаем на боевой БД по протоколу.** Отдельной dev-базы нет: `.env.local` → `127.0.0.1:5432` → SSH-туннель → тот же VPS, что обслуживает `cta.quest`. Поэтому петля «сменил порог → строка переехала» проверяется на **специально заведённом безобидном ключе**, боевые фичи не трогаются вообще. **Ш0 выполняется последним**, после live-verify UI, и **руками V4DYA** (§5 — необратимое). Поднимать локальную БД сочли дороже пользы.
13. **Флаг витрины — строка `sys:pricing-published` в `feature_gates`**, `kind: 'system'`, в админке отдельным блоком «Переключатели», не среди фич. Ноль миграций; уже едет в снимке entitlements и инвалидируется существующим `invalidateGating()`. Отдельная таблица `app_settings` отвергнута как схема ради одного булева; ENV — как требующая передеплоя (противоречит решению 5).
14. **Админка получает «Просмотр от лица тира».** Админ переключает себе видимый тир и ходит по сайту глазами бесплатного юзера — на общей боевой базе это единственный честный способ проверить пейволы без фейковых аккаунтов и без правки боевых гейтов. Просили ещё в «Ревизии MVP 28.07». ⚠️ **Жёсткое ограничение: превью только ПОНИЖАЕТ видимый ранг, никогда не повышает** — иначе это вектор эскалации прав. Действует только на чтение UI, реальную подписку не меняет.
15. **`/pricing` — из футера и пейволов.** «Тарифы» в пустующем `ADDITIONAL_LINKS` (`src/data/footerConfig.ts`) + ссылки из `PaywallLock` и кабинета. Главный хедер не трогаем — там игровая навигация. Ссылка в футере **скрывается вместе с флагом**, иначе ведёт в 404.

### Снято фактами, а не решением
- **Track-1 (трекер позиции) уже не гейтится** — `subscription-tiers.ts:3`: «НЕ гейтим: у конкурентов он бесплатный (паритет)», и в `FEATURE_MIN_TIER` его нет. Пункт висел в доках как открытый — дрейф, не развилка.
- **Период подписки уже поддержан** в `setUserTier`.

## Модель витрины — ядро задачи

Чистая функция, доменная логика вне JSX (§4.7). Вход — снимки, которые движок гейтинга **уже кеширует**: `getTiers()` + `getGateMap()` из `src/lib/gating/resolve.ts`.

**Состав тира T (rank > 0)** = гейты, у которых `min_tier` указывает ровно на T:

```
own(T) = { g ∈ gates | g.enabled && rank(g.min_tier) === rank(T) && g.behavior !== 'hide' }
```

- `enabled === false` — гейт не применяется (фича открыта всем), из витрины вон.
- `behavior === 'hide'` — фичу намеренно прячут от неплательщика; рекламировать то, что скрываем, нечестно и палит скрытое.
- **Каскад, а не повтор:** карточка ранга N печатает свои строки + одну «Всё из ‹имя тира N−1›». Актуально с возвращением третьего тира; на запуске двух — вырождается.
- **У `free` авто-списка НЕТ.** Секционные гейты по умолчанию `free` — авто-вывод дал бы сто строк. Карточка «Боец» — только editorial `perks` (там и живёт «База-6»).
- **Подписи:** фичи — `FEATURE_LABELS`, секции — заголовок узла из `HEADER_DICTIONARY`. Сейчас лейблы задвоены (`FEATURE_LABELS` в `gate-registry.ts` vs `FEATURE_META` в `admin/billing/gate-tree.ts`) — свести в один резолвер `gateLabel(key)`, чтобы админка и витрина не разъезжались (§4.10).
- **Два режима карточки:** с ценой и без (флаг витрины, решение 6). Режим без цены показывает состав и подпись «цена — скоро».

**Побочный выигрыш:** `/pricing` становится инструментом аудита матрицы — «массовая расстановка порогов по 31 фиче» из родительской заметки перестаёт быть слепой. Перекос 8/2 нашёлся именно так.

## Границы

- **В scope:** миграция состава тарифов данными · флаг витрины · резолвер витрины + ридер · `/pricing` · `PlanView` (сравнение) · `BillingPanel` (реальный тариф + история платежей) · связка `ProStatusPanel` → экран сравнения · один `gateLabel`.
- **НЕ трогаем:** адаптер ЮKassa и всё E5 · схему БД (`perks` и RLS уже есть; миграций **не нужно**) · годовой тариф · остальные вкладки кабинета · `feature-gating-entitlements.md` (её модель отменена, см. «Долги»).

## Работы

- **Ш0. Состав тарифов — данными, не кодом. ВЫПОЛНЯЕТСЯ ПОСЛЕДНИМ, руками V4DYA.** Через `/admin/billing`: архивировать `veteran`; перевести `advanced_analytics` и `early_access` на `operative`. Схему не трогаем. Проверить, что `veteran` никому не выдан вручную (`/admin/billing` → подписки); выданным — оставить как есть, `getSubscription` архивные слаги переживает.
- **Ш1. Флаг витрины.** Одна строка-переключатель, читаемая из уже кешируемого снимка. ⚠️ Реализационная тонкость: у `feature_gates.enabled` семантика **обратная** нашей (`false` = гейт снят = открыто всем), поэтому прогонять флаг через `requireTier` нельзя — читаем строку из `getGateMap()` напрямую и трактуем сами, конвенцию задокументировать в `gate-registry.ts`. Флаг едет в снимке entitlements: `PaywallLock` тоже печатает цену и обязан её прятать.
- **Ш2. Резолвер.** `src/lib/gating/showcase.ts` — чистые функции (`ownGatesForTier`, `buildTierShowcase`) + серверный ридер поверх `getTiers()`/`getGateMap()`. Новых кеш-тегов **не заводить**: `invalidateGating()` из админ-роутов инвалидирует и витрину — это и есть замыкание петли администрирования.
- **Ш3. `gateLabel(key)`** — единый резолвер подписи (фича/секция), переиспользуется админкой и витриной.
- **Ш4. Карточка тарифа** — `components/features/subscription/TierCard.tsx`: имя, цена (или «скоро»), каскад-строка, авто-список, editorial-перки, состояние «ваш текущий». Один компонент на три места (кабинет / `/pricing` / апселл в пейволе).
- **Ш5. `/pricing`** — публичная RSC-страница, `export const dynamic = "force-dynamic"` (§4.13). Только портальные тиры (`game_id === null`), `archived` скрыты. Флаг выключен → `notFound()`. Ссылка из `PaywallLock` для неавторизованного гостя.
- **Ш5б. Футер.** «Тарифы» в `ADDITIONAL_LINKS` (`src/data/footerConfig.ts`, сейчас пустой массив), ссылка скрыта при выключенном флаге.
- **Ш5в. Просмотр от лица тира.** Переключатель в `/admin/billing` + учёт в `resolveEntitlements`. Только понижение ранга (`min(realRank, previewRank)`), только для `getAdmin()`, хранение — кука/сессия, не БД. Плашка-индикатор «Просмотр от лица: Боец», чтобы админ не забыл, что смотрит не своими глазами.
- **Ш6. Кабинет.** `BillingPanel` — реальные имя тира, «активен до», источник; блок «История платежей» из `billing_events` (own-RLS, читать server-клиентом Supabase, **не** owner-Drizzle) с честным пустым состоянием. Ручные выдачи (`type: 'grant'`) подписывать «Выдано администратором» **без суммы** — иначе юзер видит платёж на 0 ₽. `PlanView` — экран сравнения на `TierCard`, кнопка оплаты `disabled` + подпись про запуск. `ProStatusPanel` — обе кнопки туда же.
- **Ш7. Петля.** Статус заметки → ✅, обновить срез `docs/state/release-readiness-2026-08-19.md` (п.7 «биллинг-вкладка») и родительскую `billing-subscription-management.md`.

Ветка `feat/billing-tier-showcase` — отдельная: текущая `feat/lighthouse-terrain` занята незакоммиченной работой по слоям карт.

## Критерий готовности

- [ ] `tsc` чисто, `eslint` (вкл. `cta/no-layout-px`) чисто, `next build --webpack` проходит
- [ ] **Тест петли — на безобидном ключе, не на боевых фичах** (решение 12): сменить `min_tier` → без деплоя и перезапуска строка переезжает в другую карточку на `/pricing` и в `PlanView`
- [ ] **Просмотр от лица тира** понижает видимый ранг и НЕ повышает: админ в превью «Боец» видит замки; попытка выставить превью выше своего реального тира не даёт доступа
- [ ] **Тест флага:** выключен → `/pricing` 404 и нигде на сайте нет цифры цены (вкл. `PaywallLock`); включён → цены везде
- [ ] `/pricing` отдаёт 200 **анониму** при включённом флаге (RLS `tiers_read` для `anon`)
- [ ] Добавить перк в `tiers.perks` в админке → появился в карточке
- [ ] Вкладка «Платежи» показывает реальный тариф вместо «Не настроено»; у free — честный текст, кнопка оплаты неактивна
- [ ] Юзер с записями в `billing_events` видит историю; ручная выдача — без суммы; без записей — пустое состояние, не ошибка
- [ ] NIGHTFALL: rem/шкала (не сырой px), цвет из токенов, скелетоны вместо спиннеров (§6, §4.8)
- [ ] Мобилка-смок (`/cta-mobile`)

## Гочи

- **Архивный тир — не «нет тира».** Юзер может сидеть на `archived` тире (дедовщина после переоценки — а после Ш0 это ещё и `veteran`). Из витрины архивные скрыты, но `tierMeta`/карточка обязаны отрисовать его имя в кабинете. Не сужать `TierId` до базовых слагов — он открытый `string`, и `getSubscription` это уже уважает.
- **Игровые тиры.** У `tiers` есть `game_id` (задел per-game). `/pricing` v1 — только портальные (`null`), иначе список поедет.
- **Кеш.** `getTiers`/`getGateMap` — `unstable_cache`, `revalidate: 3600`. Своей выборкой мимо них витрина разъедется с пейволом и перестанет реагировать на `invalidateGating()`.
- **Пустая БД → дефолты из кода.** `resolve.ts` fail-safe: витрина обязана пережить пустые `tiers` (покажет `TIERS` из `subscription-tiers.ts`), а не упасть.
- **`/pricing` без `force-dynamic` уронит деплой Coolify** — статический роут, читающий БД (§4.13).
- **Обратная семантика `enabled`** — см. Ш1. Единственное место, где мы читаем гейт-строку в обход `requireTier`; без комментария это выглядит как баг.

## Предусловия запуска (на V4DYA, вне кода)

1. **Editorial-перки «Бойца».** Авто-списка у free нет by design — карточка пустая без текста (там живёт «База-6: Карты · Задания · Предметы · Кодекс · Видео · Связь»).
2. **`/legal/offer` §2 «Стоимость и порядок оплаты»** — сейчас рыба с `draft: true`. Прямое предусловие включения флага: витрина с ценой при черновой оферте не публикуется.

## Долги, вскрытые разведкой (не в scope, зафиксировать)

- `feature-gating-entitlements.md` (🟡 черновик) описывает **отменённую** модель — матрицу прав по 5 осям и `src/lib/features/`, которых в коде нет. Победила лестница рангов. Пометить `superseded`, иначе следующая сессия прочитает неправду (§8.2). Ссылка на неё живёт и в `release-readiness-2026-08-19.md` §5.
- `monetization-subscriptions.md` — frontmatter «рельс ПЕРЕСМАТРИВАЕТСЯ» противоречит собственному разделу ниже («✅ РЕШЕНО: ЮKassa»). Там же — устаревшая трёхтировая таблица и открытый Track-1.
- ~~**Ложь в UI, вкладка «Безопасность»**~~ — ✅ **починено 06.09.** Статус 2FA теперь
  считается на сервере (`hasVerifiedTotp()` в `lib/auth/mfa.ts`) и передаётся пропом;
  строка-заглушка «Пароль → Последнее изменение: —» убрана (данных нет: Supabase дату
  не отдаёт, своей колонки нет — изображать её было нечестно).
  ⚠️ Положительная ветка («Включена») вживую НЕ наблюдалась: в `auth.mfa_factors` ноль
  записей на всю базу, а включать 2FA за пользователя нельзя. Предикат зеркалит рабочий
  `TwoFAView.refresh()`, но проверен только логически.
- `GatingBoundary` смонтирован в `app/layout.tsx:96`, но меню хедера не гейтится — из родительской заметки.
- `AccountCenter.tsx` — 1394 строки одним клиентским компонентом. Панели вкладок просятся в отдельные файлы; после Ш6 станет ещё толще.

---

## Результат исполнения (06.09.2026)

Ветка `feat/billing-tier-showcase`, два коммита, **не запушено**. `tsc` + `eslint` чисто,
`next build --webpack` зелёный, live-verify пройден через управление браузером.

- **`571695a6`** — Ш1–Ш6: реестр `kind:'system'` + `defaultEnabled`, тумблер
  `sys:pricing-published`, `lib/gating/showcase.ts`, `gateLabel()`, `/pricing`,
  ссылка в футере, живые «Платежи» + история из `billing_events`, `PlanView`,
  блок «Переключатели» и «Просмотр от лица тира» в админке.
- **`e66d3f41`** — переверстка по гриллу вёрстки: плитка на анатомии карточки сезона,
  единая матрица доступа на `RequirementChip`, арт по слагу с серверной проверкой.

### Что подтвердилось живьём
Тумблер включает/выключает `/pricing` без деплоя · матрица пересчитывается из гейтов ·
«Не настроено» заменено реальным тарифом/сроком/способом · превью тира режет доступ
(лимит сборок 0/3 под «Бойцом» против ∞ со своими правами) · ручка превью без админской
сессии отдаёт 403 · при отсутствии строки тумблера `/pricing` = 404 (fail-safe работает
против реальной БД).

### ⚠️ Находки, которых не было в плане

1. **`billing_events`, `tiers`, `feature_gates` — БЕЗ табличных грантов** для `authenticated`
   при живых RLS-политиках. В Postgres политика без `GRANT` мертва: запрос отбивается по
   привилегиям раньше фильтра строк. У `subscriptions` гранты есть — потому тариф и читается.
   Из-за этого «История платежей» показывает пусто там, где записи есть.
   Родительская заметка утверждает «RLS 66/66» — аудит, похоже, считал политики, а не гранты.
   **Чинится одной строкой, но это правка прав на проде → решение V4DYA:**
   `grant select on public.billing_events to authenticated;`
2. **Оба админа (`v4dyatv`, `fullkamen`) сидят на `veteran`** — ручная выдача 01.09.
   Меняет Ш0: архивация затронет их подписки. Доступ сохранится (ранг 2 остаётся, имя
   отрисуется из запасного каталога), но знать это надо до нажатия.
3. **Перекос 8/2 подтверждён визуально** — под превью «Боец» матрица показывает восемь
   строк «Оперативник» и две «Ветеран». Решение о запуске на двух тирах верное.

### 🔴 Гочи, стоившие времени (записаны и в коде)

- **`await` за флагом в теле `RootLayout` ломает пререндер.** Симптом обманчивый:
  `Error: Element type is invalid … got: null` на `/eft/quests/side-quests` — страница
  к биллингу отношения не имеет. Чтение вынесено в `PricingFooterLink` под `<Suspense>`.
  Диагностика: сборка базовой линии без своих правок → бисект по двум глобальным изменениям.
- **`npm run build | tail` возвращает код `tail`, а не сборки.** Упавший билд выглядел
  успешным (`exit 0`). Писать в файл и проверять `$?`.
- **Первая ложная гипотеза той же гочи** — `try/catch` вокруг `cookies()` якобы глотает
  сигнал динамики. Опровергнута экспериментом (с отключёнными куками падало так же).
  Но `try/catch` вокруг `cookies()` всё равно убран: глотать служебные исключения Next нельзя.
- **Инвалидация тега кеша доходит не мгновенно** — после щелчка тумблером первый заход на
  `/pricing` может отдать старое состояние, второй уже верное. Не баг, но при проверке ждать перезагрузки.
- **Наличие арта кешируется на процесс** — после добавления файла нужен перезапуск dev/деплой.

### Проведено 06.09 (по команде V4DYA)

- ✅ **Грант** `grant select on public.billing_events to authenticated` — история платежей
  ожила сразу: «Выдано администратором — Ветеран», без суммы. Гранты на `tiers` и
  `feature_gates` НЕ выдавались осознанно: код читает их owner-клиентом на сервере,
  привилегия не нужна. Их RLS-политики остаются декоративными.
- ✅ **Ш0** через админку: `advanced_analytics` и `early_access` → `operative`;
  `veteran` архивирован (удалить не дал гард «есть записи в леджере» — верно).
  Обе админские подписки остались на `veteran` с рангом 2 — дедовщина работает,
  доступ не потерян.
- ✅ **Перки «Бойца»** — черновик из 5 строк, правится одним полем в админке.
- 🔴 **Найдена и починена регрессия архивного тира:** история платежей стала печатать
  сырой слаг «veteran» вместо «Ветеран» — архивный тир выпадает из витрины, и подпись
  теряла имя. Заведён общий фолбэк `nameOfTier()` (витрина → `tierMeta` → слаг).
  Урок: **любое место, где имя тира резолвится из витрины, обязано иметь фолбэк** —
  архивных тиров в витрине нет по определению.

### Дополнение 07.09 — автопродление и права на БД

- ✅ **Управление автопродлением** в «Платежах» (`subscriptions.auto_renew` +
  `POST /api/account/auto-renew` + атом `ui/Switch`). Строилось до адаптера оплаты
  потому, что у ЮKassa рекуррент **merchant-initiated**: следующее списание инициируем
  мы сами, значит «отключить» — это наш флаг, а не запрос к провайдеру.
  Закрывает обещание §2.6 черновика оферты, которое иначе было бы пустым.
- 🔴 **Права на БД — важнее самой фичи.** `public.subscriptions` и `public.profiles`
  принадлежат `supabase_admin`, приложение ходит ролью `postgres` → **любой `ALTER`
  этих таблиц падает с `42501 must be owner`**. То есть `SUBSCRIPTIONS_DDL` не может
  применить сам себя, хотя шапка модуля утверждала обратное.
  Рабочий путь найден и записан в шапку модуля: `docker exec` в контейнер БД под
  `supabase_admin` (единственный суперпользователь инсталляции). Колонка накатана им 07.09.
  `tiers` / `feature_gates` / `billing_events` принадлежат `postgres` — с ними такой
  проблемы нет, поэтому грант и Ш0 прошли свободно.
- ✅ **Вкладка «Безопасность»** перестала врать про 2FA (`hasVerifiedTotp()`), строка-
  заглушка «Пароль → Последнее изменение: —» убрана.

### Осталось (не код, за V4DYA)
1. **`/legal/offer` §2 «Стоимость и порядок оплаты»** — предусловие включения тумблера.
   Черновик: [[offer-section-2-draft]], 5 пунктов ждут бухгалтера/юриста.
2. **Включить тумблер витрины** — после оферты.
3. **E5** — адаптер ЮKassa (внешний блокер: договор, касса 54-ФЗ, ключи).

---
*Процесс: [[engineering-loop]] · родитель: [[billing-subscription-management]] · оплата: [[payment-provider-yookassa]]*
