---
type: bug
status: done
game: —                     # CMS, кросс-игровой
severity: мелкий           # ~вывод (улучшение/UX)
platform: —
source: telegram
tg_from: v4dyadesign
tg_date: 2026-08-18T09:40:48Z
tg_message_id: 328
tg_thread: 134
tg_topic: БАГИ
tg_link: https://t.me/c/3424189876/134/328
---
# Медиа-библиотека: мультивыбор загруженных + управление содержимым

- **Раздел:** CMS — Медиа-библиотека
- **Компонент:** сетка загруженных изображений + управление файлами `~вывод`
- **Хотелки:**
  1. **Мультивыбор** уже загруженных скриншотов — например через галочку (выбрать несколько изображений из библиотеки).
  2. **Удаление и редактирование** содержимого медиа-библиотеки.
- **Ожидалось:** можно выбирать несколько ранее загруженных изображений и управлять файлами (удалять/редактировать), не только загружать новые.

> исходное сообщение (as-is, фрагмент):
> Медиа-библиотека.
> - добавить возможность выделить несколько уже заранее загруженных скриншотов - можно использовать галочку? В общем мульти выбор загруженных изображений из этой библиотеки
> - добавить возможность удалять и редактировать содержимое медиа-библиотеки

_Связано:_ [[bugs/2026-08-18-0926--media-library-thumbnail-grid.md]] (сетка 16:9).

## Резолюция (2026-08-18, /autopilot grill→code→live-verify)
**Скоуп (grill V4DYA):** мультивыбор в ОБА контекста (пикер=батч-добавить, страница=батч-удалить); включение — режим-тоггл **«Выбрать»**; «редактировать» = **переименовать (alt)**. Удаление per-item уже было.

**Фикс:**
- **DB** (`db/media.ts`): `updateMediaAlt(id, alt)` — drizzle-update существующей колонки `alt` (миграция НЕ нужна, db:push не трогали).
- **API** (`api/admin/media/route.ts`): новый `PATCH` (auth `canEditContent` + UUID + alt≤200 → `updateMediaAlt`).
- **`MediaLibrary`**: режим «Выбрать» (галочки-оверлей, клик тоглит выбор, per-item-кнопки/пик отключены), панель действий (пикер→«Добавить (N)» через новый проп `onPickMany`; страница→«Удалить (N)» с confirm+батч-DELETE), инлайн-переименование (карандаш→инпут→PATCH, оптимистично+откат), в подписи показано имя (alt).
- **`MediaPicker`**: проброс `onPickMany`. **`EditorialMarkerCard`**: `onPickMany` добавляет все URL в `draft.screenshots` разом.

**Live-verify (dev, `/admin/media`):** режим «Выбрать» → галочки, выбор 2 шт → бар «Выбрано: 2 · УДАЛИТЬ (2)»; переименование «7»→«rename-test-7» пережило полный reload (PATCH записал в БД), возвращено на «7». Пикер-контекст («Добавить (N)») — тот же компонент `MediaLibrary` (ветка isPicker), `onPickMany` прошит end-to-end. tsc чисто (0 ошибок; 2 предсуществующих warn — `void load()` и unused img-disable — не мои).
