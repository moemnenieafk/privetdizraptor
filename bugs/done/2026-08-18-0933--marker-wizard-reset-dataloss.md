---
type: bug
status: done
game: eft
severity: серьёзный        # ~вывод (потеря введённых данных)
platform: desktop          # ~вывод (визард редактора маркеров)
source: telegram
tg_from: v4dyadesign
tg_date: 2026-08-18T09:33:00Z
tg_message_id: 324
tg_thread: 134
tg_topic: БАГИ
tg_link: https://t.me/c/3424189876/134/324
---
# Карты: визард маркеров сбрасывает данные на шаге 2+ (закрывает окно редактирования)

- **Раздел:** Карты — `/eft/maps` (визард добавления маркера)
- **Компонент:** визард маркеров, шаги 2+ / удержание режима редактирования `~вывод`
- **Что не так:** когда пользователь на втором или более шаге, маркер может сбрасываться и закрывать окно редактирования → набранные данные теряются.
- **Хотелка:** после шага 1 удерживать режим редактирования — запретить сброс/закрытие окна во время работы с маркером, чтобы не терять введённое.
- **Ожидалось:** данные визарда сохраняются между шагами, окно не закрывается самопроизвольно на шаге 2+.

> исходное сообщение (as-is):
> Карты. Добавление маркера через визард-маркеров. Когда пользователь находится на втором или более шагов - запретить возможность маркеру сбрасывать и закрывать окно редактирования для предотвращения сброса набранных данных. Т.е. надо разрешить сохранять режим редактирования во время работы с маркером после шага 1.

## Резолюция (2026-08-18, /autopilot grill→code)
**Root cause:** `MapViewerClient.tsx` outside-click `mousedown`-листенер звал `closeCard()` на любой клик мимо карточки; черновик визарда — локальный state → терялся.

**Grill-решения V4DYA:** блокировать авто-закрытие **с шага 2+** (на шаге 1 клик мимо закрывает как прежде); осознанный выход — **× (и Esc)** с `confirm()` при набранных данных.

**Фикс:**
- `EditorialMarkerCard`: новый проп `onEditGuardChange(locked)` — карточка репортит `editing && stepIdx≥1`; `requestClose()` (× / Esc) с dirty-проверкой + confirm; × добавлен в шапку визарда; Esc-листенер (лайтбокс приоритетнее).
- `MapViewerClient`: `editGuardRef` + `handleEditGuard`; outside-click `onDown` пропускает close, пока `editGuardRef.current`.

tsc чисто; предсуществующие lint-warn (`cycleToLayer` before-define ×3) не мои — не трогал (§4.10). Live-verify рекомендуется.
