{
  "crons": [
    { "path": "/api/cron/sync-prices", "schedule": "0 5 * * *" }
  ],
  "ignoreCommand": "[ -n \"$VERCEL_GIT_PREVIOUS_SHA\" ] && git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- ':(exclude)docs/'"
}
